# EQUAVIA — MASTER BUILD PROMPT FOR CLAUDE CODE

Paste this entire file into Claude Code at the repo root. Work through it **one phase at a time, one section at a time**. Do not build ahead.

---

## HOW TO WORK (read first, applies to everything)

1. **Before building each numbered section:** ask me every question listed under its `ASK FIRST` block, plus anything else ambiguous. Do not silently default on wording, thresholds, tier names, data shapes, OAuth scopes, or edge cases. Wait for my answers, then build.
2. **Explore before touching.** Read the existing codebase first. These are plain HTML pages (dashboard.html, health.html, gym.html, peak.html, finance.html, planner.html, ability.html, notes.html) with inline/linked JS and localStorage persistence, deployed on Vercel. Match the existing patterns — do not introduce a framework, bundler, or build step unless a section explicitly requires a serverless function.
3. **Match the existing dark visual language** (near-black backgrounds #050506/#0a0a0b, card-based layout). New sections must be indistinguishable in style from existing ones.
4. **localStorage discipline:** every new feature namespaces its keys (`eq.books.*`, `eq.contacts.*`, `eq.sleep.*` etc.). Every feature's data must be included in the global export/import built in Phase 0.
5. **Reuse the existing chart pattern** (the Weight graph on gym.html) for every new chart. One charting approach across the whole app.
6. **Mobile-first.** Assume primary use is a phone with the app installed as a PWA.
7. **After each section:** show me what changed, list the files touched, and wait for my confirmation before moving on.
8. **Do NOT build any food/calorie logging.** Nutrition is handled in MyFitnessPal separately, outside this app. If any old "Fuel" references exist, remove them.

---

# PHASE 0 — FOUNDATION & CLEANUP (do this before any new features)

## 0.1 Identity & naming
- Unify the app name across every `<title>`, header, splash page, and manifest.
- ASK FIRST: final app name ("Equavia"? "William's Dashboard"? something else), and whether the splash-image landing page stays or the app should open straight to the dashboard.

## 0.2 Remove template artifacts
- health.html: remove the visible `// editable template · all data stays in your browser` text and embed the water tracker properly instead of a raw `po-water.html` link.
- peak.html: delete the third-party Patreon "Setup ↗" link and the pasted all-caps marketing copy block ("CAFFEINE RUNS REAL PHARMACOKINETICS…", "MEMBERS: TELL THE MENTOR…"). Rewrite the page's explanatory text in the app's own voice, or reduce it to a short one-paragraph description.
- Hide all "coming soon" UI (Gantt) — features that don't exist yet should not be advertised.

## 0.3 Empty states
- Replace every bare "—", "0/0", "Loading…" placeholder across all pages with a proper empty state: one line describing what the panel does + one clear action, in the style of gym.html's "Log your first weight to start tracking."

## 0.4 Bottom navigation
- Replace the plain text link row with a fixed bottom tab bar on mobile (icons + labels, active-state highlight on the current page). Keep a top nav or the same bar on desktop.
- ASK FIRST: there are 7+ pages and a mobile tab bar fits ~5. Which pages get first-class tabs and which fold into a "More" sheet?

## 0.5 PWA
- Add `manifest.json` (name, icons, standalone display, theme color) and a service worker (cache-first app shell so all pages load offline; localStorage data is already local).
- ASK FIRST: do you have a logo/icon file, or should a simple monogram icon be generated?

## 0.6 Global backup & settings page
- New `settings.html` (reachable from the nav/More sheet) containing:
  - **Export all** — one JSON file containing every localStorage namespace, with an export date. **Import all** — restores it, with a confirmation warning.
  - "Last backed up X days ago" indicator surfaced on the dashboard when > 14 days.
  - Global preferences: currency (default **GBP**, not CHF — update finance.html default), units, wake/bed times (moved here from dashboard modal but still editable there), Obsidian connection (single shared config — deduplicate the two separate Obsidian sync configs currently on planner.html and notes.html into one, both pages read the same stored connection).

## 0.7 Dashboard deduplication
- dashboard.html and planner.html currently both render full Goals + To Do components. Refactor: **Planner owns the full editable components; the Dashboard shows compact read-only summary cards** (today's tasks done/total, top 1–2 goals with countdown, streak) that link into Planner.
- The dashboard becomes an aggregator: add compact summary cards for Health (recovery % / sleep hrs once Whoop or sleep log has data), Fitness (last session, current weight + 7-day avg), Finance (net worth + month cash flow), each linking to its page.
- ASK FIRST: confirm this direction before refactoring, and which summary cards you want in which order.

---

# PHASE 1 — CORE ADDITIONS

## 1.1 Book Tracker (Ability page)
- New card section on ability.html: three statuses — **Want to Read / Reading / Read**.
- Each book: title, author, status, optional 1–5 star rating, optional notes. "Read" entries record and display date finished.
- Add via "+" button; change status via dropdown (no drag needed on v1).
- Optional cover images: fetch from Open Library covers API by title/author search (`https://openlibrary.org/search.json`), cache the image URL; graceful text-only fallback.
- A "Reading" book can optionally show a progress % slider.
- ASK FIRST:
  - Cover images on or off for v1?
  - Progress % on currently-reading books — wanted?
  - Show a yearly "books finished" count (Goodreads-challenge style) with a target?

## 1.2 Sleep Log + Trend Chart (Health page)
- Below the existing WHOOP sleep-stages block: nightly total sleep hours log.
- If the WHOOP connection is live and returns sleep totals, auto-fill each night from it; manual entry/override always available for missing nights. Manual entries flagged visually as manual.
- Line chart (existing chart pattern): sleep hours per night, toggle 30/90 days, horizontal reference line at a configurable target.
- ASK FIRST:
  - Is the "Connect WHOOP" button currently wired to the real WHOOP API (OAuth working), or is it a placeholder? (Determines whether auto-pull is buildable now or the log starts manual-only.)
  - Sleep target in hours?
  - Also chart a 7-night rolling average line?

## 1.3 Relationship Tracker (Planner page)
- New card section: contacts with **name, relationship type (family/friend/colleague/other), tier, target check-in cadence in days**.
- "Logged contact today" button per person → updates last-contacted date. Also allow backdating ("logged contact on [date]") for catch-up entry.
- **Overdue flag** when days-since-contact > cadence; list sorts overdue-first, most-overdue at top. Days-overdue count shown.
- Dashboard: if anyone is overdue, the Planner summary card shows "N people overdue" badge.
- Optional per-contact notes field (what you last talked about — useful before reaching out).
- ASK FIRST:
  - Exact tier names and how many ("priority tier" was placeholder — e.g. Inner circle / Close / Network, or A/B/C?). Should tier auto-suggest a default cadence (e.g. Inner=7d, Close=14d, Network=30–60d)?
  - Birthday field with an upcoming-birthday flag — wanted?
  - Roughly how many contacts will this hold (affects whether search/filter is needed at v1)?

## 1.4 Notification Hub — Gmail, READ-ONLY (Main dashboard)
Purpose: see that a message arrived and what it says **without opening Gmail or having mail apps on the phone** — no doomscroll vector. **No composing, no replying, no sending from this app.**
- Connect Gmail via Google OAuth using the **`gmail.readonly` scope only** (deliberately minimal — easier consent, zero send risk).
- Implementation: Google Identity Services token flow client-side (PKCE); no server storage of tokens. Token kept in memory/session, silent re-auth where possible.
- Panel on dashboard: sender, subject, snippet, time for the most recent N messages. Tapping a message expands the snippet to the fuller plain-text body preview **in-app** (still read-only). An external "Open in Gmail" link is allowed as an explicit escape hatch.
- Unread indicator + count badge. Refresh: manual button + polling interval.
- Instagram/Meta DMs: **excluded permanently** — personal-account third-party DM access isn't available (Basic Display API deprecated; business-only review process). Do not build around it.
- Cross-link (small but high value): if a Gmail sender's name/email matches a Relationship Tracker contact, show a small badge on the message ("tracked contact") and offer one tap → "count this as contact logged."
- ASK FIRST:
  - How many messages to show (10? 20?), unread-only or all recent?
  - Polling interval (every 5 min while open? manual only?)
  - Allow "mark as read" from the app? (Requires `gmail.modify` scope instead of readonly — say no if you want the strictest scope.)
  - One Gmail account or multiple?

## 1.5 Calendar Quick-Add → Google Calendar (Planner)
- On planner.html's Calendar section: input "What are you doing?" + date/time picker + duration.
- On save: create the event via Google Calendar API (same Google OAuth session as 1.4 where possible — request `calendar.events` scope) AND render it in the local in-app calendar immediately. Inline success/failure state.
- Also pull upcoming Google Calendar events into the in-app calendar view (read), so the "Connect Google Calendar" button that already exists becomes real.
- ASK FIRST:
  - Which Google account and which calendar (if several)?
  - Two-way (show Google events in-app) confirmed, or push-only?
  - Should quick-add support natural-ish parsing ("gym tomorrow 6am") or strictly the structured fields for v1?

## 1.6 Color-Coded Live Goal Countdown
- Applies to the existing live day/hr/min/sec countdown per goal.
- `percent_remaining = (deadline − now) / (deadline − start_time) × 100`
- Color stops: 100–76% neutral · 75% dark green · 50% green · 40% light green · 35% lighter green · 30% yellow · 20% orange · 10% red · 5–0% dark red + blinking.
- **Exception:** if the goal's progress slider is at 100%, always neutral — skip the gradient entirely.
- Implement as discrete steps or smooth HSL interpolation — whichever renders cleaner against the dark theme; show me both if trivial.
- Accessibility: "blinking" should be a slow opacity pulse (~1s ease), not a hard flash; respect `prefers-reduced-motion` by falling back to static dark red.
- ASK FIRST: confirm the pulse-instead-of-blink substitution is acceptable.

## 1.7 Goal Archive at 100% → History + Obsidian Folder
- When a goal's progress slider hits 100%: show an **Archive** button on that goal.
- On archive: remove from Dashboard entirely → append to a collapsed/expandable **Goal History** section on planner.html only (shows title, completion date, linked project count).
- Obsidian: using the existing per-project Obsidian page sync, move/copy the archived goal's linked projects' pages into a vault folder named after the goal title (create folder if missing, sanitize illegal filename characters).
- Archived goals and their linked projects become **read-only** — sliders and edits disabled, with a visible "archived" state.
- ASK FIRST:
  - Folder naming: exact goal title, or `Archive/<goal title>`, or date-prefixed (`2026-07 — <goal>`)?
  - Should un-archiving be possible, or is archive permanent?
  - If a project is linked to two goals and one archives — what happens? (Propose: project stays live until all linked goals archive; confirm.)

## 1.8 To-Do Completion Rate Chart (Main dashboard)
- Daily completion % = tasks completed ÷ tasks scheduled that day × 100. Store a daily snapshot at day rollover (6 AM reset already exists — snapshot at reset).
- Line chart under the To Do summary: toggle 4 / 12 / 52 weeks, raw daily points + **7-day rolling average line** (daily % is noisy; the average is the real signal).
- ASK FIRST:
  - Tasks pushed to tomorrow — count as missed for that day (recommended, keeps the metric honest), or excluded?
  - Days with zero scheduled tasks — skip in the chart, or count as 100%? (Propose: skip — gaps render as breaks.)

---

# PHASE 2 — "ASK CLAUDE" PAGE (in-app AI chat over my data)

- New nav entry + `ask.html`: standard chat UI (message list, input, send).
- Backend: **Vercel serverless function** (`/api/chat`) calling `https://api.anthropic.com/v1/messages`. The Anthropic API key lives ONLY in a Vercel environment variable — never shipped client-side, never in the repo.
- Context injection: before each request, the client assembles a compact current-state summary from localStorage (selected domains only) and sends it with the message; the serverless function forwards it as system context.
- Include a per-message domain toggle row (chips: Health · Fitness · Goals/Tasks · Finance · Habits · Relationships) so context is opt-in per conversation — full-everything context on every message wastes tokens.
- Basic protections in the serverless function: max tokens cap, simple rate limit, reject non-POST.
- Show estimated context size ("~2.1k tokens of your data attached") so cost stays visible.
- ASK FIRST:
  - Which domains ON by default?
  - Session-only history, or persist conversations to localStorage?
  - Model: `claude-sonnet-4-6` (smarter, costlier) vs `claude-haiku-4-5-20251001` (cheap, fast) as default — or a toggle?
  - Confirm you'll add `ANTHROPIC_API_KEY` in Vercel env settings.

---

# PHASE 3 — BORROWED PATTERNS FROM REFERENCE APPS

Build in this order; each is standalone.

## 3.1 Year Grid per habit (from Way of Life)
- ability.html: next to each habit's streak, a whole-year grid (365 cells, green = done, red = missed, grey = not yet/paused). Success % for the year shown alongside — so one miss doesn't visually erase months of consistency.
- Compact per-habit mini-grid on the card; tap to expand full-year view.

## 3.2 Habit Health Score with decay (from Loop Habit Tracker)
- Alongside (not replacing) the streak: a 0–100 score per habit using exponential smoothing — completing raises it toward 100, a miss decays it gradually instead of zeroing it. Loop's approach: score = previous × k + done × (1 − k), k derived from habit frequency.
- Display as a small ring/bar per habit. ASK FIRST: keep the fire-streak too, or make the score primary?

## 3.3 Weekly Report widget (from Gyroscope)
- One combined report card, generated at week end, spanning: Health (avg sleep, avg recovery if Whoop live), Fitness (sessions done, total volume, weight 7-day avg change), Planner (task completion %, goals progressed), Ability (habit success %), Finance (month cash-flow pace).
- Renders as a single shareable-looking card on the dashboard; keeps the last 12 weeks browsable.
- Also: one tap "Write to Obsidian" → weekly note via the existing sync.
- ASK FIRST: which day/time does the week close (Sunday 9 PM?), and which metrics make the cut for v1.

## 3.4 Quick Capture from anywhere (from Second-Brain templates)
- A floating "+" (or long-press on the nav's center tab) available on every page → small sheet: text input + destination chips (Task today · Task tomorrow · Note · Shopping list · Book to read). Saves to the right store without leaving the current page.

## 3.5 Correlation view (from Exist.io)
- New card on health.html: pick metric A × metric B from tracked data (sleep hrs, recovery %, task completion %, habit score, weight 7-day trend) → scatter/dual-line over the last 60–90 days + Pearson r with a plain-English line ("On nights under 7h sleep, next-day task completion averages 22% lower").
- Honesty rules: require ≥ 14 paired data points before showing r; always label it correlation, not cause.
- ASK FIRST: the 2–3 metric pairs you actually care about first, so defaults are pre-set.

## 3.6 Auto-suggest time slot (from Motion — lightweight version)
- On unscheduled Planner tasks: a "suggest slot" button → looks at today's in-app + Google Calendar events, finds the first open gap ≥ the task's duration inside waking hours, proposes it; one tap accepts and creates the calendar block.
- Rules-based only — no AI needed. ASK FIRST: default task duration when unset (30 min?), and protected blocks it must never schedule over (training window 6:00–7:30 AM, meals, wind-down after 9 PM?).

---

# ACCEPTANCE CHECKLIST (verify after all phases)

- [ ] No page shows template artifacts, third-party links, or "coming soon" stubs
- [ ] Every feature's data round-trips through global Export/Import
- [ ] App installs as a PWA and all pages open offline
- [ ] All charts share one visual pattern
- [ ] Gmail integration is readonly-scope and cannot send anything
- [ ] No API key of any kind appears in client-side code or the repo
- [ ] Dashboard aggregates; Planner owns tasks/goals; no duplicated editable components
- [ ] Archived goals are immutable and present in Obsidian under their folder
- [ ] Every new section matches the dark card aesthetic on a 380px viewport

---

# PHASE 4 — ASK CLAUDE → FULL AI ASSISTANT

Requires Phase 2 (Ask Claude page + serverless function) complete. Work section by section, ask the `ASK FIRST` questions before building each.

Global rules for this phase:
- The assistant can READ everything and WRITE only through defined tools. No tool = no action.
- The assistant can never contact the outside world (no email sending, no messages, no posting). Read-only Gmail stays read-only.
- Every write the assistant makes is tagged `source: "assistant"` in its localStorage record, and listed in an "Assistant activity" log viewable in Settings — full audit trail of what it did and when.
- All costs run against my own Anthropic API key. Show token estimates where practical.

## 4.1 Tool Use — write access to the app

Define the app's actions as Anthropic tools in the serverless `/api/chat` call. Claude requests a tool; the CLIENT executes it against localStorage and returns the result; the loop continues until Claude produces a final text reply.

Tool set (v1):
- `add_task(title, day: "today"|"tomorrow")`
- `complete_task(task_id)` / `push_task_to_tomorrow(task_id)`
- `log_weight(kg, date?)`
- `log_sleep(hours, date?)`
- `log_contact(contact_name, date?)`
- `add_calendar_event(title, date, time, duration_min)` — local + Google if connected
- `add_shopping_item(name, grams?, qty?)`
- `add_book(title, author?, status)`
- `check_stack_item(item_name)` — mark a supplement taken
- `log_habit(habit_name, done: true|false, date?)`
- `add_goal_note(goal_id, note)`
- `remember(fact)` / `forget(fact_id)` — see 4.3
- `get_data(domain, range)` — explicit read tool so context injection can shrink: instead of front-loading everything, Claude pulls what it needs

Rules:
- Logging/reading tools execute silently. Destructive or irreversible ones (delete, archive, overwrite an existing entry) require an in-chat confirm chip before executing.
- Tool results render as small inline action cards in the chat ("✓ Logged 82.4 kg") with an undo link where feasible (undo = delete the created record).
- Unknown references ("push the dentist thing") — Claude uses `get_data` to search tasks, asks if ambiguous.

ASK FIRST:
- Confirm the v1 tool list — anything to add/remove?
- Should `add_calendar_event` be allowed to write to Google Calendar directly, or local-only with a "sync" confirm?
- Undo window — keep it simple (immediate undo link only) or a full revert log in Settings?

## 4.2 Universal command bar / natural-language quick log

- The Quick Capture "+" (Phase 3.4) gains a "smart" mode: free text goes to the assistant with tools enabled, parsed into the right records. "slept rough, 6 hours, skipped morning stack, weight 82.9" → sleep entry + stack state + weight log in one pass, with a compact result card listing what was logged.
- Structured mode (destination chips) remains as the zero-cost path; smart mode is opt-in per capture because it costs a token round-trip.
- ASK FIRST: which model for smart-capture parsing — Haiku is likely sufficient and keeps per-capture cost trivial. Confirm.

## 4.3 Persistent memory

- `eq.assistant.memory` store, max ~30 entries, each `{id, fact, created}`.
- Claude can call `remember()` when I state a durable preference or fact, and `forget()` when I correct one. All memory entries injected into the system prompt of every assistant call.
- Settings page: memory viewer with manual add/edit/delete.
- Seed on first run by asking me 3–5 onboarding questions (current goal phase, training split, sleep target, tone preference for advice).
- ASK FIRST: confirm the cap, and whether the assistant should ask permission before saving a memory or save silently and show a "remembered" chip.

## 4.4 Morning Brief

- Dashboard card, generated on first open after the 6 AM reset (cached for the day; manual regenerate button).
- Serverless assembles: last night's sleep (Whoop or log), recovery % if live, today's tasks + calendar, overdue contacts, unread-count from Gmail hub, subscription renewals ≤ 7 days, habit states, weight 7-day trend.
- Claude writes ≤ 6 lines: state of play + ONE recommended focus for the day. No generic filler, no motivational padding.
- Evening variant ("Close the day" button after 8 PM): reviews completion vs plan, asks for tomorrow's top task if unset, writes the day summary to Obsidian daily note via existing sync.
- ASK FIRST: auto-generate on open vs button-only (auto = 1 API call/day baseline cost). Include evening variant in v1?

## 4.5 WEEKLY AUDIT — the core review feature

Runs at week close (default Sunday evening; manual "Run audit now" always available). Replaces/absorbs the static weekly report widget from Phase 3.3 — the Gyroscope-style stat card becomes the header of this audit.

**Data in:** the full week across every domain (sleep, recovery, training sessions + volume, weight trend, task completion %, habit grid, goals progressed, contacts logged vs overdue, finance cash-flow pace, stack adherence) PLUS the previous week's audit entry, so every audit is comparative — trajectory, not snapshot.

**Output structure (fixed sections, rendered as cards):**
1. **The week in numbers** — compact stat header (the Phase 3.3 metrics).
2. **What went well** — 3–5 specific, evidence-cited wins ("Stack adherence 96%, up from 84%"). No participation trophies: if the week was poor, this section is allowed to be short.
3. **What went badly** — equally specific, equally evidence-cited. Direct language, no cushioning. Includes broken commitments from last week's audit ("Last week you accepted 'walk after Meal 2 daily' — logged 2/7").
4. **What to do better** — the 2–3 highest-leverage fixes for next week, each tied to a metric that would prove it worked.
5. **Suggested additions** — 1–3 NEW routine/habit/tracking suggestions I can adopt or ignore. Each renders with **[Implement] [Ignore]** buttons:
   - **Implement** → the assistant uses its tools to create the actual habit/task/stack item/rule immediately, tagged as originating from audit week N.
   - **Ignore** → recorded, and the assistant does not re-suggest the same idea for at least 4 weeks (store ignored suggestions with a cooldown date).
6. **Verdict** — one line, one grade or score for the week (ASK: letter grade, /10, or a one-word verdict?).

**Behavior rules:**
- Every claim must cite a number from the data. If data is missing for a domain, say "no data" — never infer or pad.
- Implemented suggestions are automatically checked in the NEXT audit's section 3/2 — the loop closes itself.
- Audits persist (`eq.audits.*`), browsable as a history list; each also writes to Obsidian as `Audits/2026-W29.md` via existing sync.
- Tone: direct, specific, zero flattery. The audit's value is honesty.

ASK FIRST:
- Week close day/time (Sunday 8 PM?).
- Auto-run at close vs manual-only (auto = 1 larger API call/week — this one is worth Sonnet-class; confirm model).
- Verdict format (grade / score /10 / one-word)?
- Any domain to EXCLUDE from audit scope (e.g. finance private from this view)?
- Hard cap on suggestion count per week (default 3)?

## 4.6 Standing rules engine

- Settings section: user-defined rules `{trigger, condition, action}` checked locally on app open — plain JS evaluation, NO API call unless a rule fires and its action is "assistant message."
- Starter rule templates: recovery < 33% → suggest training swap · tracked contact emailed → surface on dashboard · weight 7-day avg moved > X kg in 7 days → flag · habit health score dropped below 60 → flag · subscription renewing in 48h → flag.
- When a rule fires with an assistant action, Claude writes the one-line message with the triggering numbers included.
- The weekly audit (4.5) may propose new rules in its suggestions section; Implement creates them here.
- ASK FIRST: which 3–4 rules to ship enabled by default.

## 4.7 Voice input

- Web Speech API mic button on the assistant chat input and the smart quick-capture sheet. Free, on-device recognition, no key.
- Transcript is shown before sending (tap to edit), then flows through the normal tool loop.
- Graceful fallback: hide the mic if the browser doesn't support SpeechRecognition.

## 4.8 Conversational weekly review (optional layer on 4.5)

- After the audit renders: "Review this week with me?" → 3 short questions max (what felt hardest, what to protect next week, anything the data missed). Answers appended to the Obsidian audit note and stored as context for next week's audit.
- Strictly optional and skippable — the audit must stand alone without it.
- ASK FIRST: include in v1 or defer.

---

## Phase 4 acceptance checklist
- [ ] Assistant can log/create via every v1 tool; destructive actions gated behind confirm
- [ ] Every assistant write is tagged and visible in the Settings activity log, with undo where feasible
- [ ] Assistant cannot send, post, or contact anything outside the app
- [ ] Memory persists, is capped, and is fully user-editable in Settings
- [ ] Morning brief ≤ 6 lines, generated once daily, from real data only
- [ ] Weekly audit: cites numbers for every claim, compares against previous week, tracks implemented/ignored suggestions with cooldown, writes to Obsidian
- [ ] Implement buttons create real records immediately; Ignore suppresses re-suggestion ≥ 4 weeks
- [ ] Rules engine evaluates locally; API calls only on fire
- [ ] Voice input works on supporting browsers, hides elsewhere

---

# BUILD LOG (progress notes — updated as work proceeds, not part of the original prompt)

Kept here so a fresh session can pick up exactly where a previous one left off without re-deriving decisions. Format: section, date, decision/outcome.

## Phase 0

- **0.1 Identity & naming** — DONE (2026-07-18). App name: **Equavia**. Splash/landing page (index.html) stays. "Vitality" sub-brand name folded into Equavia everywhere it was user-visible (peak.html title, train.html's "Saved to your ___ dashboard" line); the internal `window.Vitality` JS bridge object name was deliberately left unchanged (invisible plumbing, ~15 call sites across 6 files, no user-facing benefit to renaming). Icon source for Phase 0.5: reuse `images/Index Landing Page.webp` (the red V mark already used as favicon). Every page's `<title>` now follows "{page} — Equavia" except the two home surfaces (index.html, dashboard.html), which are plain "Equavia". privacy.html also got a Google Calendar disclosure added to its third-party-services list (was missing despite the OAuth backend already existing).
- **0.4 Bottom navigation** — grouping decided: "Core 8 + More(3)". First-class tabs: Main, Health, Fitness, Peak, Finance, Planner, Ability, Notes. Folded into a More sheet: Train, Vitals, Brand.
- **0.6 currency** — re-basing finance.html's storage from CHF to GBP (not just the display default). No real finance data exists yet, so no migration/conversion logic needed — clean swap of the canonical unit.
- **0.7 dedup** — confirmed: Planner becomes sole owner of full editable Goals/Tasks/Projects/Checklists; Dashboard becomes compact read-only summary cards linking into Planner. Card order: Tasks → Goals → Health → Fitness → Finance.
- **Check-in cadence for the rest of this build**: bundle low-risk/cleanup sections and report once done; pause for explicit go-ahead before/after high-blast-radius changes (nav overhaul, the dashboard/planner dedup, anything touching finance.html's stored data) and at phase boundaries.
- **0.2 Remove template artifacts** — DONE (2026-07-18). health.html: removed the "editable template" attribution line (water tracker was already a proper iframe embed, nothing else to do there). peak.html: removed the Patreon "Setup ↗" link (`patreon.com/RowanTBK/membership`) and the two marketing-voice caption lines inside the "How this plugs in" diagram; kept the diagram itself (genuinely useful, not marketing). dashboard.html + planner.html: removed the "Gantt" tab and its stub from the Projects section (left the disabled/tooltipped Filter+Sort buttons alone — already honest about being unavailable). Found but did NOT touch: vitals.html and train.html both still show a `.starterTag` banner ("… starter — the value is what you build · the videos show how") — same leftover-template flavor as the Patreon link, worth a follow-up cleanup pass but out of scope for what was asked.
- **0.3 Empty states** — audited, no changes needed. Already extensively implemented app-wide (finance/dashboard/planner/ability/gym/train/notes/brand all have proper contextual empty states). Note for future-me: don't assume a gap exists from a partial function read — nearly added a duplicate empty-state handler to health.html's supplement tracker before finding the real one further down in the same function.
- **0.4 Bottom navigation** — DONE (2026-07-18). topbar.js rewritten as the single source of truth for nav: one `PAGES` array drives both a desktop top row (all 11 pages, in-flow, replaces the old per-page `cat-tabs`) and a mobile fixed bottom bar (8 core pages in a horizontally-scrollable strip + a pinned "More" tab opening a sheet for Train/Vitals/Brand), toggled via a 768px media query. Deleted the duplicated `cat-tabs` HTML/CSS and the "remove the shared bottombar" inline script from all 9 pages that had them (ability, dashboard, gym, finance, health, peak, vitals, train, planner); notes.html and brand.html needed no changes since they already deferred to topbar.js. train.html's embedded-mode `.starterTag` cleanup logic was preserved (unrelated to nav, still needed when train.html is iframed inside gym.html's Fitness tab).
  - Bug caught during verification: the mobile bottom bar's pinned "More" tab wasn't visible at narrow widths — root cause was `.bottombar-scroll` (flex/grid item with `overflow-x:auto`) not shrinking below its content's natural width, pushing the pinned sibling out. Fixed by switching `.bottombar` to `display:grid; grid-template-columns: minmax(0,1fr) auto` with explicit `min-width:0; overflow-y:hidden` on the scroll region. Verified via `getBoundingClientRect()`/computed-style dump, not just eyeballing screenshots — headless Edge's `--window-size` flag doesn't reliably produce the exact requested viewport on this machine (requesting 390px actually rendered ~490-520px), which was the real source of confusion before the computed-style check clarified it.
  - Discovered while testing: notes.html (and presumably train/vitals/brand/peak) fetch live from the real production Supabase backend on load, even under automated headless testing — there's no test/staging separation. Testing was read-only (page loads + one click on an isolated scratch page, no form submissions), so nothing was written, but worth knowing before any future automated testing touches these pages more actively.
  - No project skill existed for running/screenshotting this app; verification used headless Edge directly (`msedge.exe --headless=new --screenshot`) since Python/Node/chromium-cli weren't available in this environment. Worth `/run-skill-generator` if this becomes a recurring need.
- **starterTag cleanup** — DONE (2026-07-18). vitals.html, train.html, and brand.html (found while cleaning the first two) all had a leftover `"… starter — the value is what you build · the videos show how"` banner plus its CSS, same tutorial-template origin as the Patreon link removed from peak.html in 0.2. Removed the banner + CSS from all three; also removed train.html's now-pointless embedded-mode script that only existed to hide that banner when iframed inside gym.html.
- **0.5 PWA** — DONE (2026-07-18). Icon source: `images/Index Landing Page.webp` (the red V mark), cropped/padded into `images/icon-{192,512}.png` (edge-to-edge "any" purpose) and `images/icon-{192,512}-maskable.png` (62% safe-zone "maskable" purpose) plus `images/apple-touch-icon.png` (180x180). Generated via headless Edge rendering an HTML crop of the source webp to canvas-sized screenshots (GDI+/System.Drawing can't decode webp directly — confirmed by testing), with 192px versions downscaled from the verified-good 512px renders after headless screenshot mode turned out to distort/mis-crop at small requested window sizes. `manifest.json` added at repo root: name/short_name "Equavia", `start_url: index.html` (consistent with keeping the splash page), `display: standalone`, `background_color`/`theme_color: #050506`, all 4 icons registered. `sw.js` added: cache-first for all same-origin GET requests, precaches the full app shell (14 HTML pages + 5 shared JS files + manifest + 7 images) on install, cross-origin requests (Supabase CDN, WHOOP/Google/Anthropic APIs) pass through untouched. Registration centralized in topbar.js (`registerServiceWorker()`, skipped when embedded in an iframe) rather than duplicated per-page. All 14 pages got `<link rel="manifest">`, `<meta name="apple-mobile-web-app-capable">`, `<meta name="apple-mobile-web-app-title">`, and their `apple-touch-icon` href switched from the raw webp to the new PNG (3 pages — finance/planner/ability — had no icon links at all before this and needed them added fresh, not just updated).
  - Verified: manifest.json parses and every icon path resolves; every one of sw.js's 27 precache URLs returns 200 from a real local server; service worker registration succeeds against `http://localhost` (secure-context requirement — file:// can't register a service worker at all, confirmed). Could NOT fully verify activation/precache-completion in headless Chrome — `navigator.serviceWorker.ready` never resolved even at a 25s virtual-time budget, which looks like a headless/virtual-time tooling limitation (SW lifecycle isn't a JS-timer-driven thing virtual time can fast-forward) rather than an app bug, given registration itself succeeds and every precached URL is independently confirmed valid. Worth a real-device check after deploy: open the app, check Application > Service Workers in devtools, confirm it shows "activated and running" and the cache has ~27 entries.
- **0.6 Global backup & settings page** — DONE (2026-07-18). New `settings.html`, added to topbar.js's PAGES array in the More sheet (not a core tab).
  - **Backup**: Export All collects every localStorage key into one JSON file (`{app, exportedAt, data}`), downloads client-side via Blob, stamps `eq.lastBackupAt`. Import All parses the file, confirms via `confirm()` (shows the backup's date + key count, warns it replaces everything), then `localStorage.clear()` + replays every key, reloads the page. Dashboard shows an amber nudge banner ("You have never backed up your data" / "Last backed up N days ago") when backup is missing or >14 days old, linking to Settings — only renders past the threshold, hidden otherwise.
  - **Currency**: re-based finance.html's storage from CHF to GBP as the canonical unit, not just the display default (matches what was actually approved — no real data existed to migrate, so this was a clean swap, no conversion logic needed). This touched ~60 spots: renamed internal identifiers (`amountCHF` to `amountGBP`, `curCHF`, `netCHF`, `deltaCHF`, `grandCHF`, `annualSubsCHF`, `nwGrandCHF()`, `bgtSumCHF()` — all via exact-match `replace_all`, verified clean with a post-rename grep), flipped the exchange-rate fetch to anchor on GBP (`open.er-api.com/v6/latest/GBP`) with CHF now just another fetched rate, updated every fallback default and reordered every currency `<select>` so GBP is first (=default-selected), and updated stale comments. CHF/USD/EUR remain fully selectable, just no longer the base. Verified via screenshot — "GBP 0" and GBP-selected everywhere, no console errors.
  - **Obsidian consolidation — this was a real bug, not just duplication**: planner.html's settings-modal Save button called `ObsidianSync.setConfig(readObsidianSettingsForm())` with NO merge against the existing stored config, and its form doesn't include `folder` — so saving from Planner silently reset Notes' folder setting back to the default. notes.html's Save had the mirror-image bug against `vaultName`/`projectsFolder`. (Planner's own "Test connection" handler did merge correctly — Save didn't; inconsistent even within the one file.) Fixed by moving the full 8-field form to settings.html as the only place that edits this config, always reading/writing the complete object. planner.html and notes.html's gear icons are now plain links to settings.html; their duplicated modals, and the now-dead JS behind them, are gone. Caught and fixed one collateral bug while removing notes.html's modal: an Escape-key handler unconditionally read `$('setModalBg').classList` — with the modal gone that would throw on every Escape press.
  - **Wake/bedtime**: added to Settings (reads/writes the same `day_ring_times_v1` key dashboard.html's own modal already used) — additive, dashboard's own editor is untouched, both stay in sync since they share the key.
  - **Units**: gym.html (kg/lb) and po-water.html (kg/lb, separate from its own oz/ml water-volume unit) had two independent, already-diverging weight-unit toggles, each baked into that page's own state blob rather than a standalone key — full unification would mean restructuring both pages' state persistence, more invasive than this ask justified. Went with a low-risk middle ground instead: a new shared `eq.units.weight` key, set from Settings, that both pages now check as a fallback only when they have no saved choice of their own yet (`s.units = s.units || sharedWeightUnit() || CONFIG.units || 'kg'`) — sets the default for new setups without touching any existing per-page choice.
  - Verified via a real local HTTP server (headless file:// can't register service workers or exercise some of this cleanly) — scripted every settings.html control end-to-end (currency change, units click, wake/bed save, Obsidian save) and confirmed each wrote the exact right value with no exceptions; screenshotted dashboard/planner/notes/settings afterward to confirm no visual or console regressions. Did not get a clean automated read on the literal file-download click (headless Chrome's download handling hung without CDP-level configuration) — the download-triggering code is standard Blob+`<a download>`, and the data-assembly logic underneath it was verified directly instead.
- **0.7 Dashboard/Planner deduplication** — DONE (2026-07-18). Turned out bigger than the master prompt described: 5 sections were fully duplicated with dashboard.html and planner.html both reading/writing the exact same storage keys (Tasks/`goals:` prefix, Goals/`longterm_goals_v1`, Calendar/`local_cal_events_v1`, Checklists/`checklist:items:`+`checklist:done:`, Projects/`projects_v1`) — not just Goals+Tasks. Day Ring and Goal Ticker were the only dashboard-exclusive pieces and are untouched.
  - Dashboard now shows 6 compact read-only summary cards in a single stacked column (`.dash-summary-stack`), each linking to the page that owns it: **Tasks → Calendar → Goals → Health → Fitness → Finance** (Calendar inserted right after Tasks — both are "what's happening today" — since the 6th-card decision came after the original 5-card order was already confirmed; flag if a different position is wanted). Removed entirely from dashboard.html: the editable Goals card, the editable To-Do card (checkbox/drag/inline-edit/Polish), the full Projects kanban, the full Calendar (day/week/month grids + add/edit event modal), and the sidebar Checklists + Quick Note/Shopping links. Planner already owned equivalent full editable versions of all of these — confirmed byte-for-byte via screenshot that Planner's Tasks/Goals/Calendar sections still work standalone and show the same underlying data (a real goal titled "grind" rendered identically on both pages before the cut).
  - Google Calendar's OAuth token handback lands on `dashboard.html#gcal_access=...` (confirmed from earlier exploration) — kept all of the GCal token machinery (`processGcalHashTokens`, `ensureValidGcalToken`, `fetchGcalEvents`, etc.) intact and still called at boot; only removed the UI-rendering half (grids, the connect/disconnect button handlers, the event modal). Losing this by accident was the single biggest risk in this section and was checked explicitly before cutting anything.
  - Health's recovery score and Fitness's weight/session data aren't cached anywhere simple enough for the dashboard to read directly (WHOOP recovery is fetched live and never stored; gym.html's weight lives in its own array but "last session" is buried in a `state.logs` object keyed by exercise ID). Added a `publishFitnessSummary()` in gym.html (writes `eq.fitness.summary` on weight save, on set log, and at boot) and a summary write in health.html's WHOOP fetch success path (writes `eq.health.summary`). Finance reads its `nw:bank/stocks/crypto/other` and `budget:income/expenses` keys directly — already simple, stable arrays, no publishing step needed.
  - Removed ~450 lines of now-dead JS (row-building, renderers, Add/Polish/AI-polish handlers, the Projects kanban, the Checklists sidebar, calendar grid rendering) after mapping every remaining reference with grep to confirm nothing else called them. Left the now-unused `dgt-*`/`proj-*`/`cal-*`/`chk-*` CSS in place (~150 lines) — genuinely inert, and removing it added risk for zero functional benefit given how much else this section already touched. Worth a dedicated cleanup pass later if the file size matters.
  - **Bug found and fixed during verification, not before**: `publishFitnessSummary()` worked correctly when called from gym.html's own boot sequence, but gym.html pulls weight data from its own separate inline Supabase sync (a different, pre-existing implementation from `sync.js` — worth knowing about, not something this session touched) which resolves *after* boot and reloads `wtEntries` from a `pcRerender()` function without re-publishing the summary. First test run showed the Fitness card stuck on "Log your first session..." despite gym.html clearly showing real logged weight (81.0kg) — caught by testing with a persistent browser profile across both pages rather than trusting a single-page screenshot. Fixed by adding the publish call inside `pcRerender()` too. Re-verified after the fix: Fitness card correctly showed "81kg · 80.3kg 7d avg · 1d since last session".
  - Verified end-to-end with a real local server and a persistent shared browser profile across page loads (necessary specifically to catch the timing bug above — a fresh profile per test would have hidden it). Confirmed zero JS errors via an injected `window.addEventListener('error', ...)` in the actual page context, all 6 cards computing correct text (both empty-state and real-data cases), Planner unaffected, desktop and mobile layouts both clean.

## Phase 1

- **Phase 1 questions (Rounds 1-4)** — answered before building. Book covers: off. Reading progress %: no. Contact tiers: Inner circle / Close / Network, with auto-suggested cadence (7/14/30 days). Birthday field: yes. Gmail: 10 messages, unread-only, manual refresh only, `gmail.readonly` scope only, one account. Calendar quick-add: primary calendar, two-way sync, natural-ish parsing. Goal archive: `Archive/<goal title>` Obsidian folder naming, un-archiving is permanent.
- **1.1 Book Tracker (ability.html)** — DONE (2026-07-18). Three shelves (Want to Read / Reading / Read) backed by a new `books_v1` key, array of `{id, title, author, status, rating, finishedAt, notes}`. Status dropdown per book drives shelf placement; switching to "read" reveals a 5-star rating control (click same star again to clear to 0) and records `finishedAt`; a year-stat line above the shelves counts books finished in the current calendar year. No cover art, no progress %, per the answered questions. Follows the exact same accessor/row-builder/render pattern already used by Habit/Skill trackers on the same page (`getX()`/`setX()`, `buildXRow()`, `renderX()`, wired into the existing boot sequence and `storage` listener). Verified via screenshot plus an iframe-injected interaction test (add → shelf placement → status change → star rating → finished date → year count all correct, zero console errors).
- **1.3 Relationship Tracker (planner.html) + dashboard badge** — DONE (2026-07-18). New card section on planner.html backed by `contacts_v1`, array of `{id, name, type, tier, cadenceDays, lastContactedAt, birthday, notes, createdAt}`. Tier select (Inner circle/Close/Network) auto-fills the cadence-days input (7/14/30) on change, still freely editable. "Log contact" button pairs with a date input defaulting to today (supports backdating for catch-up entries). Overdue = `lastContactedAt` set and days-since > cadence; list sorts most-overdue first, with never-logged contacts sorted to the very top (they need attention but aren't "overdue" in the strict sense, so they're excluded from the overdue *count* — see dashboard badge below). Birthday stored as `MM-DD` (year-agnostic); a 🎂 tag appears when the birthday falls within the next 14 days. Free-text notes field per contact, saved on blur/change.
  - Dashboard: `dsumTasksBody` (Tasks summary card) now appends "· N person/people overdue" when `overdueCount > 0`, reading `contacts_v1` directly (same direct-read pattern already used for Finance's net-worth/budget keys — a simple stable array doesn't need a publish step). Singular/plural handled explicitly, matching the existing precedent in the same function (`event`/`events`).
  - **Bug caught and fixed during verification**: the "Log contact" button anchored "today" to noon local time regardless of actual time of day. Logging a contact before noon produced a `lastContactedAt` timestamp technically in the future, so `daysSince()` floored a small negative number to `-1`, briefly showing "-1 / 7d" instead of "0 / 7d" (cosmetic only — never flipped the overdue flag, self-corrected by afternoon). Fixed by using the real current timestamp when the picked date is today, and only noon-anchoring for genuine backdating.
  - **Testing-methodology trap hit and resolved, not an app bug**: after fixing the dashboard badge's pluralization, a direct (non-iframe) navigation to planner.html earlier in the same headless test profile had already registered and activated the service worker, which then cache-first-served a stale precached copy of dashboard.html — including to subsequent *iframed* loads in that same profile, since an active SW controls all same-origin clients in scope, not just top-level navigations. Cache-busting the request URL didn't help (the precache entry is keyed to the bare `dashboard.html` filename; a differently-queried URL is a separate miss that should hit network, but dump-dom's async timing made an early read look stale too, muddying the signal). Resolved by re-testing in a brand-new profile that only ever loads app pages inside iframes (`registerServiceWorker()` already skips iframe contexts, per 0.5) — confirmed clean: correct singular "1 person overdue", zero JS errors, `navigator.serviceWorker.controller` null as expected. Lesson for future verification: once any profile has done a direct top-level navigation to a shell page, treat every subsequent load of edited pages in that same profile as potentially cache-shadowed — start a fresh profile (or iframe-only) rather than trusting a re-fetch.
  - Verified geometry directly via `getBoundingClientRect()`/computed styles at a real 390px CSS width (iframe-driven, immune to the previously-documented `--window-size`-vs-actual-viewport mismatch) — confirmed zero horizontal overflow anywhere in the add-row or per-contact rows; a same-size direct-navigation screenshot had looked like it was clipping text at the right edge, which the geometry check proved was a screenshot/viewport artifact, not a real layout bug. Worth remembering: don't trust a small-viewport screenshot's apparent overflow without a geometry check to confirm it, in either direction.
- **1.2 Sleep Log + Trend Chart (health.html)** — DONE (2026-07-18). Answered first: WHOOP OAuth is real/already wired (confirmed from earlier exploration, not re-asked); sleep target defaults to **7h**; chart includes a **7-night rolling average** line. New card between the WHOOP card and Daily Stack, backed by `sleep_log_v1` (array of `{dateKey, hours, source:'auto'|'manual'}`) plus a `sleep_target_hours_v1` key for the reference line (editable inline in the card header, no Settings-page wiring needed for this — "configurable" just meant the code shouldn't hardcode it).
  - Chart is gym.html's weight-tracker SVG pattern reused as-is (same `viewBox="0 0 320 130"`, same quadratic-smoothing path builder, same area-fill/glow-filter/dot structure), re-themed with health.html's own `--accent`/`--warning` tokens instead of gym's `--good`. Added on top of the base pattern: a dashed horizontal target-reference line (`--warning`-colored, distinct from both the data line and the plain grid lines), and a 30/90-day range toggle (gym's own chart hardcodes 30 with no toggle — this section needed one per spec, so it's new rather than copied).
  - Auto-fill hooks into the existing WHOOP fetch success path via a small `window.slAutoFillFromWhoop(sleepRecords)` function — called with the *entire* returned sleep array (up to `TREND_LOOKBACK_DAYS`=7 records), not just last night's, so a lapsed visit still backfills the gap on next load. Manual entries always win on conflict (an explicit override); auto entries only ever overwrite a previous *auto* entry for that night. Manual vs. auto nights are flagged visually as required — filled dots for WHOOP-sourced nights, hollow ring dots for manual — with a legend spelling both out plus the avg-line and target-line keys.
  - Works fully in manual-only mode with no WHOOP connection at all (the add-row and chart don't gate on connection state, matching "manual entry/override always available").
  - Verified via an iframe-injected test: hook presence, empty-state toggle, 5 manual nights logged correctly (chart/legend/meta all reflect them), a synthetic auto-fill call correctly left a manual night untouched while adding a new auto-only night, a second synthetic auto-fill call correctly overwrote that auto night's value, range-toggle and target-input both update the chart live — all assertions passed, zero console errors. Screenshot confirmed visual match with the WHOOP/Daily Stack cards above and below it. Mobile geometry check at a real 390px CSS width (iframe-driven) confirmed zero horizontal overflow in the card, chart, legend, or add-row.
  - **Process note, not a bug**: partway through this section's testing, a direct-navigation screenshot came back showing the app's splash screen instead of health.html — landing-guard.js's temporary test bypass (added, then correctly reverted at the end of 1.3's verification per the established discipline) simply hadn't been re-enabled yet for this new round of testing. Re-enabled it, re-ran, reverted again after — confirms the revert-every-time discipline is working as intended, not a gap in it.
- **1.4 Notification Hub — Gmail, read-only (dashboard.html)** — DONE (2026-07-18). Answers already on file from Phase 1 Round 2: 10 messages, unread-only, manual refresh only (no polling interval), `gmail.readonly` scope only, one account.
  - **Implementation is deliberately separate from Calendar's OAuth**, per the master prompt: Calendar uses a server-side auth-code exchange (`api/google-calendar-callback.js`, refresh tokens persisted in `gcal_tokens_v1`); Gmail uses Google Identity Services' client-side **token flow** (`google.accounts.oauth2.initTokenClient`, loaded via `<script src="https://accounts.google.com/gsi/client">`), no server involved, no PKCE code exchange to write. The access token lives only in a plain JS variable — **never** written to localStorage or sessionStorage — with a single boolean flag (`eq.gmail.everGranted`) persisted just so the page knows whether to attempt silent re-auth (`requestAccessToken({prompt:''})`) on load versus showing an explicit "Connect Gmail" button. A 401 mid-session triggers exactly one silent-reauth retry before falling back to "reconnect."
  - **Reused the existing `GOOGLE_CAL_CLIENT_ID`** rather than provisioning a second OAuth client — GIS supports requesting a different, smaller scope from the same client ID, and this is Google's own recommended incremental-auth pattern. **Heads up for whoever deploys this**: this requires the Gmail API to be enabled for that same Google Cloud project (Calendar's API being enabled doesn't imply Gmail's is) and, if the OAuth consent screen is still in Testing publish status, `gmail.readonly` needs to be added to its scope list. If either is missing, connecting will fail with a visible error in the panel rather than misbehaving silently — but it can't be verified from code alone, only from the actual Google Cloud Console for this project, which is outside what I can check. Worth a real-account spot-check post-deploy.
  - Panel sits between the Day Ring and the Tasks summary card (new "Inbox" section) — placed near the top since the whole point is glanceability, not buried under the 6 summary cards. Shows sender/subject/snippet/time for up to 10 unread messages, with an unread-count badge using Gmail's `resultSizeEstimate` (can exceed 10, since that reflects the true unread count, not just what's shown). Tapping a row lazily fetches `format=full` for just that message, walks the MIME `payload.parts` tree for a `text/plain` part first, falling back to `text/html` stripped to text via `DOMParser` (parsed into a detached, non-rendered document, so nothing in the email can execute) — decoded from Gmail's base64url. "Open in Gmail ↗" (external escape hatch, per spec) and, when the sender's display name exact-matches a Relationship Tracker contact, a "tracked contact" tag plus a "Log contact" button that writes `lastContactedAt` straight into `contacts_v1`.
  - Cross-link match is by **display name only** (no email field on the contacts model, and adding one wasn't asked for) — exact, case-insensitive match against `contact.name`. Good enough for the "small but high value" bar the spec set; noted here rather than silently expanding the Relationship Tracker's schema after the fact.
  - Verified with a mocked `google.accounts.oauth2` + mocked `fetch` (real Gmail OAuth can't be exercised headless — no real account/consent screen available in this environment): connect flow, badge count, both message rows rendering with correct sender/subject/snippet, the tracked-contact tag appearing on the matching sender and not the other, expand-to-load correctly decoding a real base64url-encoded plain-text body, the Log Contact button correctly writing `contacts_v1`, and disconnect correctly clearing state and the everGranted flag — all passed, zero console errors. Real GIS script load was blocked via `--host-resolver-rules` during testing so the mock couldn't be raced/clobbered by the genuine library arriving mid-test. Screenshot confirmed visual match with the rest of the dashboard; mobile geometry check at a real 390px width confirmed no overflow, including with a long sender name (truncates via ellipsis as intended).
  - **Not verifiable from this environment, flagged for a real-device check**: the actual Google consent screen UX, real silent-reauth behavior across a genuine browser session, and whether the reused client ID's project needs the Gmail-API/scope setup mentioned above.
- **1.5 Calendar Quick-Add → Google Calendar (planner.html)** — DONE (2026-07-18). Answers already on file from Phase 1 Round 3: primary calendar, single account; two-way sync; natural-ish parsing.
  - **Architecture call not covered by the ASK FIRST list**: the master prompt said to create events "via the same Google OAuth session as 1.4" but also said this should make "the 'Connect Google Calendar' button that already exists" real — those two point at different things. The existing button (planner.html, `#calConnectBtn`) is a fully-working **server-side** auth-code flow (`api/google-calendar-callback.js` + refresh tokens in `gcal_tokens_v1`) that already reads events; 1.4's Gmail flow is a separate client-side GIS token, and mixing that pattern into Calendar would mean two parallel "connected" states for the same feature. Went with expanding the *existing, working* Calendar OAuth instead: changed `GOOGLE_CAL_SCOPE` from `calendar.readonly` to `calendar.events` (read+write on events, not full calendar management) in both planner.html (the real owner of the connect flow) and dashboard.html (kept in sync for its own read-only summary card, though it no longer has a connect button of its own post-0.7, so this was dead-code hygiene more than a functional change). No real user has connected yet, so there's no re-consent migration to worry about.
  - New `createGcalEvent()` alongside the existing `fetchGcalEvents()`, same `ensureValidGcalToken()` underneath — one POST to `calendars/primary/events`.
  - Quick-add is a single natural-language input (no separate structured-field pickers, since the user picked natural-ish parsing as the *only* v1 input mode) with a live preview line underneath showing the parsed result before saving, so a misparse is visible rather than silent. Recognizes one date token (today/tomorrow/a weekday name — next occurrence, treating "monday" said on a Monday as next Monday, not today/skip/invalid — or `M/D`), one time token (`6am`, `6:30pm`, or 24h `14:00`), and an optional `for Xh`/`for Xm` duration; whatever text is left becomes the title. Falls back to sane defaults (today, next half-hour slot from now, 30 min) rather than blocking when a piece can't be found — the preview line visually flags whenever a fallback was used (amber, "(guessed...)").
  - Reused gym.html's chart pattern doesn't apply here (no chart in this section) — followed the existing `.contact-add-row`/`.cal-status` visual patterns already in this same file instead, for consistency within planner.html itself.
  - **Two real bugs caught by testing, not code review**: (1) the success status message ("Added 'gym' to Google Calendar.") was being set *before* calling `loadCalendar()` to refresh the grid — but `loadCalendar()` writes its own "Loading…" then blank status to the same `#calStatus` element, so the confirmation was clobbered within the same tick and would never actually be seen. Fixed by awaiting `loadCalendar()` first and setting the confirmation after, so it's the last write. (2) The duration regex (`for (\d+)h`) had both its hour and minute capture groups fully optional, so on input like *"...title for testing tomorrow 6am for 1h"* it matched the **first** "for" (in "for testing") as a zero-width empty match and gave up, silently leaving the real "for 1h" stuck in the title and defaulting to 30 minutes. Fixed with a lookahead requiring a digit immediately after "for " before attempting the match, so the regex engine correctly skips past non-numeric "for" occurrences to find the real one.
  - Verified via mocked `ensureValidGcalToken`/`fetch` (real Google consent isn't exercisable headless, same limitation as 1.4): 4 parser cases (relative day + am/pm, weekday + "for Xh", 24h time, full-fallback with no date/time at all) all produced correct title/date/time/duration; empty input hides the preview; clicking Add with no connection shows an inline "Connect Google Calendar first." and makes no API call; with a token present, POST body's `summary`/`start`/`end` (correct UTC conversion from local wall-clock time, confirmed against the test machine's `Europe/London` zone) all correct, success message persists post-refresh, input clears; a failed POST (500) surfaces inline on the preview line rather than failing silently. Zero console errors. Mobile geometry check at 390px confirmed no overflow, including with a long title that forced the duration-regex bug to surface in the first place — worth remembering that a "test the pretty case" mobile check with a short string wouldn't have caught it; the longer, messier test string did.

- **1.6 Color-Coded Live Goal Countdown (planner.html)** — DONE (2026-07-18). The one ASK FIRST item (confirm pulse-instead-of-blink) is already resolved by the master prompt's own text prescribing it, not left open — built as specified rather than re-asking. Went with **smooth HSL interpolation** over discrete steps (the prompt invited either): costs nothing extra once `percent_remaining` is computed, and reads as a continuous "heating up" gradient rather than jumpy bands against the dark theme. Discrete steps would be a small follow-up if preferred instead.
  - **Data-model gap the master prompt assumed away**: the formula `percent_remaining = (deadline − now) / (deadline − start_time) × 100` needs a goal *start time*, but the existing goal object (`longterm_goals_v1`) never stored one — only `deadline`. Added `createdAt` when new goals are created going forward. Existing goals from before this feature (there's a real one, "grind") have no `createdAt` and can't have a fabricated one guessed backward without misleading the user about their own history, so they render **neutral until the deadline actually passes**, then jump straight to critical — a safe, honest fallback rather than invented precision.
  - Color stops (percent_remaining, descending): 100–76 flat neutral (`--text-secondary`, no gradient) → 75 dark green → 50 green → 40 light green → 35 lighter green → 30 yellow → 20 orange → 10 red → 5–0 dark red, flat (not interpolated further) + pulse. The 75/76 boundary is a hard jump per the spec's own listing, not a fade. **Exception**: a goal whose progress slider reads 100% is always neutral regardless of date math — checked first, before the gradient even runs — and updates live as the slider is dragged (on `input`, not just on committed `change`), not just on the once-a-second tick.
  - Pulse is a 1s ease-in-out opacity animation (1 → 0.55 → 1), gated behind `@media (prefers-reduced-motion: no-preference)` so a reduced-motion user gets the same dark-red color with zero animation, per spec.
  - Color is applied via inline `style.color` (not a fixed set of CSS classes) since it's a continuous interpolated value, computed in one shared function (`applyGoalCountdownColor`) called from three places that all needed to agree: initial card render, the existing once-a-second countdown tick, and the slider's live `input` handler.
  - **Real testing-methodology trap, not an app bug**: `longterm_goals_v1` is a cloud-synced key (`initCloudSync`'s `syncedKeys`). Seeding test goals directly into localStorage and reloading the iframe kept silently losing them — Supabase was pulling the one real live goal ("grind") over the top within the same page load, before the test's read landed. Not obvious at first since earlier sections' test keys (`contacts_v1`, `books_v1`, `sleep_log_v1`, etc.) all happen to be un-synced, so this hadn't come up yet. Resolved the same way as 1.4's Google-domain block: `--host-resolver-rules="MAP ubhjdibldkknviezrjys.supabase.co 0.0.0.0"` for the duration of the test. Worth remembering for any future test that touches a cloud-synced key specifically (goals/projects/calendar/checklists, per the synced-keys list) — un-synced keys don't have this problem.
  - Verified numerically across all 8 named stops plus an interpolated midpoint (45%, confirmed to fall strictly between the 50% and 40% stops' RGB values in every channel — correct interpolation direction and ordering), the 100%-neutral exception (including live update on slider drag), and both legacy-goal fallback paths (no `createdAt`, future deadline → neutral; no `createdAt`, already overdue → critical). Zero console errors. Screenshot confirmed the full gradient reads cleanly against the dark theme, and made the 100%-exception visually unambiguous: a goal sitting at the same deadline as the "5% remaining, critical" goal directly above it renders fully neutral because its slider is at 100%.

- **1.7 Goal Archive at 100% → History + Obsidian folder (planner.html + obsidian-sync.js)** — DONE (2026-07-18). Two of three ASK FIRST answers were already on file (folder naming `Archive/<goal title>`, un-archiving permanent). The third ("project linked to two goals") turned out to be **moot, not skipped** — Projects only ever store a single `goalId` (not a list), so a project can never belong to two goals simultaneously; there's no reconciliation case to design for. Confirmed by reading the actual data model rather than assuming the prompt's premise held.
  - **Reused the existing Projects archive mechanism instead of building a parallel one.** Projects already had `archived: boolean`, a `.proj-history` collapsible section, and reversible Unarchive — discovered while investigating (flagged as a to-check item in an earlier session note). Goal-archiving now reuses that same `archived` flag on linked projects rather than inventing a second archive concept, but adds `archivedViaGoal: true` on those specific projects so their Projects-History row shows a static "Archived with goal" badge instead of the normal Unarchive button — the goal's permanence has to hold even though plain project-archiving is still deliberately reversible. Projects archived independently (manually, via their own `status: 'done'` → Archive button) are completely unaffected — verified as a regression check, not just assumed.
  - Goals themselves: a new `.dgt-goal-archive` button appears only at `value >= 100` (shows/hides live as the slider crosses the threshold, not just on next reload), gated behind `window.confirm()` naming the goal and how many linked projects will be swept in — matches the Settings page's existing precedent for irreversible actions. New `archivedAt` timestamp; archived goals disappear from the active list (and from dashboard.html's summary card, which needed its own `!g.archived` filter added) and reappear in a new read-only **Goal History** `<details>` section (title, formatted completion date, linked-project count) — built by directly reusing `.proj-history`/`.proj-history-summary`/`.proj-history-list` CSS rather than writing parallel styles for visually identical structure.
  - **Obsidian move, not copy**: added `moveProjectPageToFolder()` to `obsidian-sync.js` (exported alongside the existing `pushProjectPage`) since the Local REST API has no move/rename verb — implemented as GET the current content (preserving whatever the user actually wrote into the page, not re-generating `pushProjectPage`'s bare stub) → PUT to the new sanitized path → best-effort DELETE of the old one. Runs *after* the local archive already committed, one project at a time, so a slow or unreachable Obsidian instance can never leave a goal half-archived — failures are reported in the status line ("N failed to move (still archived locally)") rather than rolling anything back. Skipped entirely (no-op, not an error) for goals with no linked projects or when Obsidian isn't enabled.
  - **Real bug caught by testing with a deliberately messy title, not a clean one**: initially passed the destination as a pre-joined `'Archive/' + goal.title` string, sanitized by splitting on `/`. A goal titled *"Q3: Ship it!/Launch"* — chosen for the test specifically because it contains a literal slash — got misread as **three** path segments instead of two, silently creating an extra nested subfolder (`Archive/Q3- Ship it!/Launch/`) instead of sanitizing the embedded slash within one segment (`Archive/Q3- Ship it!-Launch/`). Fixed by changing `moveProjectPageToFolder`'s signature to take an array of already-distinct segments (`['Archive', goal.title]`) rather than a joined path string, so the caller's structural boundaries can never be confused with illegal characters inside the user's own text. Re-verified directly against the sanitizer (not through a mock that bypassed it) with two adversarial titles — one with an embedded slash+colon, one with a full spread of illegal characters — both now produce the correct flat two-level folder with the exact expected GET → PUT → DELETE call sequence.
  - Verified end-to-end with `ObsidianSync` swapped for a recording mock (real Local REST API isn't reachable from this environment) plus `window.confirm` auto-accepted: Archive button appears only at 100%; archiving a goal with no linked projects removes it from the active list and adds a correct History row; archiving a goal with two linked projects (one Obsidian-linked, one not) correctly archived both, called the move function only for the linked one, left an unrelated already-archived project's `archivedViaGoal` untouched, and left a fully unrelated active project untouched. Zero console errors. Screenshot confirmed the Archive button and expanded Goal History row render cleanly, reusing the established Projects-History visual language.

- **1.8 To-Do Completion Rate Chart (dashboard.html + planner.html)** — DONE (2026-07-18). Both ASK FIRST items were self-recommended in the master prompt's own text ("count as missed [...] recommended", "Propose: skip") rather than genuinely open, so built as proposed without a re-ask, consistent with how 1.6's pulse-substitution was handled.
  - **The real design problem wasn't the math, it was WHERE to capture it.** `rollover()` (planner.html) already permanently deletes every past day's `goals:YYYY-MM-DD` key after folding its undone tasks into today — that's pre-existing behavior, not something this section changed. That means a day's scheduled/completed counts are only ever available for the single moment just before that deletion; there's no other durable record to compute a snapshot from later. The snapshot is taken *inside* `rollover()`'s existing per-key loop, immediately before `storeDelete(k)`, into a new `todo_completion_snapshots_v1` array — the only new persistent key this section adds.
  - **"Pushed to tomorrow counts as missed" needed its own small fix**, not just a formula choice: the existing "Push remaining to tomorrow" button (`gmPushBtn`) already *removes* unchecked tasks from today's list immediately (moving them to tomorrow's key) — by the time rollover snapshots that day, those tasks would already be gone from the list, silently shrinking the scheduled denominator and making a day with pushed-away tasks look artificially like 100%. Fixed by adding a tiny `goals_pushed:YYYY-MM-DD` counter, incremented wherever the push button removes tasks, folded into `scheduled` (not `completed`) at snapshot time, then deleted — so pushed tasks are counted as missed for their original day without changing the push button's existing behavior at all.
  - Chart lives on dashboard.html directly under the Tasks summary card (a separate non-link card, not nested inside the Tasks `<a>`, since it needs its own clickable 4W/12W/52W toggle). Reuses gym.html's SVG chart pattern (viewBox, quadratic-smoothing path, area/glow) re-themed with `--success` instead of gym's `--good`. New on top of that base pattern: **zero-scheduled or unrecorded days are skipped, not plotted as 0% or 100%, and render as a literal break in the line** — built by grouping valid days into contiguous runs (by array position) and drawing each run as its own `<path>`, rather than one continuous path that would misleadingly connect across a gap. The 7-day rolling average is calendar-based (trailing 7 calendar days from each valid point, not 7 array-positions), broken at the same gaps as the raw line, since gaps mean those aren't the same thing here the way they are for gym/sleep's daily-contiguous data.
  - Verified in two layers. Data layer (planner.html): seeded two past days (one with an auto-rolled-forward undone task, one with 2 tasks manually pushed earlier the same day) and let a real boot run `rollover()` — both produced correct `{completed, scheduled}` snapshots (the pushed-away day correctly counted its 2 pushed tasks in the denominator), old `goals:` keys were deleted, the `goals_pushed:` counter was consumed/deleted, and today's own key was left untouched. Chart layer (dashboard.html): insufficient-data empty state; 18 valid days plus a deliberate 2-day gap correctly produced exactly 2 raw-line segments and 2 avg-line segments (not 1 continuous path) with 18 dots and a single area fill on the longest run; range toggle correctly switches the active button and re-renders. Zero console errors. Screenshot with a trending, noisy 28-day dataset (3-day gap included) showed the gap as a clean visual break and the 7-day average visibly smoothing the noisy raw line, sitting correctly under Tasks. Mobile geometry check at 390px confirmed no overflow.

This closes out Phase 1 (1.1–1.8) — see the section-by-section entries above.

## Phase 2

- **Phase 2 questions** — answered before building: default context domains ON = Goals/Tasks, Health, Fitness, Finance, Habits (Relationships defaults OFF, still toggleable per-message); conversations persist to localStorage (consistent with the rest of the app); default model Sonnet 5, no user-facing toggle. **Corrected one detail from the master prompt rather than following it literally**: it named `claude-sonnet-4-6` as the smarter-model option, which is not a real/current model ID (the prompt predates it) — used the real current ones (`claude-sonnet-5` / `claude-haiku-4-5-20251001`) instead, flagged to the user before building so the substitution wasn't silent.
- **"Ask Claude" page (ask.html) + backend (api/chat.js)** — DONE (2026-07-18).
  - **Nav placement wasn't asked about, and the master prompt didn't specify it** — put "Ask" in the mobile More sheet / desktop overflow (alongside Train/Vitals/Brand/Settings) rather than as a 9th core tab, since Phase 0.4 already deliberately fixed the core row at 8 tabs; adding a 9th would quietly re-litigate that earlier decision. Easy to promote to core later if it turns out to get used constantly.
  - **Backend follows the exact existing serverless-function conventions** (`api/whoop-refresh.js` was the template): CORS headers, OPTIONS→204, non-POST→405, `process.env.ANTHROPIC_API_KEY` read server-side only (never sent to or stored by the client), try/catch around the outbound fetch. Model and `max_tokens` (1024) are fixed server-side, not client-controlled, so a compromised or buggy client can't drive up spend or swap models. Message/context sizes are hard-capped (8000/6000 chars) before forwarding. Rate limiting is a simple in-memory sliding-window counter — explicitly best-effort, since a Vercel function instance isn't guaranteed to stay warm or be the only instance handling traffic; proportionate to "simple rate limit" for a single-user personal app guarding against a runaway client bug, not real abuse.
  - **Context assembly** builds one compact text block per enabled domain by reading localStorage directly (today's tasks + streak + longterm goal titles/%, `eq.health.summary`, `eq.fitness.summary`, net worth + budget from the same `nw:`/`budget:` keys Finance's own dashboard card reads, `habits_v1`/`skills_v1` name+streak/minutes, `contacts_v1` overdue names) — same direct-read pattern used throughout this build for simple stable arrays, no new publish step anywhere. Per-message domain chips (6, matching the spec's list) default to the answered set; toggling persists across sessions but every message re-reads live data, so context is always current, never stale from when a chip was first turned on. An estimated token count (`~length/4`, a standard rough heuristic) is shown next to the chips so cost stays visible before sending, per spec.
  - Chat UI: user/assistant bubbles, persisted message history, a typing indicator (respects `prefers-reduced-motion`, same convention as 1.6's pulse), Enter-to-send / Shift+Enter-for-newline, auto-growing textarea, a failed request renders as its own distinctly-styled error bubble inline in the conversation (not a toast) so the failure stays in context, and a "Clear conversation" action gated behind `confirm()`.
  - **Updated `privacy.html`'s existing Anthropic disclosure** rather than adding a separate one — it already covered the pre-existing "✨ Polish" goal-rewording feature but not this page's much larger data flow (full domain summaries, not just one goal's text). Expanded the same bullet to cover both, naming exactly which domains can be sent and making clear the toggle is per-message/opt-in, not a standing grant.
  - Added `ask.html` (and, incidentally, the already-existing but previously-uncovered `settings.html`) to `sw.js`'s precache list, bumping `CACHE_NAME` to `v2` per that file's own documented convention for shell-list changes.
  - Verified with a mocked `/api/chat` (a real call needs a deployed `ANTHROPIC_API_KEY`, not available in this environment — flagged below): domain chip defaults matched the answered set exactly; toggling a chip persisted to localStorage and changed both the visible token estimate and the actual `systemContext` sent on the next message (confirmed present when on, confirmed the other domains' sections were correctly formatted); a real send produced correct request shape (`messages` + `systemContext`) and rendered both bubbles with correct styling and persistence; a failed request rendered as an error bubble with the server's actual error text; Shift+Enter correctly did not send; Clear conversation correctly reset state and restored the empty view. Zero console errors. Screenshot confirmed bubble styling, chip states, and the input bar match the rest of the app. Mobile geometry check at 390px (including deliberately long messages meant to stress bubble max-width) confirmed no overflow.
  - **Not verifiable from this environment, flagged for a real check post-deploy**: the actual Anthropic API call end-to-end (model availability/response shape, real latency), and — this is the one actionable item — **`ANTHROPIC_API_KEY` still needs to be added in Vercel's project environment variables**; the page will show a clear inline error ("server not configured") rather than failing silently if it's missing, but nothing will send until it's set.

## Phase 3

- **3.1 Year Grid per habit (ability.html)** — DONE (2026-07-18). No ASK FIRST items for this section, built as specified.
  - **Rolling 365-day window ending today, not calendar-year-to-date** — a judgment call the spec didn't quite pin down ("whole-year grid (365 cells)" / "success % for the year"). A fixed Jan 1–Dec 31 view would render almost empty every January and reset the visible history arbitrarily; a rolling year (same convention as GitHub's contribution graph) stays consistently useful year-round.
  - **Same data-model gap as goals in 1.6, same fix**: habits had no `createdAt`, which the "grey = not yet" state needs to distinguish "before this habit existed" from "existed but missed." Added `createdAt` to newly-created habits; existing habits infer a start boundary from their earliest history entry, falling back to today (all-grey) if they have no history at all — same reasoning as 1.6, not a new pattern.
  - Today is always grey regardless of done state (the day isn't over), matching the existing 30-day toggle grid's own convention for `.is-today` — kept consistent rather than introducing a different rule for the new grid.
  - Compact preview (tiny 3px cells, non-interactive, click-to-expand) and a full modal (11px cells, legend, horizontal-scrolls on narrow screens) show the *same* 365 cells at different sizes, both computed by one shared `habitYearCells()` function so the two views can never disagree.
  - **Caught by screenshot, not by the numeric test**: "done" was initially styled with `--accent` (the app's warm orange) instead of `--success` (green) — the spec explicitly says "green = done, red = missed," and orange-vs-red (`--accent` `#E07658` vs `--danger`-derived `#FF6B6B`) turned out to be genuinely hard to tell apart at a glance, working against the whole point of the feature. The interaction test's cell-count assertions couldn't have caught this since they only check class names, not perceptual color distance — this needed an actual look at the rendered image. Fixed by switching to `--success`; re-screenshotted to confirm green/red now reads instantly.
  - Verified the state-classification math by hand against two constructed habits (one with an exact 20-day-old `createdAt`, one legacy habit with no `createdAt` inferring a 10-day-old start from its earliest history entry) — done/missed/grey counts and the resulting percentage matched exact hand-calculated expectations in both cases (25% and 20%), including the off-by-one edge case of the start-boundary day itself counting as "in range." Modal open/close (backdrop click and × button) and the today-cell-is-always-grey rule both confirmed. Zero console errors. Mobile geometry check at 390px confirmed no page-level overflow, with the modal's internal grid correctly using its own contained horizontal scroll rather than breaking the layout.

- **3.2 Habit Health Score with decay (ability.html)** — DONE (2026-07-18). The ASK FIRST item ("keep the streak too, or make the score primary?") is answered by the section's own body text one line above it ("Alongside (not replacing) the streak") — built as the body text already specified rather than re-asking a question the spec had already answered itself, same pattern as 1.6/1.8.
  - `score = previous×k + done×(1−k)` exactly as specified, with `k=0.95` — chosen for roughly a 2-week half-life under sustained misses (`k^14 ≈ 0.49`), matching "decays gradually instead of zeroing it." Loop Habit Tracker derives `k` from each habit's configured frequency (e.g., 3×/week vs daily); this app's habits have no frequency field at all — just a daily done/not-done toggle — so introducing one purely to feed this formula would be scope creep nothing else asked for. Used one fixed `k` for all habits instead.
  - New habits start from a neutral seed (50) with no history; the exponential decay means the seed's influence fades within a few weeks regardless of its starting value, so 50 isn't a "wrong first guess" that needs correcting later.
  - Today only factors into the score once actually marked done — an unmarked "today" doesn't get a decay step applied, same "the day isn't over yet" rule as 3.1's year grid, kept consistent rather than inventing a different treatment of "today" for this section.
  - Small ring (not a bar — ring fit ability.html's existing recovery-ring visual language better, given the app already uses this exact ring pattern on the WHOOP recovery ring and the dashboard day-ring) rendered next to the streak badge, color-banded green/amber/red at the 70/40 thresholds so the score reads at a glance without needing to parse the number.
  - Verified against hand-calculated expected values for four constructed habits: 30 days old with every day (including today) marked done → score 90 (hand estimate ~89, within expected rounding of a log-approximation done by hand); 30 days old with nothing ever marked → score 11, exact match; a brand-new habit with no history → score 50 exactly; and the key qualitative case, 60 days old with exactly one miss 10 days back → score 95, concretely demonstrating "one miss doesn't crater it" rather than just asserting it. Streak badge confirmed still present and correct alongside the new ring (31-day streak on the all-done habit). Ring fill percentage cross-checked against its own `stroke-dasharray`/`stroke-dashoffset` math and found to match the displayed number exactly. Zero console errors. Screenshot confirmed all three color bands render correctly and the ring sits cleanly in the row. Mobile geometry check at 390px (with a deliberately long habit name) confirmed no overflow — the name truncates via ellipsis, leaving the streak/ring/delete controls their full space.

- **3.3 Weekly Report widget (dashboard.html + gym.html + health.html + obsidian-sync.js)** — DONE (2026-07-18). Answers: week closes Monday 6 AM (matches the app's existing daily 6 AM rollover, so a "day" and a "week" reset together, rather than the master prompt's own Sunday 9 PM placeholder); all 5 categories as listed ship in v1.
  - **The real work here was sourcing the data, not the UI.** Two of the five categories needed genuinely new persistence, since nothing in the app tracked them over time before now: gym.html's `publishFitnessSummary()` (already existed, from 1.7) is extended to also compute `sessionsThisWeek`/`volumeThisWeek`/`weight7dAvgChange` — read from gym.html's own already-normalized `state`, not parsed raw from outside, since its `po_coach_v1` blob goes through a `normalize()` step on load that a naive outside read would miss. health.html gets a new `recovery_log_v1`, one entry per day, same append-only shape as the existing Sleep Log (1.2) — `eq.health.summary` only ever held the *latest* reading, which isn't enough to average a week.
  - **Finance "cash-flow pace" is a simplification, flagged rather than overclaimed**: `budget:income`/`budget:expenses` are flat recurring monthly line items with no date-stamped transactions, so there's no way to compute a real week-by-week "pace" from what the app actually stores. Reports the current monthly income−expenses figure instead (the same number Finance's own dashboard summary card already shows) rather than fabricating week-over-week granularity the data can't support.
  - **"Goals progressed" became "goals archived this week"**: the spec's own metric needs a goal's progress *value* at the start of the week vs now, which isn't tracked (only the current value persists). Rather than add yet another new snapshot mechanism for this one sub-metric, used the real `archivedAt` timestamps from 1.7's Goal Archive — an honest, directly-computable proxy (completions, not partial progress) rather than a fabricated delta.
  - **A week with literally zero data across all 5 categories is not saved as a report** — a brand-new install's very first visit would otherwise immediately synthesize a "last week" report full of "no data" placeholders before the user has done anything, which is a poor first impression and effectively made the empty-state UI unreachable. Caught while writing the generation logic, before it ever got tested wrong, by tracing through what a fresh install's first `ensureWeeklyReports()` call would actually produce.
  - Report generation walks forward from the last recorded week (catches up correctly if the app wasn't opened for several weeks) and keeps the most recent 12, trimming as a side effect of generating a new one — not on every render, so data that's already in storage is never silently truncated outside of that path. "Write to Obsidian" (`ObsidianSync.pushWeeklyReport`, new) always overwrites for a given week rather than preserving prior content the way `moveProjectPageToFolder` does for project pages — deliberately different from that pattern, since a weekly report is "the current numbers for this week," not freeform user-owned content that must survive a re-write.
  - Verified against one fully hand-constructed week of cross-page test data (5 nights of sleep/recovery, a published fitness summary, 4 days of task-completion snapshots, one goal archived mid-week, a habit done 5 of 7 days, and simple monthly budget figures): all five categories' computed numbers matched exact hand calculations (7.4h avg sleep, 64% avg recovery, 3 sessions, 12,500 volume, -0.4kg weight trend, 75% task completion, 1 goal completed, 71% habit success = 5/7 rounded, +GBP 800 cash flow) with zero discrepancies. Separately verified the catch-up/backfill logic and the 12-week trim (seeded 14 weeks with a deliberate 1-week gap, confirmed generation filled exactly the missing week then correctly trimmed to 12, keeping the newest week rather than an arbitrary slice) and the Obsidian export (correct weekKey, correctly formatted markdown, status message). Zero console errors. Screenshot confirmed the card reads cleanly as a single "shareable-looking" summary, sitting correctly below the six existing summary cards. Mobile geometry check at 390px confirmed the responsive metric grid correctly reflows to 2 columns with no overflow.

- **3.4 Quick Capture from anywhere (topbar.js)** — DONE (2026-07-18). No ASK FIRST items; built as specified (floating "+", not the long-press alternative — more discoverable on its own).
  - **Lives in `topbar.js`, not any single page** — the only section so far that genuinely needs to run everywhere, matching how the nav/service-worker registration are already centralized there rather than duplicated per-page.
  - **Real bug caught during architecture research, before any code was written**: the natural-looking approach for the "Note" destination would be to call the existing `window.Vitality.save()` bridge. But `vitality-bridge.js` derives its storage key from *the current page's own filename* (`appKey()` reads `location.pathname`) — since `topbar.js` runs on every page, calling that bridge from Quick Capture would silently write the note into whichever page happened to be open (e.g., into `vitality_health`'s slot while sitting on health.html) instead of into Notes. Writing directly to Notes' actual cache key (`vitality_bridge:vitality_notes`) sidesteps this entirely, regardless of which page Quick Capture is opened from. Confirmed both halves of this explicitly in testing: captured a note *from health.html* and verified `vitality_bridge:vitality_health` stayed untouched while `vitality_bridge:vitality_notes` got exactly the right entry.
  - **Trade-off, documented rather than silently accepted**: this write only updates Notes' local cache immediately — the cross-device Supabase push happens the next time Notes is actually opened on this device (its own boot already does a full load/save cycle), not duplicated inline in topbar.js. Replicating the push here would mean depending on `window.supabase` being loaded, which isn't guaranteed on every page topbar.js runs on (the CDN script tag is a per-page include, not universal) — an inline push could silently no-op on pages that don't happen to have it.
  - All four other destinations write directly to their real, already-established shapes rather than inventing new ones: `goals:<dateKey>` for tasks (today's key computed with the same 6am day-boundary rule as planner.html's `getActiveDateString()` — a task captured at 2am belongs to the still-open previous day, not a fresh empty one), `shopping_list_v1`, and `books_v1` (matching 1.1's Book Tracker fields exactly, landing new captures on the "Want to Read" shelf via `status:'want'`).
  - Reused the existing "More" sheet's exact visual pattern (bottom sheet, dark backdrop, same border-radius/padding conventions) for visual consistency rather than inventing a new sheet style, and the app's established white-pill "primary action" look (same as Settings' Save button) for the floating button itself.
  - **Testing needed its own temporary bypass, not just landing-guard's**: the existing `isEmbedded()` check correctly suppresses all nav chrome (bottombar, cat-tabs, and now Quick Capture) when a page is iframed — exactly right for production, since an embedded tile shouldn't grow a floating capture button, but it also meant this session's usual iframe-harness testing pattern couldn't see the button at all on the first attempt (it silently never rendered). Added a second temporary one-line bypass (`return false;` at the top of `isEmbedded()`) for the duration of testing, reverted immediately after — confirmed via the same before/after `TEMP: disabled` grep check already used for `landing-guard.js`'s bypass.
  - Verified end-to-end from health.html (deliberately *not* Notes, Planner, or Ability — the three pages actually being written to) with a shared browser profile: FAB opens the sheet; an empty-input chip click neither saves nor closes; Task today and Task tomorrow both land in the correct dated key with `done:false`; the Note capture correctly split "first line = title, rest = body" exactly matching Notes' own composer logic, and (the critical check) went to the right page's storage key, not the current page's; Shopping list and Book captures matched their real pages' exact field shapes; backdrop-click closes without saving. Zero console errors. Screenshot confirmed the FAB and sheet render cleanly and match the app's established visual language. Mobile geometry check at 390px confirmed the chip row wraps correctly with no overflow.

- **3.5 Correlation view (health.html)** — DONE (2026-07-18). Answer: default preset pairs are Recovery×Task completion, Weight trend×Task completion, Sleep×Recovery (all three available as one-tap chips; any of the 5×4 possible pairs still reachable via the two dropdowns directly).
  - **"Habit score" needed a different metric than 3.2's ring**, not the same one reused: 3.2's Habit Health Score is a live, smoothed, decaying rolling value with no daily history of its own — there's nothing to plot it against day-by-day. Computed a directly-available per-day proxy instead (% of habits done that day, from `habits_v1`'s raw history — the same math as the Weekly Report's `habitSuccessPct`, just per-day instead of per-week) and labeled it "Habit success %" in the UI specifically so it doesn't get confused with the 3.2 ring, which is a genuinely different number answering a different question.
  - Weight trend reads gym.html's flat `po_coach_weights` array directly (safe, per the established precedent for simple stable arrays) and computes a trailing 7-day average *per day* across the window, not the raw daily weight — matching what "7-day trend" actually means rather than just relabeling the raw log.
  - Scatter (not dual-line) — Pearson r is inherently about paired (x,y) values, and a scatter is the more honest visualization for "these two things move together," where a dual-line risks implying a time-series story the correlation itself doesn't claim.
  - Both honesty rules from the spec enforced directly, not just described: fewer than 14 paired days shows an explicit count-based empty state ("8 of 14 days needed...") instead of a chart with too little data to mean anything, and the "Correlation, not causation" line is hardcoded to always render alongside the r value, never conditional on the r value itself.
  - **Real bug, caught by checking the sentence's actual claim against the test data's known constructed relationship, not just checking that a sentence rendered**: the plain-English generator's direction word (`diff >= 0 ? 'higher' : 'lower'`) was computed from *above-minus-below*, but the sentence describes the *below* group's value relative to the above group — backwards from what the comparison needs. On deliberately-constructed data where higher recovery cleanly predicted higher task completion, the generated sentence read "...task completion averages 24% **higher** [on below-median days]" — stating the exact opposite of the real relationship. This is the kind of bug that would look completely plausible (a well-formed sentence with a real percentage) while being actively misleading, and a test that only checked "is there a sentence" would have passed it. Fixed by comparing `avgBelow` directly against `avgAboveEq` for the direction word specifically, and re-verified: the same dataset now correctly reads "...averages 24% lower".
  - Verified against constructed data with a known, deliberately near-perfect relationship (task% ≈ 1.5× recovery, clipped at 100): 20 paired days, r = +0.96 ("strong"), median split at exactly the hand-calculated value (50.5), and — after the fix — a sentence that correctly states the true direction of the relationship. Separately confirmed the 14-point honesty gate with an 8-pair dataset (correctly blocked, correct count in the message) and that switching presets/dropdowns correctly re-renders and clears preset highlighting on manual selection. Zero console errors. Screenshot with noisier, more realistic constructed data (still positively trending but with scatter) showed a visually convincing upward scatter, correct r and sentence, and the disclaimer always present. Mobile geometry check at 380px confirmed both dropdowns and the scatter fit with no overflow.

- **3.6 Auto-suggest time slot (planner.html)** — DONE (2026-07-18), the last Phase 3 section. Answers: 30-minute default duration when unset; no protected blocks for v1 (only real calendar events count as busy — training windows/meal times/wind-down were the master prompt's own speculative guesses at a routine it doesn't actually know, not defaults to accept).
  - A "🕐" button appears only on undone, non-read-only task rows (a done task doesn't need scheduling; the read-only "tomorrow, locked until 6 AM" rows already disable every other control the same way). Opens a `.dr-modal` reusing the exact same modal pattern already used for the Calendar event editor in this same file, rather than inventing a new modal style.
  - Busy intervals come from two sources merged and sorted together: today's local calendar events (`local_cal_events_v1`) and, if connected, today's real Google Calendar events via the same `fetchGcalEvents()` built in 1.5. No Google connection just means the search only avoids local events — a soft degrade, not a hard requirement to use the feature.
  - Waking hours reuse the existing `day_ring_times_v1` Settings value (same key dashboard's Day Ring already reads/writes) rather than asking for a separate wake/bed time just for this feature — one source of truth for "when the day starts and ends" across the app.
  - The search always starts from `max(wake time, right now)`, never the past, and rounds up to the next 5-minute mark so a suggestion never lands on an odd time like 2:47. Walks forward through the sorted busy list, and returns the first gap wide enough that also ends before bedtime — or an honest "no slot found, try shorter" message rather than forcing a bad suggestion.
  - **Second real regression of the exact same bug this build already fixed once, caught only by testing**: the accept handler called `loadCalendar()` without awaiting it before setting the "Added to Google Calendar" confirmation — the identical race from 1.5's quick-add, where `loadCalendar()`'s own "Loading…"/blank status writes clobber whatever was written just before it. 1.5's build-log entry says outright to await it first; this later section's accept handler was written without applying that already-documented lesson. Caught the same way as before — the mocked-fetch test showed an empty `calStatus` where "Added..." should have been — and fixed identically (`await loadCalendar()` before the confirmation write). Worth remembering this class of bug can recur even after being fixed and documented once, if a later section copies the shape of the code without re-deriving why the ordering matters.
  - Verified the slot math against deliberately time-relative constructed scenarios (wide-open waking hours so "now" is always the effective floor, avoiding dependence on whatever the real wall-clock hour happened to be during the test run): no busy events → a slot starting almost immediately; bedtime forced to ~6 minutes away with a 30-minute task → correctly reports no slot rather than suggesting an impossible one; restored open hours with a 15-minute task → correct 15-minute-wide suggestion. Confirmed the duration field resets to the 30-minute default on every fresh open (not remembered from a prior session, which is the intended behavior, not a bug my own test briefly mis-assumed). Confirmed the full accept flow with a mocked token + Calendar API: correct summary, correct start/end, correct duration, modal closes, and (after the fix) the confirmation message actually stays on screen. Zero console errors. Screenshot confirmed the modal matches the existing Calendar-event modal's visual language exactly. Mobile geometry check confirmed no page-level overflow (a same-class-name selector ambiguity in the test itself, not the app, briefly obscured two of the six checked elements — resolved by relying on the definitive `body.scrollWidth`-vs-viewport check instead, which is unambiguous).

This closes out Phase 3 (3.1–3.6) and the master build prompt's phased feature work. Remaining before considering everything done: a pass through the "Acceptance Checklist" section of this document.

## Phase 4

- **Phase 4 appended 2026-07-18**, same one-section/ASK-FIRST-before-building process as 0-3. Some of 4.1's architecture (the Anthropic tool-use loop in api/chat.js, and a working calendar-event tool) already existed from ad-hoc same-session work done *before* this phase was formally appended — treated as a head start, not rebuilt.
- **4.1 questions** — answered before building: use the v1 tool list as specified (all 13, including get_data and remember/forget — remember/forget deferred to 4.3 itself, since building them now would mean deciding 4.3's own open questions early); add_calendar_event keeps the confirm-first card and now writes to both `local_cal_events_v1` and Google (previously Google-only); undo is an immediate in-chat link only, no persisted revert log.
- **4.1 Tool Use** — DONE (2026-07-18). 12 tools + `get_data` implemented in `ask.html` via a `TOOL_REGISTRY` (schema + `confirmMode` + `execute` per tool) and a generalized `runTurn()` loop: silent tools execute immediately and the loop re-POSTs the tool_result for another round; confirm-gated tools render a proposal card and pause, resumed later by `resolvePendingAction` on click. `api/chat.js` extended to pass `tools` through, accept array-shaped tool_use/tool_result message content (previously string-only), and return `{reply, toolUse, rawContent}` instead of just `reply`.
  - **Every data shape was verified against the real code before writing a tool against it, not assumed** — caught in the process: weight lives in gym.html's own `po_coach_weights` (simple array, *not* behind the `state`/`normalize()` pipeline that had been a worry from the Weekly Report section), shopping items need `lastsValue`/`lastsUnit`/`estDays` fields the tool doesn't collect (defaulted sensibly rather than omitted, which would have produced a malformed item the real Shopping List UI half-renders), and stack items resolve through `stack:items` (a possibly-customized live copy) not the static `STACK_DEFAULTS` template array.
  - **`confirmMode` is not a fixed per-tool flag** — it's `'never'` / `'always'` / or a function of the tool's own input, because the spec's rule ("destructive or overwrite... requires confirm, logging executes silently") only makes some tools *conditionally* confirm-gated: `log_weight`/`log_sleep`/`log_habit` are silent for a brand-new day's entry but render a confirm card the moment that call would overwrite an existing one for the same date — checked live against the real store at call time, not guessed from the tool name.
  - **Tasks had no `id` field at all before this** (`{text, done}` only, de-duplicated by text everywhere in planner.html) — added `id` for tasks the assistant creates, plus a small fix to planner.html's `rollover()` and the "push to tomorrow" button so an id survives both the daily fold-forward and a manual push, not just a fresh add. Tasks predating this field are still addressable, via a synthesized `text:<exact text>` reference `get_data` hands out instead of a fabricated id — same non-fabrication precedent as goals' `createdAt` backfill in 1.6.
  - **Goals had no notes concept either** — added `notes: []` (array of `{text, at}`, additive/backward-compatible, same pattern as the task id above) directly on the goal object rather than a separate keyed store, matching how contacts already keep notes inline.
  - **Every write goes through `logAssistantActivity()`**, appending to `eq.assistant.activity_log_v1` (capped 200) and returning an id used two ways: an in-chat "Undo" link (in-memory closure per entry, session-only, per the answered undo-scope question) and a new read-only "Assistant Activity" section in settings.html listing the full log. Silent-tool results render as a small inline card ("✓ Logged 82.4 kg") with that Undo link; failed/not-found results (e.g. `log_contact` for an unmatched name) render the same card shape with a ⚠️ and no Undo link, and the failure reason is fed back to Claude as the tool_result so it can naturally explain/ask rather than the app inventing UI copy for every possible failure.
  - **A hard `MAX_TOOL_ROUNDS = 6` cap** on the silent-tool loop, client-side — not in the spec, added because a loop with no cap and a model that keeps reaching for tools has no natural stopping point other than running up API spend against the user's own key.
  - Verified end-to-end with a scripted, stateful mock of `/api/chat` (and the Google Calendar create endpoint) driving six real scenarios through the actual page: a silent add_task followed by its Undo (task removed, activity entry marked undone, no button left to double-click); log_weight silent on a fresh day *then* correctly switching to a confirm card on a same-day re-log, with the stored value verified unchanged until the actual confirm click; add_calendar_event's confirm writing to both `local_cal_events_v1` and a mocked Google Calendar create in one action; log_contact against a nonexistent name failing gracefully with the right tool_result text back to Claude; and the round cap stopping a deliberately infinite tool-call sequence at exactly the limit with a visible error rather than hanging or silently truncating. Zero console errors across all six.
  - **Re-verified from scratch in a follow-up session-continuity check** (asked explicitly since the build had spanned a context/usage boundary) — re-read every touched file end to end for structural completeness (balanced braces, proper closing tags, all 13 registry entries matched to a real `exec_*` function) before re-running a fresh, wider test hitting all 12 tools individually. That pass caught one real gap: `add_task`/`add_shopping_item`/`add_book`'s tool_result summaries never included the id they'd just generated, so a same-conversation "add X, then do Y to it" couldn't chain without an extra `get_data` round-trip (the tool's own description anticipated this as a fallback, so nothing was actually broken — just less direct than it should be). Fixed by including `(id: ...)` in those three summaries; re-tested with a corrected harness that reads the id back out of the mocked tool_result the way Claude actually would, rather than assuming a reference upfront. (Also caught two bugs in the *test* harness itself along the way, not the app — an operator-precedence mistake that silently swallowed one assertion's label, and an unrealistic hardcoded task reference — both fixed before trusting the result.)

## Acceptance Checklist pass

- **Export/Import round-trips every feature's data** — confirmed by reading, not assumed: `settings.html`'s export loops `localStorage.key(i)` for `i` in `[0, localStorage.length)`, a fully dynamic enumeration with no hardcoded key list. Every key added across this entire build (sleep/recovery logs, contacts, books, snapshots, weekly reports, ask conversation state, etc.) is captured automatically — no code change was ever needed for this to keep working, and none is needed now.
- **PWA precache is complete** — `sw.js`'s `APP_SHELL` list cross-checked line-for-line against the actual 16 `.html` files on disk (added `ask.html` in Phase 2; found and added the already-existing but previously-missing `settings.html` at the same time). `CACHE_NAME` bumped to `v2` per the file's own documented convention for shell-list changes.
- **No "coming soon" stubs** — one match found (`planner.html`'s Projects Filter/Sort buttons), but it's the same disabled-with-honest-tooltip pattern already reviewed and deliberately kept in Phase 0.2 ("already honest about being unavailable," not a misleading stub). No new instances from Phase 1-3 work.
- **No API key in client-side code — real, pre-existing violation found and fixed, not just checked.** The existing "✨ Polish" goal-rewording feature had a literal `const ANTHROPIC_API_KEY = ''` with a "paste your key here" comment in **both** dashboard.html and planner.html's client-side source, calling `api.anthropic.com` directly from the browser (Anthropic's API even requires a special `anthropic-dangerous-direct-browser-access` header to allow this, which is itself a signal). The key was empty/inert, not a live leak, but the pattern itself is exactly what this checklist item exists to catch. Confirmed with the user before touching it, since it's a pre-existing feature this build didn't originally scope in. Fixed two ways: dashboard.html's copy was 100% dead code (zero other references anywhere in the file — the button it belonged to was removed in 0.7's dedup) and was simply deleted; planner.html's copy is the real, live Polish button, and now calls `/api/chat` (the same serverless proxy Phase 2 built for Ask Claude) instead of Anthropic directly — same `ANTHROPIC_API_KEY` env var, zero new setup, no key anywhere in client code anymore. Also fixed the model string in passing (`claude-sonnet-4-5`, the same kind of stale ID the master prompt itself used for Phase 2 — now goes through api/chat.js's already-current `claude-sonnet-5` instead of needing its own fix). Verified with a mocked `/api/chat`: successful polish returns the polished text and never touches `api.anthropic.com`; a simulated server error still falls back to the original as-typed text with the same user-facing message as before, also without ever touching Anthropic directly. Final grep across every `.html`/`.js` file confirms zero remaining client-side references to the key constant or the direct API endpoint — the only matches left are `api/chat.js` itself (server-side, correct) and an informational string in ask.html naming the env var for the user's benefit.
- **Gmail is readonly-scope, cannot send** — confirmed by construction in 1.4: `gmail.readonly` is the only scope ever requested, and there is no compose/reply/send/modify code path anywhere in `ask.html` or `dashboard.html`'s Gmail integration.
- **Dashboard aggregates, Planner owns tasks/goals, no duplicated editable components** — re-confirmed no Phase 1-3 section reintroduced duplication after 0.7's dedup: 1.8's chart and 3.3's report are both read-only on dashboard.html; 1.5's quick-add and 3.6's slot-suggester both live on planner.html, where tasks are actually edited.
- **Archived goals are immutable and present in Obsidian under their folder** — re-confirmed from 1.7: Goal History rows render title/date/count as plain text with no edit affordance at all (not just disabled controls — there are none to disable), and linked projects' Obsidian pages are moved into `Archive/<goal title>/` at archive time.
- **All charts share one visual pattern — mostly true by construction, one deliberate exception.** Sleep Log (1.2), To-Do Completion (1.8), and every line/area chart reuse gym.html's exact SVG pattern (viewBox, quadratic-smoothing path, area fill, glow filter, dot styling) with only the color tokens swapped per page. The one exception is 3.5's Correlation view, which uses a scatter plot instead — a deliberate choice, not an oversight (documented in 3.5's own entry above): Pearson r is inherently about paired x/y values, and a line chart would visually imply a time-series story the correlation itself doesn't claim. It still shares the same card wrapper, grid-line styling, and color tokens as every other chart, so it reads as part of the same family even though the plot type differs for a data-shape reason.
- **Every new section matches the dark card aesthetic on a 380px viewport** — every section built this session was individually geometry-checked at 390px (10px more generous than the checklist's 380px) via `getBoundingClientRect()`, never just screenshots, per the methodology this build settled on after 1.3's screenshot-viewport mismatch false alarm. Re-ran a final check at the *exact* 380px the checklist specifies against dashboard.html (the single densest page — Gmail, Tasks, Completion Chart, Calendar, Goals, Health, Fitness, Finance, and Weekly Report all stacked) to be certain rather than assume the 10px margin was automatically safe: confirmed `body.scrollWidth === document.documentElement.clientWidth === 380`, no horizontal overflow anywhere on the page.

## Key architecture facts discovered while exploring (as of 2026-07-18, verify against current code before relying on these — they will drift as the build progresses)

- WHOOP OAuth is fully wired server-side already (`api/whoop-callback.js`, `whoop-refresh.js`, `whoop-data.js`) — not a placeholder.
- Google Calendar OAuth is fully wired server-side already (`api/google-calendar-callback.js`, `google-calendar-refresh.js`) — not a placeholder. Token handback lands on `dashboard.html#gcal_access=...`.
- There's a live cross-device sync layer on top of localStorage via Supabase: `sync.js` (`window.initCloudSync`, used by dashboard/health/gym/finance-style pages) and `vitality-bridge.js` (`window.Vitality.load()/save()`, used by the "sealed" tile pages: peak/train/vitals/notes/brand.html). Global export/import (0.6) is a portability/backup layer on top of this, not the only sync mechanism.
- Obsidian sync already runs through one shared module (`obsidian-sync.js`, `window.ObsidianSync`) and one shared localStorage config key (`obsidian_sync_config`), used by both planner.html and notes.html. What might still be duplicated is the settings UI/form for editing that config, not the underlying data.
- 11 pages total, not 7: dashboard(Main)/health/gym(Fitness)/peak/finance/planner/ability/notes/train/vitals/brand. Before 0.4, navigation was split two incomplete ways — a top "cat-tabs" row (Main/Health/Fitness/Peak/Finance/Planner/Ability only) duplicated verbatim across 9 pages, and topbar.js's bottom bar (Main/Health/Fitness/Peak/Train/Notes/Vitals/Brand, missing Finance/Planner/Ability) shown only on notes.html/brand.html, which don't have the top row. No single nav reached all 11 pages. Check current state of topbar.js and the `cat-tabs` markup before assuming 0.4's fix is still needed — it may already be done.
- Dashboard/Planner duplication (0.7): the Day Ring and Goal Ticker are dashboard-only (not duplicated). What's duplicated near-verbatim is the Projects section (including a "Gantt view coming soon" stub) and Checklists. Dashboard also has its own separate deadline-based Goals tracker (`dgt*` prefixed IDs) distinct from the daily-checklist goals (`gm*` prefixed IDs) — check whether Planner has an equivalent before deduping it.
- finance.html stored amounts internally pegged to CHF before 0.6 (variable names like `amountCHF`, exchange rates fetched from `.../latest/CHF`), with CHF/USD/EUR/GBP as display-only picker options tied to a `nw_currency` localStorage key.
- "Fuel" text found outside the already-removed Fuel page is unrelated: a post-workout nutrition nudge in health.html and a caffeine-log legend in peak.html. Different feature, not calorie logging — intentionally left alone.
