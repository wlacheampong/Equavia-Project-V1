# EQUAVIA — MASTER BUILD PROMPT FOR CLAUDE CODE

Paste this entire file into Claude Code at the repo root. Work through it **one phase at a time, one section at a time**. Do not build ahead.

---

## HOW TO WORK (read first, applies to everything)

1. **Before building each numbered section:** ask me every question listed under its `ASK FIRST` block, plus anything else ambiguous. Do not silently default on wording, thresholds, tier names, data shapes, OAuth scopes, or edge cases. Wait for my answers, then build.
2. **Explore before touching.** Read the existing codebase first. These are plain HTML pages (dashboard.html, health.html, gym.html, peak.html, finance.html, planner.html, ability.html, notes.html) with inline/linked JS and localStorage persistence, deployed on Vercel. Match the existing patterns — do not introduce a framework, bundler, or build step unless a section explicitly requires a serverless function.
3. **Match the existing dark visual language** (near-black backgrounds #050506/#0a0a0b, card-based layout). New sections must be indistinguishable in style from existing ones.
4. **localStorage discipline:** every new feature namespaces its keys (`eq.books.*`, `eq.contacts.*`, `eq.sleep.*` etc.). Every feature's data must be included in the global export/import built in Phase 0.
5. **Reuse the existing chart pattern** (the Weight graph on gym.html) for every new chart. One charting approach across the whole app.
6. **Mobile-first.** Assume primary use is a phone with the app installed as a PWA.
7. **After each section:** show me what changed, list the files touched, and wait for my confirmation before moving on.
8. **Do NOT build any food/calorie logging.** Nutrition is handled in MyFitnessPal separately, outside this app. If any old "Fuel" references exist, remove them.

---

# PHASE 0 — FOUNDATION & CLEANUP (do this before any new features)

## 0.1 Identity & naming
- Unify the app name across every `<title>`, header, splash page, and manifest.
- ASK FIRST: final app name ("Equavia"? "William's Dashboard"? something else), and whether the splash-image landing page stays or the app should open straight to the dashboard.

## 0.2 Remove template artifacts
- health.html: remove the visible `// editable template · all data stays in your browser` text and embed the water tracker properly instead of a raw `po-water.html` link.
- peak.html: delete the third-party Patreon "Setup ↗" link and the pasted all-caps marketing copy block ("CAFFEINE RUNS REAL PHARMACOKINETICS…", "MEMBERS: TELL THE MENTOR…"). Rewrite the page's explanatory text in the app's own voice, or reduce it to a short one-paragraph description.
- Hide all "coming soon" UI (Gantt) — features that don't exist yet should not be advertised.

## 0.3 Empty states
- Replace every bare "—", "0/0", "Loading…" placeholder across all pages with a proper empty state: one line describing what the panel does + one clear action, in the style of gym.html's "Log your first weight to start tracking."

## 0.4 Bottom navigation
- Replace the plain text link row with a fixed bottom tab bar on mobile (icons + labels, active-state highlight on the current page). Keep a top nav or the same bar on desktop.
- ASK FIRST: there are 7+ pages and a mobile tab bar fits ~5. Which pages get first-class tabs and which fold into a "More" sheet?

## 0.5 PWA
- Add `manifest.json` (name, icons, standalone display, theme color) and a service worker (cache-first app shell so all pages load offline; localStorage data is already local).
- ASK FIRST: do you have a logo/icon file, or should a simple monogram icon be generated?

## 0.6 Global backup & settings page
- New `settings.html` (reachable from the nav/More sheet) containing:
  - **Export all** — one JSON file containing every localStorage namespace, with an export date. **Import all** — restores it, with a confirmation warning.
  - "Last backed up X days ago" indicator surfaced on the dashboard when > 14 days.
  - Global preferences: currency (default **GBP**, not CHF — update finance.html default), units, wake/bed times (moved here from dashboard modal but still editable there), Obsidian connection (single shared config — deduplicate the two separate Obsidian sync configs currently on planner.html and notes.html into one, both pages read the same stored connection).

## 0.7 Dashboard deduplication
- dashboard.html and planner.html currently both render full Goals + To Do components. Refactor: **Planner owns the full editable components; the Dashboard shows compact read-only summary cards** (today's tasks done/total, top 1–2 goals with countdown, streak) that link into Planner.
- The dashboard becomes an aggregator: add compact summary cards for Health (recovery % / sleep hrs once Whoop or sleep log has data), Fitness (last session, current weight + 7-day avg), Finance (net worth + month cash flow), each linking to its page.
- ASK FIRST: confirm this direction before refactoring, and which summary cards you want in which order.

---

# PHASE 1 — CORE ADDITIONS

## 1.1 Book Tracker (Ability page)
- New card section on ability.html: three statuses — **Want to Read / Reading / Read**.
- Each book: title, author, status, optional 1–5 star rating, optional notes. "Read" entries record and display date finished.
- Add via "+" button; change status via dropdown (no drag needed on v1).
- Optional cover images: fetch from Open Library covers API by title/author search (`https://openlibrary.org/search.json`), cache the image URL; graceful text-only fallback.
- A "Reading" book can optionally show a progress % slider.
- ASK FIRST:
  - Cover images on or off for v1?
  - Progress % on currently-reading books — wanted?
  - Show a yearly "books finished" count (Goodreads-challenge style) with a target?

## 1.2 Sleep Log + Trend Chart (Health page)
- Below the existing WHOOP sleep-stages block: nightly total sleep hours log.
- If the WHOOP connection is live and returns sleep totals, auto-fill each night from it; manual entry/override always available for missing nights. Manual entries flagged visually as manual.
- Line chart (existing chart pattern): sleep hours per night, toggle 30/90 days, horizontal reference line at a configurable target.
- ASK FIRST:
  - Is the "Connect WHOOP" button currently wired to the real WHOOP API (OAuth working), or is it a placeholder? (Determines whether auto-pull is buildable now or the log starts manual-only.)
  - Sleep target in hours?
  - Also chart a 7-night rolling average line?

## 1.3 Relationship Tracker (Planner page)
- New card section: contacts with **name, relationship type (family/friend/colleague/other), tier, target check-in cadence in days**.
- "Logged contact today" button per person → updates last-contacted date. Also allow backdating ("logged contact on [date]") for catch-up entry.
- **Overdue flag** when days-since-contact > cadence; list sorts overdue-first, most-overdue at top. Days-overdue count shown.
- Dashboard: if anyone is overdue, the Planner summary card shows "N people overdue" badge.
- Optional per-contact notes field (what you last talked about — useful before reaching out).
- ASK FIRST:
  - Exact tier names and how many ("priority tier" was placeholder — e.g. Inner circle / Close / Network, or A/B/C?). Should tier auto-suggest a default cadence (e.g. Inner=7d, Close=14d, Network=30–60d)?
  - Birthday field with an upcoming-birthday flag — wanted?
  - Roughly how many contacts will this hold (affects whether search/filter is needed at v1)?

## 1.4 Notification Hub — Gmail, READ-ONLY (Main dashboard)
Purpose: see that a message arrived and what it says **without opening Gmail or having mail apps on the phone** — no doomscroll vector. **No composing, no replying, no sending from this app.**
- Connect Gmail via Google OAuth using the **`gmail.readonly` scope only** (deliberately minimal — easier consent, zero send risk).
- Implementation: Google Identity Services token flow client-side (PKCE); no server storage of tokens. Token kept in memory/session, silent re-auth where possible.
- Panel on dashboard: sender, subject, snippet, time for the most recent N messages. Tapping a message expands the snippet to the fuller plain-text body preview **in-app** (still read-only). An external "Open in Gmail" link is allowed as an explicit escape hatch.
- Unread indicator + count badge. Refresh: manual button + polling interval.
- Instagram/Meta DMs: **excluded permanently** — personal-account third-party DM access isn't available (Basic Display API deprecated; business-only review process). Do not build around it.
- Cross-link (small but high value): if a Gmail sender's name/email matches a Relationship Tracker contact, show a small badge on the message ("tracked contact") and offer one tap → "count this as contact logged."
- ASK FIRST:
  - How many messages to show (10? 20?), unread-only or all recent?
  - Polling interval (every 5 min while open? manual only?)
  - Allow "mark as read" from the app? (Requires `gmail.modify` scope instead of readonly — say no if you want the strictest scope.)
  - One Gmail account or multiple?

## 1.5 Calendar Quick-Add → Google Calendar (Planner)
- On planner.html's Calendar section: input "What are you doing?" + date/time picker + duration.
- On save: create the event via Google Calendar API (same Google OAuth session as 1.4 where possible — request `calendar.events` scope) AND render it in the local in-app calendar immediately. Inline success/failure state.
- Also pull upcoming Google Calendar events into the in-app calendar view (read), so the "Connect Google Calendar" button that already exists becomes real.
- ASK FIRST:
  - Which Google account and which calendar (if several)?
  - Two-way (show Google events in-app) confirmed, or push-only?
  - Should quick-add support natural-ish parsing ("gym tomorrow 6am") or strictly the structured fields for v1?

## 1.6 Color-Coded Live Goal Countdown
- Applies to the existing live day/hr/min/sec countdown per goal.
- `percent_remaining = (deadline − now) / (deadline − start_time) × 100`
- Color stops: 100–76% neutral · 75% dark green · 50% green · 40% light green · 35% lighter green · 30% yellow · 20% orange · 10% red · 5–0% dark red + blinking.
- **Exception:** if the goal's progress slider is at 100%, always neutral — skip the gradient entirely.
- Implement as discrete steps or smooth HSL interpolation — whichever renders cleaner against the dark theme; show me both if trivial.
- Accessibility: "blinking" should be a slow opacity pulse (~1s ease), not a hard flash; respect `prefers-reduced-motion` by falling back to static dark red.
- ASK FIRST: confirm the pulse-instead-of-blink substitution is acceptable.

## 1.7 Goal Archive at 100% → History + Obsidian Folder
- When a goal's progress slider hits 100%: show an **Archive** button on that goal.
- On archive: remove from Dashboard entirely → append to a collapsed/expandable **Goal History** section on planner.html only (shows title, completion date, linked project count).
- Obsidian: using the existing per-project Obsidian page sync, move/copy the archived goal's linked projects' pages into a vault folder named after the goal title (create folder if missing, sanitize illegal filename characters).
- Archived goals and their linked projects become **read-only** — sliders and edits disabled, with a visible "archived" state.
- ASK FIRST:
  - Folder naming: exact goal title, or `Archive/<goal title>`, or date-prefixed (`2026-07 — <goal>`)?
  - Should un-archiving be possible, or is archive permanent?
  - If a project is linked to two goals and one archives — what happens? (Propose: project stays live until all linked goals archive; confirm.)

## 1.8 To-Do Completion Rate Chart (Main dashboard)
- Daily completion % = tasks completed ÷ tasks scheduled that day × 100. Store a daily snapshot at day rollover (6 AM reset already exists — snapshot at reset).
- Line chart under the To Do summary: toggle 4 / 12 / 52 weeks, raw daily points + **7-day rolling average line** (daily % is noisy; the average is the real signal).
- ASK FIRST:
  - Tasks pushed to tomorrow — count as missed for that day (recommended, keeps the metric honest), or excluded?
  - Days with zero scheduled tasks — skip in the chart, or count as 100%? (Propose: skip — gaps render as breaks.)

---

# PHASE 2 — "ASK CLAUDE" PAGE (in-app AI chat over my data)

- New nav entry + `ask.html`: standard chat UI (message list, input, send).
- Backend: **Vercel serverless function** (`/api/chat`) calling `https://api.anthropic.com/v1/messages`. The Anthropic API key lives ONLY in a Vercel environment variable — never shipped client-side, never in the repo.
- Context injection: before each request, the client assembles a compact current-state summary from localStorage (selected domains only) and sends it with the message; the serverless function forwards it as system context.
- Include a per-message domain toggle row (chips: Health · Fitness · Goals/Tasks · Finance · Habits · Relationships) so context is opt-in per conversation — full-everything context on every message wastes tokens.
- Basic protections in the serverless function: max tokens cap, simple rate limit, reject non-POST.
- Show estimated context size ("~2.1k tokens of your data attached") so cost stays visible.
- ASK FIRST:
  - Which domains ON by default?
  - Session-only history, or persist conversations to localStorage?
  - Model: `claude-sonnet-4-6` (smarter, costlier) vs `claude-haiku-4-5-20251001` (cheap, fast) as default — or a toggle?
  - Confirm you'll add `ANTHROPIC_API_KEY` in Vercel env settings.

---

# PHASE 3 — BORROWED PATTERNS FROM REFERENCE APPS

Build in this order; each is standalone.

## 3.1 Year Grid per habit (from Way of Life)
- ability.html: next to each habit's streak, a whole-year grid (365 cells, green = done, red = missed, grey = not yet/paused). Success % for the year shown alongside — so one miss doesn't visually erase months of consistency.
- Compact per-habit mini-grid on the card; tap to expand full-year view.

## 3.2 Habit Health Score with decay (from Loop Habit Tracker)
- Alongside (not replacing) the streak: a 0–100 score per habit using exponential smoothing — completing raises it toward 100, a miss decays it gradually instead of zeroing it. Loop's approach: score = previous × k + done × (1 − k), k derived from habit frequency.
- Display as a small ring/bar per habit. ASK FIRST: keep the fire-streak too, or make the score primary?

## 3.3 Weekly Report widget (from Gyroscope)
- One combined report card, generated at week end, spanning: Health (avg sleep, avg recovery if Whoop live), Fitness (sessions done, total volume, weight 7-day avg change), Planner (task completion %, goals progressed), Ability (habit success %), Finance (month cash-flow pace).
- Renders as a single shareable-looking card on the dashboard; keeps the last 12 weeks browsable.
- Also: one tap "Write to Obsidian" → weekly note via the existing sync.
- ASK FIRST: which day/time does the week close (Sunday 9 PM?), and which metrics make the cut for v1.

## 3.4 Quick Capture from anywhere (from Second-Brain templates)
- A floating "+" (or long-press on the nav's center tab) available on every page → small sheet: text input + destination chips (Task today · Task tomorrow · Note · Shopping list · Book to read). Saves to the right store without leaving the current page.

## 3.5 Correlation view (from Exist.io)
- New card on health.html: pick metric A × metric B from tracked data (sleep hrs, recovery %, task completion %, habit score, weight 7-day trend) → scatter/dual-line over the last 60–90 days + Pearson r with a plain-English line ("On nights under 7h sleep, next-day task completion averages 22% lower").
- Honesty rules: require ≥ 14 paired data points before showing r; always label it correlation, not cause.
- ASK FIRST: the 2–3 metric pairs you actually care about first, so defaults are pre-set.

## 3.6 Auto-suggest time slot (from Motion — lightweight version)
- On unscheduled Planner tasks: a "suggest slot" button → looks at today's in-app + Google Calendar events, finds the first open gap ≥ the task's duration inside waking hours, proposes it; one tap accepts and creates the calendar block.
- Rules-based only — no AI needed. ASK FIRST: default task duration when unset (30 min?), and protected blocks it must never schedule over (training window 6:00–7:30 AM, meals, wind-down after 9 PM?).

---

# ACCEPTANCE CHECKLIST (verify after all phases)

- [ ] No page shows template artifacts, third-party links, or "coming soon" stubs
- [ ] Every feature's data round-trips through global Export/Import
- [ ] App installs as a PWA and all pages open offline
- [ ] All charts share one visual pattern
- [ ] Gmail integration is readonly-scope and cannot send anything
- [ ] No API key of any kind appears in client-side code or the repo
- [ ] Dashboard aggregates; Planner owns tasks/goals; no duplicated editable components
- [ ] Archived goals are immutable and present in Obsidian under their folder
- [ ] Every new section matches the dark card aesthetic on a 380px viewport

---

# PHASE 4 — ASK CLAUDE → FULL AI ASSISTANT

Requires Phase 2 (Ask Claude page + serverless function) complete. Work section by section, ask the `ASK FIRST` questions before building each.

Global rules for this phase:
- The assistant can READ everything and WRITE only through defined tools. No tool = no action.
- The assistant can never contact the outside world (no email sending, no messages, no posting). Read-only Gmail stays read-only.
- Every write the assistant makes is tagged `source: "assistant"` in its localStorage record, and listed in an "Assistant activity" log viewable in Settings — full audit trail of what it did and when.
- All costs run against my own Anthropic API key. Show token estimates where practical.

## 4.1 Tool Use — write access to the app

Define the app's actions as Anthropic tools in the serverless `/api/chat` call. Claude requests a tool; the CLIENT executes it against localStorage and returns the result; the loop continues until Claude produces a final text reply.

Tool set (v1):
- `add_task(title, day: "today"|"tomorrow")`
- `complete_task(task_id)` / `push_task_to_tomorrow(task_id)`
- `log_weight(kg, date?)`
- `log_sleep(hours, date?)`
- `log_contact(contact_name, date?)`
- `add_calendar_event(title, date, time, duration_min)` — local + Google if connected
- `add_shopping_item(name, grams?, qty?)`
- `add_book(title, author?, status)`
- `check_stack_item(item_name)` — mark a supplement taken
- `log_habit(habit_name, done: true|false, date?)`
- `add_goal_note(goal_id, note)`
- `remember(fact)` / `forget(fact_id)` — see 4.3
- `get_data(domain, range)` — explicit read tool so context injection can shrink: instead of front-loading everything, Claude pulls what it needs

Rules:
- Logging/reading tools execute silently. Destructive or irreversible ones (delete, archive, overwrite an existing entry) require an in-chat confirm chip before executing.
- Tool results render as small inline action cards in the chat ("✓ Logged 82.4 kg") with an undo link where feasible (undo = delete the created record).
- Unknown references ("push the dentist thing") — Claude uses `get_data` to search tasks, asks if ambiguous.

ASK FIRST:
- Confirm the v1 tool list — anything to add/remove?
- Should `add_calendar_event` be allowed to write to Google Calendar directly, or local-only with a "sync" confirm?
- Undo window — keep it simple (immediate undo link only) or a full revert log in Settings?

## 4.2 Universal command bar / natural-language quick log

- The Quick Capture "+" (Phase 3.4) gains a "smart" mode: free text goes to the assistant with tools enabled, parsed into the right records. "slept rough, 6 hours, skipped morning stack, weight 82.9" → sleep entry + stack state + weight log in one pass, with a compact result card listing what was logged.
- Structured mode (destination chips) remains as the zero-cost path; smart mode is opt-in per capture because it costs a token round-trip.
- ASK FIRST: which model for smart-capture parsing — Haiku is likely sufficient and keeps per-capture cost trivial. Confirm.

## 4.3 Persistent memory

- `eq.assistant.memory` store, max ~30 entries, each `{id, fact, created}`.
- Claude can call `remember()` when I state a durable preference or fact, and `forget()` when I correct one. All memory entries injected into the system prompt of every assistant call.
- Settings page: memory viewer with manual add/edit/delete.
- Seed on first run by asking me 3–5 onboarding questions (current goal phase, training split, sleep target, tone preference for advice).
- ASK FIRST: confirm the cap, and whether the assistant should ask permission before saving a memory or save silently and show a "remembered" chip.

## 4.4 Morning Brief

- Dashboard card, generated on first open after the 6 AM reset (cached for the day; manual regenerate button).
- Serverless assembles: last night's sleep (Whoop or log), recovery % if live, today's tasks + calendar, overdue contacts, unread-count from Gmail hub, subscription renewals ≤ 7 days, habit states, weight 7-day trend.
- Claude writes ≤ 6 lines: state of play + ONE recommended focus for the day. No generic filler, no motivational padding.
- Evening variant ("Close the day" button after 8 PM): reviews completion vs plan, asks for tomorrow's top task if unset, writes the day summary to Obsidian daily note via existing sync.
- ASK FIRST: auto-generate on open vs button-only (auto = 1 API call/day baseline cost). Include evening variant in v1?

## 4.5 WEEKLY AUDIT — the core review feature

Runs at week close (default Sunday evening; manual "Run audit now" always available). Replaces/absorbs the static weekly report widget from Phase 3.3 — the Gyroscope-style stat card becomes the header of this audit.

**Data in:** the full week across every domain (sleep, recovery, training sessions + volume, weight trend, task completion %, habit grid, goals progressed, contacts logged vs overdue, finance cash-flow pace, stack adherence) PLUS the previous week's audit entry, so every audit is comparative — trajectory, not snapshot.

**Output structure (fixed sections, rendered as cards):**
1. **The week in numbers** — compact stat header (the Phase 3.3 metrics).
2. **What went well** — 3–5 specific, evidence-cited wins ("Stack adherence 96%, up from 84%"). No participation trophies: if the week was poor, this section is allowed to be short.
3. **What went badly** — equally specific, equally evidence-cited. Direct language, no cushioning. Includes broken commitments from last week's audit ("Last week you accepted 'walk after Meal 2 daily' — logged 2/7").
4. **What to do better** — the 2–3 highest-leverage fixes for next week, each tied to a metric that would prove it worked.
5. **Suggested additions** — 1–3 NEW routine/habit/tracking suggestions I can adopt or ignore. Each renders with **[Implement] [Ignore]** buttons:
   - **Implement** → the assistant uses its tools to create the actual habit/task/stack item/rule immediately, tagged as originating from audit week N.
   - **Ignore** → recorded, and the assistant does not re-suggest the same idea for at least 4 weeks (store ignored suggestions with a cooldown date).
6. **Verdict** — one line, one grade or score for the week (ASK: letter grade, /10, or a one-word verdict?).

**Behavior rules:**
- Every claim must cite a number from the data. If data is missing for a domain, say "no data" — never infer or pad.
- Implemented suggestions are automatically checked in the NEXT audit's section 3/2 — the loop closes itself.
- Audits persist (`eq.audits.*`), browsable as a history list; each also writes to Obsidian as `Audits/2026-W29.md` via existing sync.
- Tone: direct, specific, zero flattery. The audit's value is honesty.

ASK FIRST:
- Week close day/time (Sunday 8 PM?).
- Auto-run at close vs manual-only (auto = 1 larger API call/week — this one is worth Sonnet-class; confirm model).
- Verdict format (grade / score /10 / one-word)?
- Any domain to EXCLUDE from audit scope (e.g. finance private from this view)?
- Hard cap on suggestion count per week (default 3)?

## 4.6 Standing rules engine

- Settings section: user-defined rules `{trigger, condition, action}` checked locally on app open — plain JS evaluation, NO API call unless a rule fires and its action is "assistant message."
- Starter rule templates: recovery < 33% → suggest training swap · tracked contact emailed → surface on dashboard · weight 7-day avg moved > X kg in 7 days → flag · habit health score dropped below 60 → flag · subscription renewing in 48h → flag.
- When a rule fires with an assistant action, Claude writes the one-line message with the triggering numbers included.
- The weekly audit (4.5) may propose new rules in its suggestions section; Implement creates them here.
- ASK FIRST: which 3–4 rules to ship enabled by default.

## 4.7 Voice input

- Web Speech API mic button on the assistant chat input and the smart quick-capture sheet. Free, on-device recognition, no key.
- Transcript is shown before sending (tap to edit), then flows through the normal tool loop.
- Graceful fallback: hide the mic if the browser doesn't support SpeechRecognition.

## 4.8 Conversational weekly review (optional layer on 4.5)

- After the audit renders: "Review this week with me?" → 3 short questions max (what felt hardest, what to protect next week, anything the data missed). Answers appended to the Obsidian audit note and stored as context for next week's audit.
- Strictly optional and skippable — the audit must stand alone without it.
- ASK FIRST: include in v1 or defer.

---

## Phase 4 acceptance checklist
- [ ] Assistant can log/create via every v1 tool; destructive actions gated behind confirm
- [ ] Every assistant write is tagged and visible in the Settings activity log, with undo where feasible
- [ ] Assistant cannot send, post, or contact anything outside the app
- [ ] Memory persists, is capped, and is fully user-editable in Settings
- [ ] Morning brief ≤ 6 lines, generated once daily, from real data only
- [ ] Weekly audit: cites numbers for every claim, compares against previous week, tracks implemented/ignored suggestions with cooldown, writes to Obsidian
- [ ] Implement buttons create real records immediately; Ignore suppresses re-suggestion ≥ 4 weeks
- [ ] Rules engine evaluates locally; API calls only on fire
- [ ] Voice input works on supporting browsers, hides elsewhere

---

# PHASE 5 — INTERACTIONS PAGE + NEWS PAGE (two pages, deliberately separate)

Requires Phase 0 (nav, settings, backup), Phase 2 (serverless /api/chat), and benefits from Phase 4 (tools, audit, rules). Work section by section; ask every `ASK FIRST` question before building.

**Purpose:** two pages with one purpose each, kept separate on purpose.
- **Interactions** = *people I know*: who to reach out to, what arrived, what to say. Visited when being social.
- **News** = *the world*: category feed + weekly digest. Visited deliberately, not ambiently.

Do NOT co-locate the news feed with the message inbox — pairing a feed with an inbox recreates the exact check-messages-then-scroll pattern this build exists to escape. The data still flows between them (news→talking topics, digest→audit) regardless of page placement; the split is about attention, not architecture.

**A React prototype exists for the contact manager** (component name "Keep", light theme). Treat it as a UX reference ONLY: port its concepts and layout logic into the app's existing dark plain-HTML + localStorage patterns. Do NOT paste React into the codebase, do NOT adopt its light palette, and do NOT keep its direct client-side `api.anthropic.com` call — every AI call in this phase routes through the existing serverless `/api/chat` so the key stays server-side.

## 5.1 New "Interactions" page + nav slot

- New `interactions.html`. Nav placement: **Interactions gets a first-class tab slot; News (5.6) lives in the More sheet** — the dashboard digest card covers the daily glance, and one extra tap to reach the full feed is intentional friction for the page most at risk of becoming a scroll. (ASK FIRST: which existing page yields its tab slot to Interactions.)
- Two stacked zones only: **People** (contact manager, 5.2–5.4) · **Inbox** (message funnel, 5.5). No news content anywhere on this page — the only news that appears near a contact is matched talking-topic items inside that contact's profile (5.3), which is destination, not feed.
- The Relationship Tracker built in Phase 1.3 MOVES here from Planner (single source of truth; Planner keeps only the overdue-count summary card, dashboard summary card now points here).

## 5.2 Contact manager (port + upgrade of the prototype)

Adopt from the prototype:
- Tier system with cadences — **Inner every 7d · Close every 21d · Acquaintance every 60d · Networking every 90d** (this answers Phase 1.3's tier question; migrate any existing contacts). Tier colors adapted to the dark theme.
- Sidebar list sorted by overdue-ratio (most overdue first), search, tier filter chips, "due" flag, birthday flag when ≤ 14 days out.
- Profile fields: name, birthday, tier, last spoke, free notes.

Fix and extend beyond the prototype:
- **No hardcoded date** — the prototype pins `today` to a fixed date; use real current date everywhere.
- **Interaction history, not a single date** — replace lone `lastSpoke` with an append-only log: `{date, channel (call/text/in-person/other), optional one-line note}`. "Log a chat today" opens a 2-tap sheet (channel + optional note). Last-spoke derives from the log. History renders on the profile, newest first.
- **Interest tags** — alongside free notes, a tag field per contact (e.g. `judo`, `gaming`, `NHS`, `climbing`). Powers talking topics (5.3) and news matching (5.6).
- **Birthday pipeline** — birthdays ≤ 7 days surface on the Morning Brief (4.4) and as a default standing rule (4.6). On the birthday itself, the contact jumps to top of list with a distinct flag regardless of cadence.
- **Snooze** — per contact: "not now, remind in N days" so an overdue flag can be deliberately deferred without logging fake contact.
- ASK FIRST:
  - Confirm the four tier names/cadences or give replacements (note: tiers are labeled by closeness, not usefulness — cadence already encodes priority).
  - Channels list for the history log — call/text/in-person/other enough?
  - Import: start empty, or do you have a contact list (CSV/paste) to seed?

## 5.3 Talking topics + prompts (per contact)

New tab on each contact profile: **"What to talk about."** Three stacked sources, clearly labeled:

1. **Open threads (manual)** — a per-contact checklist of topics to raise ("ask how the 6c project went", "sister's wedding — September"). Add/check-off/edit. Checked items move into that day's interaction history note. The weekly audit may NOT read private notes content — only counts (ASK FIRST to confirm privacy boundary).
2. **From their interests (generated)** — button: "Suggest topics." Serverless call sends the contact's tags + notes + last 2 history notes → Claude returns 4–6 specific conversation prompts tuned to that person (JSON array, rendered as tappable cards, tap = copy). Cached per contact until notes/tags change; regenerate button.
3. **From this week's news (auto)** — any news item (5.6) whose category/keywords match the contact's interest tags appears here as "You could mention: [headline]" with the link. Zero API cost — plain keyword/category matching client-side.

Also port the prototype's **Toolkit** as a page-level (not per-contact) reference card: conversation starters, follow-up questions, open-ended prompts, meeting-spot ideas — static lists, click-to-copy. Keep the content editable in a small settings block so the lists are mine, not hardcoded.

## 5.4 Reply helper (ported through serverless)

Port the prototype's reply helper per contact:
- Inputs: "what they said" (paste), optional "my rough draft."
- Four modes: Open-ended reply · Follow-up question · Change subject · Smooth my draft.
- Output: exactly 3 ready-to-send options, tap-to-copy. Context sent: contact notes + tags + tier + last 2 history notes, so replies fit the actual relationship.
- Route through `/api/chat` (never client-side key). Model: ASK FIRST — Haiku is likely fine here and keeps each generation ~free; confirm.
- Add a fifth mode the prototype lacks: **"Re-open after silence"** — a warm re-engagement opener for overdue contacts, referencing an open thread if one exists. Surface this as the quick action directly on overdue contacts in the list.

## 5.5 Inbox — the notification funnel (honest constraints)

**Platform reality, stated up front so we don't build fiction:** Instagram, WhatsApp (personal), Snapchat, and TikTok expose **no API access to personal DMs**. Meta killed Basic Display; WhatsApp's API is business-messaging only; Snapchat and TikTok have none. No web app can read these directly, and iOS forbids notification interception entirely. What IS buildable:

- **Tier A — Gmail (already built, Phase 1.4):** mirrors here as well as the dashboard. Tracked-contact matching already exists.
- **Tier B — Android notification relay (optional, the only real DM funnel):** if my daily phone is Android, an automation app (MacroDroid / Tasker / BuzzKill) can catch notifications from WhatsApp/Instagram/Snapchat/TikTok and POST `{app, sender, text, time}` to a new Vercel serverless endpoint `/api/inbox` (secured with a secret token in the URL/header). The endpoint stores recent items (Vercel KV or simplest available store) and the Inbox panel polls it. Result: sender + message preview visible in-app, read-only, no social app opened. Claude Code: build the endpoint + panel and write me the exact MacroDroid setup steps as a doc.
- **Tier C — manual quick-log fallback:** whatever can't be relayed, one tap on a contact logs "they messaged me on [app]" so the cadence tracker stays truthful even without automation.

Inbox behavior (all tiers):
- Unified list: source icon (Gmail/WhatsApp/IG/etc.), sender, preview, time. Read-only — no replying from here (reply helper 5.4 is for composing, then I paste into the real app on my own terms).
- Sender-name matching against contacts: matched items show the contact chip; one tap = "count as contact received" (logged in history as inbound). Inbound does NOT reset the cadence clock by default — ASK FIRST: should receiving a message count as contact, or only when I reply?
- A "clear/dismiss" per item; auto-expire after 7 days.
- ASK FIRST:
  - Is your daily phone Android or iPhone? (Determines whether Tier B is buildable at all. If iPhone: state clearly in the UI that DM funneling isn't possible and hide Tier B, leaving Gmail + manual.)
  - If Android: which apps to relay (WhatsApp, IG, Snapchat, TikTok, SMS?), and quiet hours where the relay drops or holds items?

## 5.6 News — standalone page (`news.html`, reached via More sheet)

Own page, NOT on interactions.html. A compact digest card on the dashboard links here.

Design stance — this is a briefing page, not a feed:
- The **weekly digest is the centrepiece** at the top; the live category feed sits below it, collapsed by default.
- No infinite scroll: each category shows a fixed batch (e.g. 10 stories) with an explicit "load more" — ending is a feature.
- Optional "daily cap" indicator (ASK FIRST: want a soft "you've checked news N times today" counter, or is that overkill?).

**Sources (free, no scraping):**
- **The Guardian Open Platform API** — free key, excellent UK/London coverage: `section=uk-news` + `q=London` for local, `world` for global, `politics`, `technology`, `sport`, `games`.
- **RSS via serverless proxy** — `/api/news` endpoint fetches + parses feeds server-side (avoids CORS): BBC London, BBC Sport, The Verge, Eurogamer/PC Gamer, plus any feed I add in settings. Cache responses ~30 min server-side to stay polite.
- ASK FIRST: confirm category list — **London/local · Global · Gaming · Sports (which sports/teams?) · Politics · Tech** — and 2–3 preferred outlets per category. NBA/judo/basketball feeds worth adding given interests?

**Display:**
- Category tabs; each story: headline, source, time, one-line standfirst, **external link opening the original article** (target=_blank). Never reproduce article text beyond title + short snippet — link out for reading.
- "Mark seen" per story; seen stories collapse.

**Weekly digest (the talking-ammunition feature):**
- At week close (same trigger as the audit), a serverless call sends the week's collected headlines+snippets per category → Claude writes a tight per-category summary: 3–5 bullets of "what actually happened this week," each bullet linking its source article. Stored (`eq.news.digests.*`), browsable history, and written to Obsidian as `News/2026-W29.md`.
- The digest is context ammunition: the audit (4.5) and talking topics (5.3) both read from it.
- ASK FIRST: digest on the same weekly API call as the audit (cheaper, one big call) or separate button-triggered call?

## 5.7 Interaction score (feeds the weekly audit)

One number that makes "maintain relationships while off the apps" measurable:
- Weekly score from: % of due contacts actually contacted · overdue count trend vs last week · breadth (distinct people contacted) · inbound left unanswered > 72h (only if Inbox tiers make inbound visible).
- Displayed on the Interactions page header and included as a domain in the Phase 4.5 weekly audit — the audit can now praise/criticize social upkeep with numbers and suggest specific people ("Daniel is 28d over cadence; you have an open thread about his Berlin move").
- ASK FIRST: confirm the inputs and weighting, and whether the score shows as /100 or a simple A–F.

---

## Interconnections summary (why this page multiplies the rest)
- Contacts → Morning Brief (overdue + birthdays) and Rules engine (default rules).
- News tags → per-contact talking topics (free, client-side matching).
- News digest → weekly audit context + Obsidian archive.
- Inbox sender-matching → contact history (inbound logging).
- Interaction score → weekly audit domain; audit suggestions can Implement new contacts' cadence changes or rules via Phase 4 tools.
- Assistant tools (4.1): add `log_contact` variants — `log_interaction(name, channel, note?)`, `add_open_thread(name, topic)`, `snooze_contact(name, days)`.

## Phase 5 acceptance checklist
- [ ] Interactions page holds People/Inbox only; tracker no longer duplicated on Planner
- [ ] News is its own page in the More sheet; no feed content on Interactions; digest card on dashboard links to it
- [ ] Contact history is append-only log; last-spoke derived, never hand-edited except backfill
- [ ] All AI calls (topics, reply helper, digest) go through serverless — zero client-side keys
- [ ] Reply helper produces 3 options in my voice using contact context; overdue contacts expose "re-open" quick action
- [ ] Inbox states platform limits honestly; Tier B only if Android confirmed; relay endpoint token-secured
- [ ] News links out to originals; snippets stay short; digest bullets each carry a source link
- [ ] Digest + interaction score flow into the Phase 4.5 audit
- [ ] All new stores namespaced and covered by global export/import

---

# BUILD LOG (progress notes — updated as work proceeds, not part of the original prompt)

Kept here so a fresh session can pick up exactly where a previous one left off without re-deriving decisions. Format: section, date, decision/outcome.

## Phase 0

- **0.1 Identity & naming** — DONE (2026-07-18). App name: **Equavia**. Splash/landing page (index.html) stays. "Vitality" sub-brand name folded into Equavia everywhere it was user-visible (peak.html title, train.html's "Saved to your ___ dashboard" line); the internal `window.Vitality` JS bridge object name was deliberately left unchanged (invisible plumbing, ~15 call sites across 6 files, no user-facing benefit to renaming). Icon source for Phase 0.5: reuse `images/Index Landing Page.webp` (the red V mark already used as favicon). Every page's `<title>` now follows "{page} — Equavia" except the two home surfaces (index.html, dashboard.html), which are plain "Equavia". privacy.html also got a Google Calendar disclosure added to its third-party-services list (was missing despite the OAuth backend already existing).
- **0.4 Bottom navigation** — grouping decided: "Core 8 + More(3)". First-class tabs: Main, Health, Fitness, Peak, Finance, Planner, Ability, Notes. Folded into a More sheet: Train, Vitals, Brand.
- **0.6 currency** — re-basing finance.html's storage from CHF to GBP (not just the display default). No real finance data exists yet, so no migration/conversion logic needed — clean swap of the canonical unit.
- **0.7 dedup** — confirmed: Planner becomes sole owner of full editable Goals/Tasks/Projects/Checklists; Dashboard becomes compact read-only summary cards linking into Planner. Card order: Tasks → Goals → Health → Fitness → Finance.
- **Check-in cadence for the rest of this build**: bundle low-risk/cleanup sections and report once done; pause for explicit go-ahead before/after high-blast-radius changes (nav overhaul, the dashboard/planner dedup, anything touching finance.html's stored data) and at phase boundaries.
- **0.2 Remove template artifacts** — DONE (2026-07-18). health.html: removed the "editable template" attribution line (water tracker was already a proper iframe embed, nothing else to do there). peak.html: removed the Patreon "Setup ↗" link (`patreon.com/RowanTBK/membership`) and the two marketing-voice caption lines inside the "How this plugs in" diagram; kept the diagram itself (genuinely useful, not marketing). dashboard.html + planner.html: removed the "Gantt" tab and its stub from the Projects section (left the disabled/tooltipped Filter+Sort buttons alone — already honest about being unavailable). Found but did NOT touch: vitals.html and train.html both still show a `.starterTag` banner ("… starter — the value is what you build · the videos show how") — same leftover-template flavor as the Patreon link, worth a follow-up cleanup pass but out of scope for what was asked.
- **0.3 Empty states** — audited, no changes needed. Already extensively implemented app-wide (finance/dashboard/planner/ability/gym/train/notes/brand all have proper contextual empty states). Note for future-me: don't assume a gap exists from a partial function read — nearly added a duplicate empty-state handler to health.html's supplement tracker before finding the real one further down in the same function.
- **0.4 Bottom navigation** — DONE (2026-07-18). topbar.js rewritten as the single source of truth for nav: one `PAGES` array drives both a desktop top row (all 11 pages, in-flow, replaces the old per-page `cat-tabs`) and a mobile fixed bottom bar (8 core pages in a horizontally-scrollable strip + a pinned "More" tab opening a sheet for Train/Vitals/Brand), toggled via a 768px media query. Deleted the duplicated `cat-tabs` HTML/CSS and the "remove the shared bottombar" inline script from all 9 pages that had them (ability, dashboard, gym, finance, health, peak, vitals, train, planner); notes.html and brand.html needed no changes since they already deferred to topbar.js. train.html's embedded-mode `.starterTag` cleanup logic was preserved (unrelated to nav, still needed when train.html is iframed inside gym.html's Fitness tab).
  - Bug caught during verification: the mobile bottom bar's pinned "More" tab wasn't visible at narrow widths — root cause was `.bottombar-scroll` (flex/grid item with `overflow-x:auto`) not shrinking below its content's natural width, pushing the pinned sibling out. Fixed by switching `.bottombar` to `display:grid; grid-template-columns: minmax(0,1fr) auto` with explicit `min-width:0; overflow-y:hidden` on the scroll region. Verified via `getBoundingClientRect()`/computed-style dump, not just eyeballing screenshots — headless Edge's `--window-size` flag doesn't reliably produce the exact requested viewport on this machine (requesting 390px actually rendered ~490-520px), which was the real source of confusion before the computed-style check clarified it.
  - Discovered while testing: notes.html (and presumably train/vitals/brand/peak) fetch live from the real production Supabase backend on load, even under automated headless testing — there's no test/staging separation. Testing was read-only (page loads + one click on an isolated scratch page, no form submissions), so nothing was written, but worth knowing before any future automated testing touches these pages more actively.
  - No project skill existed for running/screenshotting this app; verification used headless Edge directly (`msedge.exe --headless=new --screenshot`) since Python/Node/chromium-cli weren't available in this environment. Worth `/run-skill-generator` if this becomes a recurring need.
- **starterTag cleanup** — DONE (2026-07-18). vitals.html, train.html, and brand.html (found while cleaning the first two) all had a leftover `"… starter — the value is what you build · the videos show how"` banner plus its CSS, same tutorial-template origin as the Patreon link removed from peak.html in 0.2. Removed the banner + CSS from all three; also removed train.html's now-pointless embedded-mode script that only existed to hide that banner when iframed inside gym.html.
- **0.5 PWA** — DONE (2026-07-18). Icon source: `images/Index Landing Page.webp` (the red V mark), cropped/padded into `images/icon-{192,512}.png` (edge-to-edge "any" purpose) and `images/icon-{192,512}-maskable.png` (62% safe-zone "maskable" purpose) plus `images/apple-touch-icon.png` (180x180). Generated via headless Edge rendering an HTML crop of the source webp to canvas-sized screenshots (GDI+/System.Drawing can't decode webp directly — confirmed by testing), with 192px versions downscaled from the verified-good 512px renders after headless screenshot mode turned out to distort/mis-crop at small requested window sizes. `manifest.json` added at repo root: name/short_name "Equavia", `start_url: index.html` (consistent with keeping the splash page), `display: standalone`, `background_color`/`theme_color: #050506`, all 4 icons registered. `sw.js` added: cache-first for all same-origin GET requests, precaches the full app shell (14 HTML pages + 5 shared JS files + manifest + 7 images) on install, cross-origin requests (Supabase CDN, WHOOP/Google/Anthropic APIs) pass through untouched. Registration centralized in topbar.js (`registerServiceWorker()`, skipped when embedded in an iframe) rather than duplicated per-page. All 14 pages got `<link rel="manifest">`, `<meta name="apple-mobile-web-app-capable">`, `<meta name="apple-mobile-web-app-title">`, and their `apple-touch-icon` href switched from the raw webp to the new PNG (3 pages — finance/planner/ability — had no icon links at all before this and needed them added fresh, not just updated).
  - Verified: manifest.json parses and every icon path resolves; every one of sw.js's 27 precache URLs returns 200 from a real local server; service worker registration succeeds against `http://localhost` (secure-context requirement — file:// can't register a service worker at all, confirmed). Could NOT fully verify activation/precache-completion in headless Chrome — `navigator.serviceWorker.ready` never resolved even at a 25s virtual-time budget, which looks like a headless/virtual-time tooling limitation (SW lifecycle isn't a JS-timer-driven thing virtual time can fast-forward) rather than an app bug, given registration itself succeeds and every precached URL is independently confirmed valid. Worth a real-device check after deploy: open the app, check Application > Service Workers in devtools, confirm it shows "activated and running" and the cache has ~27 entries.
- **0.6 Global backup & settings page** — DONE (2026-07-18). New `settings.html`, added to topbar.js's PAGES array in the More sheet (not a core tab).
  - **Backup**: Export All collects every localStorage key into one JSON file (`{app, exportedAt, data}`), downloads client-side via Blob, stamps `eq.lastBackupAt`. Import All parses the file, confirms via `confirm()` (shows the backup's date + key count, warns it replaces everything), then `localStorage.clear()` + replays every key, reloads the page. Dashboard shows an amber nudge banner ("You have never backed up your data" / "Last backed up N days ago") when backup is missing or >14 days old, linking to Settings — only renders past the threshold, hidden otherwise.
  - **Currency**: re-based finance.html's storage from CHF to GBP as the canonical unit, not just the display default (matches what was actually approved — no real data existed to migrate, so this was a clean swap, no conversion logic needed). This touched ~60 spots: renamed internal identifiers (`amountCHF` to `amountGBP`, `curCHF`, `netCHF`, `deltaCHF`, `grandCHF`, `annualSubsCHF`, `nwGrandCHF()`, `bgtSumCHF()` — all via exact-match `replace_all`, verified clean with a post-rename grep), flipped the exchange-rate fetch to anchor on GBP (`open.er-api.com/v6/latest/GBP`) with CHF now just another fetched rate, updated every fallback default and reordered every currency `<select>` so GBP is first (=default-selected), and updated stale comments. CHF/USD/EUR remain fully selectable, just no longer the base. Verified via screenshot — "GBP 0" and GBP-selected everywhere, no console errors.
  - **Obsidian consolidation — this was a real bug, not just duplication**: planner.html's settings-modal Save button called `ObsidianSync.setConfig(readObsidianSettingsForm())` with NO merge against the existing stored config, and its form doesn't include `folder` — so saving from Planner silently reset Notes' folder setting back to the default. notes.html's Save had the mirror-image bug against `vaultName`/`projectsFolder`. (Planner's own "Test connection" handler did merge correctly — Save didn't; inconsistent even within the one file.) Fixed by moving the full 8-field form to settings.html as the only place that edits this config, always reading/writing the complete object. planner.html and notes.html's gear icons are now plain links to settings.html; their duplicated modals, and the now-dead JS behind them, are gone. Caught and fixed one collateral bug while removing notes.html's modal: an Escape-key handler unconditionally read `$('setModalBg').classList` — with the modal gone that would throw on every Escape press.
  - **Wake/bedtime**: added to Settings (reads/writes the same `day_ring_times_v1` key dashboard.html's own modal already used) — additive, dashboard's own editor is untouched, both stay in sync since they share the key.
  - **Units**: gym.html (kg/lb) and po-water.html (kg/lb, separate from its own oz/ml water-volume unit) had two independent, already-diverging weight-unit toggles, each baked into that page's own state blob rather than a standalone key — full unification would mean restructuring both pages' state persistence, more invasive than this ask justified. Went with a low-risk middle ground instead: a new shared `eq.units.weight` key, set from Settings, that both pages now check as a fallback only when they have no saved choice of their own yet (`s.units = s.units || sharedWeightUnit() || CONFIG.units || 'kg'`) — sets the default for new setups without touching any existing per-page choice.
  - Verified via a real local HTTP server (headless file:// can't register service workers or exercise some of this cleanly) — scripted every settings.html control end-to-end (currency change, units click, wake/bed save, Obsidian save) and confirmed each wrote the exact right value with no exceptions; screenshotted dashboard/planner/notes/settings afterward to confirm no visual or console regressions. Did not get a clean automated read on the literal file-download click (headless Chrome's download handling hung without CDP-level configuration) — the download-triggering code is standard Blob+`<a download>`, and the data-assembly logic underneath it was verified directly instead.
- **0.7 Dashboard/Planner deduplication** — DONE (2026-07-18). Turned out bigger than the master prompt described: 5 sections were fully duplicated with dashboard.html and planner.html both reading/writing the exact same storage keys (Tasks/`goals:` prefix, Goals/`longterm_goals_v1`, Calendar/`local_cal_events_v1`, Checklists/`checklist:items:`+`checklist:done:`, Projects/`projects_v1`) — not just Goals+Tasks. Day Ring and Goal Ticker were the only dashboard-exclusive pieces and are untouched.
  - Dashboard now shows 6 compact read-only summary cards in a single stacked column (`.dash-summary-stack`), each linking to the page that owns it: **Tasks → Calendar → Goals → Health → Fitness → Finance** (Calendar inserted right after Tasks — both are "what's happening today" — since the 6th-card decision came after the original 5-card order was already confirmed; flag if a different position is wanted). Removed entirely from dashboard.html: the editable Goals card, the editable To-Do card (checkbox/drag/inline-edit/Polish), the full Projects kanban, the full Calendar (day/week/month grids + add/edit event modal), and the sidebar Checklists + Quick Note/Shopping links. Planner already owned equivalent full editable versions of all of these — confirmed byte-for-byte via screenshot that Planner's Tasks/Goals/Calendar sections still work standalone and show the same underlying data (a real goal titled "grind" rendered identically on both pages before the cut).
  - Google Calendar's OAuth token handback lands on `dashboard.html#gcal_access=...` (confirmed from earlier exploration) — kept all of the GCal token machinery (`processGcalHashTokens`, `ensureValidGcalToken`, `fetchGcalEvents`, etc.) intact and still called at boot; only removed the UI-rendering half (grids, the connect/disconnect button handlers, the event modal). Losing this by accident was the single biggest risk in this section and was checked explicitly before cutting anything.
  - Health's recovery score and Fitness's weight/session data aren't cached anywhere simple enough for the dashboard to read directly (WHOOP recovery is fetched live and never stored; gym.html's weight lives in its own array but "last session" is buried in a `state.logs` object keyed by exercise ID). Added a `publishFitnessSummary()` in gym.html (writes `eq.fitness.summary` on weight save, on set log, and at boot) and a summary write in health.html's WHOOP fetch success path (writes `eq.health.summary`). Finance reads its `nw:bank/stocks/crypto/other` and `budget:income/expenses` keys directly — already simple, stable arrays, no publishing step needed.
  - Removed ~450 lines of now-dead JS (row-building, renderers, Add/Polish/AI-polish handlers, the Projects kanban, the Checklists sidebar, calendar grid rendering) after mapping every remaining reference with grep to confirm nothing else called them. Left the now-unused `dgt-*`/`proj-*`/`cal-*`/`chk-*` CSS in place (~150 lines) — genuinely inert, and removing it added risk for zero functional benefit given how much else this section already touched. Worth a dedicated cleanup pass later if the file size matters.
  - **Bug found and fixed during verification, not before**: `publishFitnessSummary()` worked correctly when called from gym.html's own boot sequence, but gym.html pulls weight data from its own separate inline Supabase sync (a different, pre-existing implementation from `sync.js` — worth knowing about, not something this session touched) which resolves *after* boot and reloads `wtEntries` from a `pcRerender()` function without re-publishing the summary. First test run showed the Fitness card stuck on "Log your first session..." despite gym.html clearly showing real logged weight (81.0kg) — caught by testing with a persistent browser profile across both pages rather than trusting a single-page screenshot. Fixed by adding the publish call inside `pcRerender()` too. Re-verified after the fix: Fitness card correctly showed "81kg · 80.3kg 7d avg · 1d since last session".
  - Verified end-to-end with a real local server and a persistent shared browser profile across page loads (necessary specifically to catch the timing bug above — a fresh profile per test would have hidden it). Confirmed zero JS errors via an injected `window.addEventListener('error', ...)` in the actual page context, all 6 cards computing correct text (both empty-state and real-data cases), Planner unaffected, desktop and mobile layouts both clean.

## Phase 1

- **Phase 1 questions (Rounds 1-4)** — answered before building. Book covers: off. Reading progress %: no. Contact tiers: Inner circle / Close / Network, with auto-suggested cadence (7/14/30 days). Birthday field: yes. Gmail: 10 messages, unread-only, manual refresh only, `gmail.readonly` scope only, one account. Calendar quick-add: primary calendar, two-way sync, natural-ish parsing. Goal archive: `Archive/<goal title>` Obsidian folder naming, un-archiving is permanent.
- **1.1 Book Tracker (ability.html)** — DONE (2026-07-18). Three shelves (Want to Read / Reading / Read) backed by a new `books_v1` key, array of `{id, title, author, status, rating, finishedAt, notes}`. Status dropdown per book drives shelf placement; switching to "read" reveals a 5-star rating control (click same star again to clear to 0) and records `finishedAt`; a year-stat line above the shelves counts books finished in the current calendar year. No cover art, no progress %, per the answered questions. Follows the exact same accessor/row-builder/render pattern already used by Habit/Skill trackers on the same page (`getX()`/`setX()`, `buildXRow()`, `renderX()`, wired into the existing boot sequence and `storage` listener). Verified via screenshot plus an iframe-injected interaction test (add → shelf placement → status change → star rating → finished date → year count all correct, zero console errors).
- **1.3 Relationship Tracker (planner.html) + dashboard badge** — DONE (2026-07-18). New card section on planner.html backed by `contacts_v1`, array of `{id, name, type, tier, cadenceDays, lastContactedAt, birthday, notes, createdAt}`. Tier select (Inner circle/Close/Network) auto-fills the cadence-days input (7/14/30) on change, still freely editable. "Log contact" button pairs with a date input defaulting to today (supports backdating for catch-up entries). Overdue = `lastContactedAt` set and days-since > cadence; list sorts most-overdue first, with never-logged contacts sorted to the very top (they need attention but aren't "overdue" in the strict sense, so they're excluded from the overdue *count* — see dashboard badge below). Birthday stored as `MM-DD` (year-agnostic); a 🎂 tag appears when the birthday falls within the next 14 days. Free-text notes field per contact, saved on blur/change.
  - Dashboard: `dsumTasksBody` (Tasks summary card) now appends "· N person/people overdue" when `overdueCount > 0`, reading `contacts_v1` directly (same direct-read pattern already used for Finance's net-worth/budget keys — a simple stable array doesn't need a publish step). Singular/plural handled explicitly, matching the existing precedent in the same function (`event`/`events`).
  - **Bug caught and fixed during verification**: the "Log contact" button anchored "today" to noon local time regardless of actual time of day. Logging a contact before noon produced a `lastContactedAt` timestamp technically in the future, so `daysSince()` floored a small negative number to `-1`, briefly showing "-1 / 7d" instead of "0 / 7d" (cosmetic only — never flipped the overdue flag, self-corrected by afternoon). Fixed by using the real current timestamp when the picked date is today, and only noon-anchoring for genuine backdating.
  - **Testing-methodology trap hit and resolved, not an app bug**: after fixing the dashboard badge's pluralization, a direct (non-iframe) navigation to planner.html earlier in the same headless test profile had already registered and activated the service worker, which then cache-first-served a stale precached copy of dashboard.html — including to subsequent *iframed* loads in that same profile, since an active SW controls all same-origin clients in scope, not just top-level navigations. Cache-busting the request URL didn't help (the precache entry is keyed to the bare `dashboard.html` filename; a differently-queried URL is a separate miss that should hit network, but dump-dom's async timing made an early read look stale too, muddying the signal). Resolved by re-testing in a brand-new profile that only ever loads app pages inside iframes (`registerServiceWorker()` already skips iframe contexts, per 0.5) — confirmed clean: correct singular "1 person overdue", zero JS errors, `navigator.serviceWorker.controller` null as expected. Lesson for future verification: once any profile has done a direct top-level navigation to a shell page, treat every subsequent load of edited pages in that same profile as potentially cache-shadowed — start a fresh profile (or iframe-only) rather than trusting a re-fetch.
  - Verified geometry directly via `getBoundingClientRect()`/computed styles at a real 390px CSS width (iframe-driven, immune to the previously-documented `--window-size`-vs-actual-viewport mismatch) — confirmed zero horizontal overflow anywhere in the add-row or per-contact rows; a same-size direct-navigation screenshot had looked like it was clipping text at the right edge, which the geometry check proved was a screenshot/viewport artifact, not a real layout bug. Worth remembering: don't trust a small-viewport screenshot's apparent overflow without a geometry check to confirm it, in either direction.
- **1.2 Sleep Log + Trend Chart (health.html)** — DONE (2026-07-18). Answered first: WHOOP OAuth is real/already wired (confirmed from earlier exploration, not re-asked); sleep target defaults to **7h**; chart includes a **7-night rolling average** line. New card between the WHOOP card and Daily Stack, backed by `sleep_log_v1` (array of `{dateKey, hours, source:'auto'|'manual'}`) plus a `sleep_target_hours_v1` key for the reference line (editable inline in the card header, no Settings-page wiring needed for this — "configurable" just meant the code shouldn't hardcode it).
  - Chart is gym.html's weight-tracker SVG pattern reused as-is (same `viewBox="0 0 320 130"`, same quadratic-smoothing path builder, same area-fill/glow-filter/dot structure), re-themed with health.html's own `--accent`/`--warning` tokens instead of gym's `--good`. Added on top of the base pattern: a dashed horizontal target-reference line (`--warning`-colored, distinct from both the data line and the plain grid lines), and a 30/90-day range toggle (gym's own chart hardcodes 30 with no toggle — this section needed one per spec, so it's new rather than copied).
  - Auto-fill hooks into the existing WHOOP fetch success path via a small `window.slAutoFillFromWhoop(sleepRecords)` function — called with the *entire* returned sleep array (up to `TREND_LOOKBACK_DAYS`=7 records), not just last night's, so a lapsed visit still backfills the gap on next load. Manual entries always win on conflict (an explicit override); auto entries only ever overwrite a previous *auto* entry for that night. Manual vs. auto nights are flagged visually as required — filled dots for WHOOP-sourced nights, hollow ring dots for manual — with a legend spelling both out plus the avg-line and target-line keys.
  - Works fully in manual-only mode with no WHOOP connection at all (the add-row and chart don't gate on connection state, matching "manual entry/override always available").
  - Verified via an iframe-injected test: hook presence, empty-state toggle, 5 manual nights logged correctly (chart/legend/meta all reflect them), a synthetic auto-fill call correctly left a manual night untouched while adding a new auto-only night, a second synthetic auto-fill call correctly overwrote that auto night's value, range-toggle and target-input both update the chart live — all assertions passed, zero console errors. Screenshot confirmed visual match with the WHOOP/Daily Stack cards above and below it. Mobile geometry check at a real 390px CSS width (iframe-driven) confirmed zero horizontal overflow in the card, chart, legend, or add-row.
  - **Process note, not a bug**: partway through this section's testing, a direct-navigation screenshot came back showing the app's splash screen instead of health.html — landing-guard.js's temporary test bypass (added, then correctly reverted at the end of 1.3's verification per the established discipline) simply hadn't been re-enabled yet for this new round of testing. Re-enabled it, re-ran, reverted again after — confirms the revert-every-time discipline is working as intended, not a gap in it.
- **1.4 Notification Hub — Gmail, read-only (dashboard.html)** — DONE (2026-07-18). Answers already on file from Phase 1 Round 2: 10 messages, unread-only, manual refresh only (no polling interval), `gmail.readonly` scope only, one account.
  - **Implementation is deliberately separate from Calendar's OAuth**, per the master prompt: Calendar uses a server-side auth-code exchange (`api/google-calendar-callback.js`, refresh tokens persisted in `gcal_tokens_v1`); Gmail uses Google Identity Services' client-side **token flow** (`google.accounts.oauth2.initTokenClient`, loaded via `<script src="https://accounts.google.com/gsi/client">`), no server involved, no PKCE code exchange to write. The access token lives only in a plain JS variable — **never** written to localStorage or sessionStorage — with a single boolean flag (`eq.gmail.everGranted`) persisted just so the page knows whether to attempt silent re-auth (`requestAccessToken({prompt:''})`) on load versus showing an explicit "Connect Gmail" button. A 401 mid-session triggers exactly one silent-reauth retry before falling back to "reconnect."
  - **Reused the existing `GOOGLE_CAL_CLIENT_ID`** rather than provisioning a second OAuth client — GIS supports requesting a different, smaller scope from the same client ID, and this is Google's own recommended incremental-auth pattern. **Heads up for whoever deploys this**: this requires the Gmail API to be enabled for that same Google Cloud project (Calendar's API being enabled doesn't imply Gmail's is) and, if the OAuth consent screen is still in Testing publish status, `gmail.readonly` needs to be added to its scope list. If either is missing, connecting will fail with a visible error in the panel rather than misbehaving silently — but it can't be verified from code alone, only from the actual Google Cloud Console for this project, which is outside what I can check. Worth a real-account spot-check post-deploy.
  - Panel sits between the Day Ring and the Tasks summary card (new "Inbox" section) — placed near the top since the whole point is glanceability, not buried under the 6 summary cards. Shows sender/subject/snippet/time for up to 10 unread messages, with an unread-count badge using Gmail's `resultSizeEstimate` (can exceed 10, since that reflects the true unread count, not just what's shown). Tapping a row lazily fetches `format=full` for just that message, walks the MIME `payload.parts` tree for a `text/plain` part first, falling back to `text/html` stripped to text via `DOMParser` (parsed into a detached, non-rendered document, so nothing in the email can execute) — decoded from Gmail's base64url. "Open in Gmail ↗" (external escape hatch, per spec) and, when the sender's display name exact-matches a Relationship Tracker contact, a "tracked contact" tag plus a "Log contact" button that writes `lastContactedAt` straight into `contacts_v1`.
  - Cross-link match is by **display name only** (no email field on the contacts model, and adding one wasn't asked for) — exact, case-insensitive match against `contact.name`. Good enough for the "small but high value" bar the spec set; noted here rather than silently expanding the Relationship Tracker's schema after the fact.
  - Verified with a mocked `google.accounts.oauth2` + mocked `fetch` (real Gmail OAuth can't be exercised headless — no real account/consent screen available in this environment): connect flow, badge count, both message rows rendering with correct sender/subject/snippet, the tracked-contact tag appearing on the matching sender and not the other, expand-to-load correctly decoding a real base64url-encoded plain-text body, the Log Contact button correctly writing `contacts_v1`, and disconnect correctly clearing state and the everGranted flag — all passed, zero console errors. Real GIS script load was blocked via `--host-resolver-rules` during testing so the mock couldn't be raced/clobbered by the genuine library arriving mid-test. Screenshot confirmed visual match with the rest of the dashboard; mobile geometry check at a real 390px width confirmed no overflow, including with a long sender name (truncates via ellipsis as intended).
  - **Not verifiable from this environment, flagged for a real-device check**: the actual Google consent screen UX, real silent-reauth behavior across a genuine browser session, and whether the reused client ID's project needs the Gmail-API/scope setup mentioned above.
- **1.5 Calendar Quick-Add → Google Calendar (planner.html)** — DONE (2026-07-18). Answers already on file from Phase 1 Round 3: primary calendar, single account; two-way sync; natural-ish parsing.
  - **Architecture call not covered by the ASK FIRST list**: the master prompt said to create events "via the same Google OAuth session as 1.4" but also said this should make "the 'Connect Google Calendar' button that already exists" real — those two point at different things. The existing button (planner.html, `#calConnectBtn`) is a fully-working **server-side** auth-code flow (`api/google-calendar-callback.js` + refresh tokens in `gcal_tokens_v1`) that already reads events; 1.4's Gmail flow is a separate client-side GIS token, and mixing that pattern into Calendar would mean two parallel "connected" states for the same feature. Went with expanding the *existing, working* Calendar OAuth instead: changed `GOOGLE_CAL_SCOPE` from `calendar.readonly` to `calendar.events` (read+write on events, not full calendar management) in both planner.html (the real owner of the connect flow) and dashboard.html (kept in sync for its own read-only summary card, though it no longer has a connect button of its own post-0.7, so this was dead-code hygiene more than a functional change). No real user has connected yet, so there's no re-consent migration to worry about.
  - New `createGcalEvent()` alongside the existing `fetchGcalEvents()`, same `ensureValidGcalToken()` underneath — one POST to `calendars/primary/events`.
  - Quick-add is a single natural-language input (no separate structured-field pickers, since the user picked natural-ish parsing as the *only* v1 input mode) with a live preview line underneath showing the parsed result before saving, so a misparse is visible rather than silent. Recognizes one date token (today/tomorrow/a weekday name — next occurrence, treating "monday" said on a Monday as next Monday, not today/skip/invalid — or `M/D`), one time token (`6am`, `6:30pm`, or 24h `14:00`), and an optional `for Xh`/`for Xm` duration; whatever text is left becomes the title. Falls back to sane defaults (today, next half-hour slot from now, 30 min) rather than blocking when a piece can't be found — the preview line visually flags whenever a fallback was used (amber, "(guessed...)").
  - Reused gym.html's chart pattern doesn't apply here (no chart in this section) — followed the existing `.contact-add-row`/`.cal-status` visual patterns already in this same file instead, for consistency within planner.html itself.
  - **Two real bugs caught by testing, not code review**: (1) the success status message ("Added 'gym' to Google Calendar.") was being set *before* calling `loadCalendar()` to refresh the grid — but `loadCalendar()` writes its own "Loading…" then blank status to the same `#calStatus` element, so the confirmation was clobbered within the same tick and would never actually be seen. Fixed by awaiting `loadCalendar()` first and setting the confirmation after, so it's the last write. (2) The duration regex (`for (\d+)h`) had both its hour and minute capture groups fully optional, so on input like *"...title for testing tomorrow 6am for 1h"* it matched the **first** "for" (in "for testing") as a zero-width empty match and gave up, silently leaving the real "for 1h" stuck in the title and defaulting to 30 minutes. Fixed with a lookahead requiring a digit immediately after "for " before attempting the match, so the regex engine correctly skips past non-numeric "for" occurrences to find the real one.
  - Verified via mocked `ensureValidGcalToken`/`fetch` (real Google consent isn't exercisable headless, same limitation as 1.4): 4 parser cases (relative day + am/pm, weekday + "for Xh", 24h time, full-fallback with no date/time at all) all produced correct title/date/time/duration; empty input hides the preview; clicking Add with no connection shows an inline "Connect Google Calendar first." and makes no API call; with a token present, POST body's `summary`/`start`/`end` (correct UTC conversion from local wall-clock time, confirmed against the test machine's `Europe/London` zone) all correct, success message persists post-refresh, input clears; a failed POST (500) surfaces inline on the preview line rather than failing silently. Zero console errors. Mobile geometry check at 390px confirmed no overflow, including with a long title that forced the duration-regex bug to surface in the first place — worth remembering that a "test the pretty case" mobile check with a short string wouldn't have caught it; the longer, messier test string did.

- **1.6 Color-Coded Live Goal Countdown (planner.html)** — DONE (2026-07-18). The one ASK FIRST item (confirm pulse-instead-of-blink) is already resolved by the master prompt's own text prescribing it, not left open — built as specified rather than re-asking. Went with **smooth HSL interpolation** over discrete steps (the prompt invited either): costs nothing extra once `percent_remaining` is computed, and reads as a continuous "heating up" gradient rather than jumpy bands against the dark theme. Discrete steps would be a small follow-up if preferred instead.
  - **Data-model gap the master prompt assumed away**: the formula `percent_remaining = (deadline − now) / (deadline − start_time) × 100` needs a goal *start time*, but the existing goal object (`longterm_goals_v1`) never stored one — only `deadline`. Added `createdAt` when new goals are created going forward. Existing goals from before this feature (there's a real one, "grind") have no `createdAt` and can't have a fabricated one guessed backward without misleading the user about their own history, so they render **neutral until the deadline actually passes**, then jump straight to critical — a safe, honest fallback rather than invented precision.
  - Color stops (percent_remaining, descending): 100–76 flat neutral (`--text-secondary`, no gradient) → 75 dark green → 50 green → 40 light green → 35 lighter green → 30 yellow → 20 orange → 10 red → 5–0 dark red, flat (not interpolated further) + pulse. The 75/76 boundary is a hard jump per the spec's own listing, not a fade. **Exception**: a goal whose progress slider reads 100% is always neutral regardless of date math — checked first, before the gradient even runs — and updates live as the slider is dragged (on `input`, not just on committed `change`), not just on the once-a-second tick.
  - Pulse is a 1s ease-in-out opacity animation (1 → 0.55 → 1), gated behind `@media (prefers-reduced-motion: no-preference)` so a reduced-motion user gets the same dark-red color with zero animation, per spec.
  - Color is applied via inline `style.color` (not a fixed set of CSS classes) since it's a continuous interpolated value, computed in one shared function (`applyGoalCountdownColor`) called from three places that all needed to agree: initial card render, the existing once-a-second countdown tick, and the slider's live `input` handler.
  - **Real testing-methodology trap, not an app bug**: `longterm_goals_v1` is a cloud-synced key (`initCloudSync`'s `syncedKeys`). Seeding test goals directly into localStorage and reloading the iframe kept silently losing them — Supabase was pulling the one real live goal ("grind") over the top within the same page load, before the test's read landed. Not obvious at first since earlier sections' test keys (`contacts_v1`, `books_v1`, `sleep_log_v1`, etc.) all happen to be un-synced, so this hadn't come up yet. Resolved the same way as 1.4's Google-domain block: `--host-resolver-rules="MAP ubhjdibldkknviezrjys.supabase.co 0.0.0.0"` for the duration of the test. Worth remembering for any future test that touches a cloud-synced key specifically (goals/projects/calendar/checklists, per the synced-keys list) — un-synced keys don't have this problem.
  - Verified numerically across all 8 named stops plus an interpolated midpoint (45%, confirmed to fall strictly between the 50% and 40% stops' RGB values in every channel — correct interpolation direction and ordering), the 100%-neutral exception (including live update on slider drag), and both legacy-goal fallback paths (no `createdAt`, future deadline → neutral; no `createdAt`, already overdue → critical). Zero console errors. Screenshot confirmed the full gradient reads cleanly against the dark theme, and made the 100%-exception visually unambiguous: a goal sitting at the same deadline as the "5% remaining, critical" goal directly above it renders fully neutral because its slider is at 100%.

- **1.7 Goal Archive at 100% → History + Obsidian folder (planner.html + obsidian-sync.js)** — DONE (2026-07-18). Two of three ASK FIRST answers were already on file (folder naming `Archive/<goal title>`, un-archiving permanent). The third ("project linked to two goals") turned out to be **moot, not skipped** — Projects only ever store a single `goalId` (not a list), so a project can never belong to two goals simultaneously; there's no reconciliation case to design for. Confirmed by reading the actual data model rather than assuming the prompt's premise held.
  - **Reused the existing Projects archive mechanism instead of building a parallel one.** Projects already had `archived: boolean`, a `.proj-history` collapsible section, and reversible Unarchive — discovered while investigating (flagged as a to-check item in an earlier session note). Goal-archiving now reuses that same `archived` flag on linked projects rather than inventing a second archive concept, but adds `archivedViaGoal: true` on those specific projects so their Projects-History row shows a static "Archived with goal" badge instead of the normal Unarchive button — the goal's permanence has to hold even though plain project-archiving is still deliberately reversible. Projects archived independently (manually, via their own `status: 'done'` → Archive button) are completely unaffected — verified as a regression check, not just assumed.
  - Goals themselves: a new `.dgt-goal-archive` button appears only at `value >= 100` (shows/hides live as the slider crosses the threshold, not just on next reload), gated behind `window.confirm()` naming the goal and how many linked projects will be swept in — matches the Settings page's existing precedent for irreversible actions. New `archivedAt` timestamp; archived goals disappear from the active list (and from dashboard.html's summary card, which needed its own `!g.archived` filter added) and reappear in a new read-only **Goal History** `<details>` section (title, formatted completion date, linked-project count) — built by directly reusing `.proj-history`/`.proj-history-summary`/`.proj-history-list` CSS rather than writing parallel styles for visually identical structure.
  - **Obsidian move, not copy**: added `moveProjectPageToFolder()` to `obsidian-sync.js` (exported alongside the existing `pushProjectPage`) since the Local REST API has no move/rename verb — implemented as GET the current content (preserving whatever the user actually wrote into the page, not re-generating `pushProjectPage`'s bare stub) → PUT to the new sanitized path → best-effort DELETE of the old one. Runs *after* the local archive already committed, one project at a time, so a slow or unreachable Obsidian instance can never leave a goal half-archived — failures are reported in the status line ("N failed to move (still archived locally)") rather than rolling anything back. Skipped entirely (no-op, not an error) for goals with no linked projects or when Obsidian isn't enabled.
  - **Real bug caught by testing with a deliberately messy title, not a clean one**: initially passed the destination as a pre-joined `'Archive/' + goal.title` string, sanitized by splitting on `/`. A goal titled *"Q3: Ship it!/Launch"* — chosen for the test specifically because it contains a literal slash — got misread as **three** path segments instead of two, silently creating an extra nested subfolder (`Archive/Q3- Ship it!/Launch/`) instead of sanitizing the embedded slash within one segment (`Archive/Q3- Ship it!-Launch/`). Fixed by changing `moveProjectPageToFolder`'s signature to take an array of already-distinct segments (`['Archive', goal.title]`) rather than a joined path string, so the caller's structural boundaries can never be confused with illegal characters inside the user's own text. Re-verified directly against the sanitizer (not through a mock that bypassed it) with two adversarial titles — one with an embedded slash+colon, one with a full spread of illegal characters — both now produce the correct flat two-level folder with the exact expected GET → PUT → DELETE call sequence.
  - Verified end-to-end with `ObsidianSync` swapped for a recording mock (real Local REST API isn't reachable from this environment) plus `window.confirm` auto-accepted: Archive button appears only at 100%; archiving a goal with no linked projects removes it from the active list and adds a correct History row; archiving a goal with two linked projects (one Obsidian-linked, one not) correctly archived both, called the move function only for the linked one, left an unrelated already-archived project's `archivedViaGoal` untouched, and left a fully unrelated active project untouched. Zero console errors. Screenshot confirmed the Archive button and expanded Goal History row render cleanly, reusing the established Projects-History visual language.

- **1.8 To-Do Completion Rate Chart (dashboard.html + planner.html)** — DONE (2026-07-18). Both ASK FIRST items were self-recommended in the master prompt's own text ("count as missed [...] recommended", "Propose: skip") rather than genuinely open, so built as proposed without a re-ask, consistent with how 1.6's pulse-substitution was handled.
  - **The real design problem wasn't the math, it was WHERE to capture it.** `rollover()` (planner.html) already permanently deletes every past day's `goals:YYYY-MM-DD` key after folding its undone tasks into today — that's pre-existing behavior, not something this section changed. That means a day's scheduled/completed counts are only ever available for the single moment just before that deletion; there's no other durable record to compute a snapshot from later. The snapshot is taken *inside* `rollover()`'s existing per-key loop, immediately before `storeDelete(k)`, into a new `todo_completion_snapshots_v1` array — the only new persistent key this section adds.
  - **"Pushed to tomorrow counts as missed" needed its own small fix**, not just a formula choice: the existing "Push remaining to tomorrow" button (`gmPushBtn`) already *removes* unchecked tasks from today's list immediately (moving them to tomorrow's key) — by the time rollover snapshots that day, those tasks would already be gone from the list, silently shrinking the scheduled denominator and making a day with pushed-away tasks look artificially like 100%. Fixed by adding a tiny `goals_pushed:YYYY-MM-DD` counter, incremented wherever the push button removes tasks, folded into `scheduled` (not `completed`) at snapshot time, then deleted — so pushed tasks are counted as missed for their original day without changing the push button's existing behavior at all.
  - Chart lives on dashboard.html directly under the Tasks summary card (a separate non-link card, not nested inside the Tasks `<a>`, since it needs its own clickable 4W/12W/52W toggle). Reuses gym.html's SVG chart pattern (viewBox, quadratic-smoothing path, area/glow) re-themed with `--success` instead of gym's `--good`. New on top of that base pattern: **zero-scheduled or unrecorded days are skipped, not plotted as 0% or 100%, and render as a literal break in the line** — built by grouping valid days into contiguous runs (by array position) and drawing each run as its own `<path>`, rather than one continuous path that would misleadingly connect across a gap. The 7-day rolling average is calendar-based (trailing 7 calendar days from each valid point, not 7 array-positions), broken at the same gaps as the raw line, since gaps mean those aren't the same thing here the way they are for gym/sleep's daily-contiguous data.
  - Verified in two layers. Data layer (planner.html): seeded two past days (one with an auto-rolled-forward undone task, one with 2 tasks manually pushed earlier the same day) and let a real boot run `rollover()` — both produced correct `{completed, scheduled}` snapshots (the pushed-away day correctly counted its 2 pushed tasks in the denominator), old `goals:` keys were deleted, the `goals_pushed:` counter was consumed/deleted, and today's own key was left untouched. Chart layer (dashboard.html): insufficient-data empty state; 18 valid days plus a deliberate 2-day gap correctly produced exactly 2 raw-line segments and 2 avg-line segments (not 1 continuous path) with 18 dots and a single area fill on the longest run; range toggle correctly switches the active button and re-renders. Zero console errors. Screenshot with a trending, noisy 28-day dataset (3-day gap included) showed the gap as a clean visual break and the 7-day average visibly smoothing the noisy raw line, sitting correctly under Tasks. Mobile geometry check at 390px confirmed no overflow.

This closes out Phase 1 (1.1–1.8) — see the section-by-section entries above.

## Phase 2

- **Phase 2 questions** — answered before building: default context domains ON = Goals/Tasks, Health, Fitness, Finance, Habits (Relationships defaults OFF, still toggleable per-message); conversations persist to localStorage (consistent with the rest of the app); default model Sonnet 5, no user-facing toggle. **Corrected one detail from the master prompt rather than following it literally**: it named `claude-sonnet-4-6` as the smarter-model option, which is not a real/current model ID (the prompt predates it) — used the real current ones (`claude-sonnet-5` / `claude-haiku-4-5-20251001`) instead, flagged to the user before building so the substitution wasn't silent.
- **"Ask Claude" page (ask.html) + backend (api/chat.js)** — DONE (2026-07-18).
  - **Nav placement wasn't asked about, and the master prompt didn't specify it** — put "Ask" in the mobile More sheet / desktop overflow (alongside Train/Vitals/Brand/Settings) rather than as a 9th core tab, since Phase 0.4 already deliberately fixed the core row at 8 tabs; adding a 9th would quietly re-litigate that earlier decision. Easy to promote to core later if it turns out to get used constantly.
  - **Backend follows the exact existing serverless-function conventions** (`api/whoop-refresh.js` was the template): CORS headers, OPTIONS→204, non-POST→405, `process.env.ANTHROPIC_API_KEY` read server-side only (never sent to or stored by the client), try/catch around the outbound fetch. Model and `max_tokens` (1024) are fixed server-side, not client-controlled, so a compromised or buggy client can't drive up spend or swap models. Message/context sizes are hard-capped (8000/6000 chars) before forwarding. Rate limiting is a simple in-memory sliding-window counter — explicitly best-effort, since a Vercel function instance isn't guaranteed to stay warm or be the only instance handling traffic; proportionate to "simple rate limit" for a single-user personal app guarding against a runaway client bug, not real abuse.
  - **Context assembly** builds one compact text block per enabled domain by reading localStorage directly (today's tasks + streak + longterm goal titles/%, `eq.health.summary`, `eq.fitness.summary`, net worth + budget from the same `nw:`/`budget:` keys Finance's own dashboard card reads, `habits_v1`/`skills_v1` name+streak/minutes, `contacts_v1` overdue names) — same direct-read pattern used throughout this build for simple stable arrays, no new publish step anywhere. Per-message domain chips (6, matching the spec's list) default to the answered set; toggling persists across sessions but every message re-reads live data, so context is always current, never stale from when a chip was first turned on. An estimated token count (`~length/4`, a standard rough heuristic) is shown next to the chips so cost stays visible before sending, per spec.
  - Chat UI: user/assistant bubbles, persisted message history, a typing indicator (respects `prefers-reduced-motion`, same convention as 1.6's pulse), Enter-to-send / Shift+Enter-for-newline, auto-growing textarea, a failed request renders as its own distinctly-styled error bubble inline in the conversation (not a toast) so the failure stays in context, and a "Clear conversation" action gated behind `confirm()`.
  - **Updated `privacy.html`'s existing Anthropic disclosure** rather than adding a separate one — it already covered the pre-existing "✨ Polish" goal-rewording feature but not this page's much larger data flow (full domain summaries, not just one goal's text). Expanded the same bullet to cover both, naming exactly which domains can be sent and making clear the toggle is per-message/opt-in, not a standing grant.
  - Added `ask.html` (and, incidentally, the already-existing but previously-uncovered `settings.html`) to `sw.js`'s precache list, bumping `CACHE_NAME` to `v2` per that file's own documented convention for shell-list changes.
  - Verified with a mocked `/api/chat` (a real call needs a deployed `ANTHROPIC_API_KEY`, not available in this environment — flagged below): domain chip defaults matched the answered set exactly; toggling a chip persisted to localStorage and changed both the visible token estimate and the actual `systemContext` sent on the next message (confirmed present when on, confirmed the other domains' sections were correctly formatted); a real send produced correct request shape (`messages` + `systemContext`) and rendered both bubbles with correct styling and persistence; a failed request rendered as an error bubble with the server's actual error text; Shift+Enter correctly did not send; Clear conversation correctly reset state and restored the empty view. Zero console errors. Screenshot confirmed bubble styling, chip states, and the input bar match the rest of the app. Mobile geometry check at 390px (including deliberately long messages meant to stress bubble max-width) confirmed no overflow.
  - **Not verifiable from this environment, flagged for a real check post-deploy**: the actual Anthropic API call end-to-end (model availability/response shape, real latency), and — this is the one actionable item — **`ANTHROPIC_API_KEY` still needs to be added in Vercel's project environment variables**; the page will show a clear inline error ("server not configured") rather than failing silently if it's missing, but nothing will send until it's set.

## Phase 3

- **3.1 Year Grid per habit (ability.html)** — DONE (2026-07-18). No ASK FIRST items for this section, built as specified.
  - **Rolling 365-day window ending today, not calendar-year-to-date** — a judgment call the spec didn't quite pin down ("whole-year grid (365 cells)" / "success % for the year"). A fixed Jan 1–Dec 31 view would render almost empty every January and reset the visible history arbitrarily; a rolling year (same convention as GitHub's contribution graph) stays consistently useful year-round.
  - **Same data-model gap as goals in 1.6, same fix**: habits had no `createdAt`, which the "grey = not yet" state needs to distinguish "before this habit existed" from "existed but missed." Added `createdAt` to newly-created habits; existing habits infer a start boundary from their earliest history entry, falling back to today (all-grey) if they have no history at all — same reasoning as 1.6, not a new pattern.
  - Today is always grey regardless of done state (the day isn't over), matching the existing 30-day toggle grid's own convention for `.is-today` — kept consistent rather than introducing a different rule for the new grid.
  - Compact preview (tiny 3px cells, non-interactive, click-to-expand) and a full modal (11px cells, legend, horizontal-scrolls on narrow screens) show the *same* 365 cells at different sizes, both computed by one shared `habitYearCells()` function so the two views can never disagree.
  - **Caught by screenshot, not by the numeric test**: "done" was initially styled with `--accent` (the app's warm orange) instead of `--success` (green) — the spec explicitly says "green = done, red = missed," and orange-vs-red (`--accent` `#E07658` vs `--danger`-derived `#FF6B6B`) turned out to be genuinely hard to tell apart at a glance, working against the whole point of the feature. The interaction test's cell-count assertions couldn't have caught this since they only check class names, not perceptual color distance — this needed an actual look at the rendered image. Fixed by switching to `--success`; re-screenshotted to confirm green/red now reads instantly.
  - Verified the state-classification math by hand against two constructed habits (one with an exact 20-day-old `createdAt`, one legacy habit with no `createdAt` inferring a 10-day-old start from its earliest history entry) — done/missed/grey counts and the resulting percentage matched exact hand-calculated expectations in both cases (25% and 20%), including the off-by-one edge case of the start-boundary day itself counting as "in range." Modal open/close (backdrop click and × button) and the today-cell-is-always-grey rule both confirmed. Zero console errors. Mobile geometry check at 390px confirmed no page-level overflow, with the modal's internal grid correctly using its own contained horizontal scroll rather than breaking the layout.

- **3.2 Habit Health Score with decay (ability.html)** — DONE (2026-07-18). The ASK FIRST item ("keep the streak too, or make the score primary?") is answered by the section's own body text one line above it ("Alongside (not replacing) the streak") — built as the body text already specified rather than re-asking a question the spec had already answered itself, same pattern as 1.6/1.8.
  - `score = previous×k + done×(1−k)` exactly as specified, with `k=0.95` — chosen for roughly a 2-week half-life under sustained misses (`k^14 ≈ 0.49`), matching "decays gradually instead of zeroing it." Loop Habit Tracker derives `k` from each habit's configured frequency (e.g., 3×/week vs daily); this app's habits have no frequency field at all — just a daily done/not-done toggle — so introducing one purely to feed this formula would be scope creep nothing else asked for. Used one fixed `k` for all habits instead.
  - New habits start from a neutral seed (50) with no history; the exponential decay means the seed's influence fades within a few weeks regardless of its starting value, so 50 isn't a "wrong first guess" that needs correcting later.
  - Today only factors into the score once actually marked done — an unmarked "today" doesn't get a decay step applied, same "the day isn't over yet" rule as 3.1's year grid, kept consistent rather than inventing a different treatment of "today" for this section.
  - Small ring (not a bar — ring fit ability.html's existing recovery-ring visual language better, given the app already uses this exact ring pattern on the WHOOP recovery ring and the dashboard day-ring) rendered next to the streak badge, color-banded green/amber/red at the 70/40 thresholds so the score reads at a glance without needing to parse the number.
  - Verified against hand-calculated expected values for four constructed habits: 30 days old with every day (including today) marked done → score 90 (hand estimate ~89, within expected rounding of a log-approximation done by hand); 30 days old with nothing ever marked → score 11, exact match; a brand-new habit with no history → score 50 exactly; and the key qualitative case, 60 days old with exactly one miss 10 days back → score 95, concretely demonstrating "one miss doesn't crater it" rather than just asserting it. Streak badge confirmed still present and correct alongside the new ring (31-day streak on the all-done habit). Ring fill percentage cross-checked against its own `stroke-dasharray`/`stroke-dashoffset` math and found to match the displayed number exactly. Zero console errors. Screenshot confirmed all three color bands render correctly and the ring sits cleanly in the row. Mobile geometry check at 390px (with a deliberately long habit name) confirmed no overflow — the name truncates via ellipsis, leaving the streak/ring/delete controls their full space.

- **3.3 Weekly Report widget (dashboard.html + gym.html + health.html + obsidian-sync.js)** — DONE (2026-07-18). Answers: week closes Monday 6 AM (matches the app's existing daily 6 AM rollover, so a "day" and a "week" reset together, rather than the master prompt's own Sunday 9 PM placeholder); all 5 categories as listed ship in v1.
  - **The real work here was sourcing the data, not the UI.** Two of the five categories needed genuinely new persistence, since nothing in the app tracked them over time before now: gym.html's `publishFitnessSummary()` (already existed, from 1.7) is extended to also compute `sessionsThisWeek`/`volumeThisWeek`/`weight7dAvgChange` — read from gym.html's own already-normalized `state`, not parsed raw from outside, since its `po_coach_v1` blob goes through a `normalize()` step on load that a naive outside read would miss. health.html gets a new `recovery_log_v1`, one entry per day, same append-only shape as the existing Sleep Log (1.2) — `eq.health.summary` only ever held the *latest* reading, which isn't enough to average a week.
  - **Finance "cash-flow pace" is a simplification, flagged rather than overclaimed**: `budget:income`/`budget:expenses` are flat recurring monthly line items with no date-stamped transactions, so there's no way to compute a real week-by-week "pace" from what the app actually stores. Reports the current monthly income−expenses figure instead (the same number Finance's own dashboard summary card already shows) rather than fabricating week-over-week granularity the data can't support.
  - **"Goals progressed" became "goals archived this week"**: the spec's own metric needs a goal's progress *value* at the start of the week vs now, which isn't tracked (only the current value persists). Rather than add yet another new snapshot mechanism for this one sub-metric, used the real `archivedAt` timestamps from 1.7's Goal Archive — an honest, directly-computable proxy (completions, not partial progress) rather than a fabricated delta.
  - **A week with literally zero data across all 5 categories is not saved as a report** — a brand-new install's very first visit would otherwise immediately synthesize a "last week" report full of "no data" placeholders before the user has done anything, which is a poor first impression and effectively made the empty-state UI unreachable. Caught while writing the generation logic, before it ever got tested wrong, by tracing through what a fresh install's first `ensureWeeklyReports()` call would actually produce.
  - Report generation walks forward from the last recorded week (catches up correctly if the app wasn't opened for several weeks) and keeps the most recent 12, trimming as a side effect of generating a new one — not on every render, so data that's already in storage is never silently truncated outside of that path. "Write to Obsidian" (`ObsidianSync.pushWeeklyReport`, new) always overwrites for a given week rather than preserving prior content the way `moveProjectPageToFolder` does for project pages — deliberately different from that pattern, since a weekly report is "the current numbers for this week," not freeform user-owned content that must survive a re-write.
  - Verified against one fully hand-constructed week of cross-page test data (5 nights of sleep/recovery, a published fitness summary, 4 days of task-completion snapshots, one goal archived mid-week, a habit done 5 of 7 days, and simple monthly budget figures): all five categories' computed numbers matched exact hand calculations (7.4h avg sleep, 64% avg recovery, 3 sessions, 12,500 volume, -0.4kg weight trend, 75% task completion, 1 goal completed, 71% habit success = 5/7 rounded, +GBP 800 cash flow) with zero discrepancies. Separately verified the catch-up/backfill logic and the 12-week trim (seeded 14 weeks with a deliberate 1-week gap, confirmed generation filled exactly the missing week then correctly trimmed to 12, keeping the newest week rather than an arbitrary slice) and the Obsidian export (correct weekKey, correctly formatted markdown, status message). Zero console errors. Screenshot confirmed the card reads cleanly as a single "shareable-looking" summary, sitting correctly below the six existing summary cards. Mobile geometry check at 390px confirmed the responsive metric grid correctly reflows to 2 columns with no overflow.

- **3.4 Quick Capture from anywhere (topbar.js)** — DONE (2026-07-18). No ASK FIRST items; built as specified (floating "+", not the long-press alternative — more discoverable on its own).
  - **Lives in `topbar.js`, not any single page** — the only section so far that genuinely needs to run everywhere, matching how the nav/service-worker registration are already centralized there rather than duplicated per-page.
  - **Real bug caught during architecture research, before any code was written**: the natural-looking approach for the "Note" destination would be to call the existing `window.Vitality.save()` bridge. But `vitality-bridge.js` derives its storage key from *the current page's own filename* (`appKey()` reads `location.pathname`) — since `topbar.js` runs on every page, calling that bridge from Quick Capture would silently write the note into whichever page happened to be open (e.g., into `vitality_health`'s slot while sitting on health.html) instead of into Notes. Writing directly to Notes' actual cache key (`vitality_bridge:vitality_notes`) sidesteps this entirely, regardless of which page Quick Capture is opened from. Confirmed both halves of this explicitly in testing: captured a note *from health.html* and verified `vitality_bridge:vitality_health` stayed untouched while `vitality_bridge:vitality_notes` got exactly the right entry.
  - **Trade-off, documented rather than silently accepted**: this write only updates Notes' local cache immediately — the cross-device Supabase push happens the next time Notes is actually opened on this device (its own boot already does a full load/save cycle), not duplicated inline in topbar.js. Replicating the push here would mean depending on `window.supabase` being loaded, which isn't guaranteed on every page topbar.js runs on (the CDN script tag is a per-page include, not universal) — an inline push could silently no-op on pages that don't happen to have it.
  - All four other destinations write directly to their real, already-established shapes rather than inventing new ones: `goals:<dateKey>` for tasks (today's key computed with the same 6am day-boundary rule as planner.html's `getActiveDateString()` — a task captured at 2am belongs to the still-open previous day, not a fresh empty one), `shopping_list_v1`, and `books_v1` (matching 1.1's Book Tracker fields exactly, landing new captures on the "Want to Read" shelf via `status:'want'`).
  - Reused the existing "More" sheet's exact visual pattern (bottom sheet, dark backdrop, same border-radius/padding conventions) for visual consistency rather than inventing a new sheet style, and the app's established white-pill "primary action" look (same as Settings' Save button) for the floating button itself.
  - **Testing needed its own temporary bypass, not just landing-guard's**: the existing `isEmbedded()` check correctly suppresses all nav chrome (bottombar, cat-tabs, and now Quick Capture) when a page is iframed — exactly right for production, since an embedded tile shouldn't grow a floating capture button, but it also meant this session's usual iframe-harness testing pattern couldn't see the button at all on the first attempt (it silently never rendered). Added a second temporary one-line bypass (`return false;` at the top of `isEmbedded()`) for the duration of testing, reverted immediately after — confirmed via the same before/after `TEMP: disabled` grep check already used for `landing-guard.js`'s bypass.
  - Verified end-to-end from health.html (deliberately *not* Notes, Planner, or Ability — the three pages actually being written to) with a shared browser profile: FAB opens the sheet; an empty-input chip click neither saves nor closes; Task today and Task tomorrow both land in the correct dated key with `done:false`; the Note capture correctly split "first line = title, rest = body" exactly matching Notes' own composer logic, and (the critical check) went to the right page's storage key, not the current page's; Shopping list and Book captures matched their real pages' exact field shapes; backdrop-click closes without saving. Zero console errors. Screenshot confirmed the FAB and sheet render cleanly and match the app's established visual language. Mobile geometry check at 390px confirmed the chip row wraps correctly with no overflow.

- **3.5 Correlation view (health.html)** — DONE (2026-07-18). Answer: default preset pairs are Recovery×Task completion, Weight trend×Task completion, Sleep×Recovery (all three available as one-tap chips; any of the 5×4 possible pairs still reachable via the two dropdowns directly).
  - **"Habit score" needed a different metric than 3.2's ring**, not the same one reused: 3.2's Habit Health Score is a live, smoothed, decaying rolling value with no daily history of its own — there's nothing to plot it against day-by-day. Computed a directly-available per-day proxy instead (% of habits done that day, from `habits_v1`'s raw history — the same math as the Weekly Report's `habitSuccessPct`, just per-day instead of per-week) and labeled it "Habit success %" in the UI specifically so it doesn't get confused with the 3.2 ring, which is a genuinely different number answering a different question.
  - Weight trend reads gym.html's flat `po_coach_weights` array directly (safe, per the established precedent for simple stable arrays) and computes a trailing 7-day average *per day* across the window, not the raw daily weight — matching what "7-day trend" actually means rather than just relabeling the raw log.
  - Scatter (not dual-line) — Pearson r is inherently about paired (x,y) values, and a scatter is the more honest visualization for "these two things move together," where a dual-line risks implying a time-series story the correlation itself doesn't claim.
  - Both honesty rules from the spec enforced directly, not just described: fewer than 14 paired days shows an explicit count-based empty state ("8 of 14 days needed...") instead of a chart with too little data to mean anything, and the "Correlation, not causation" line is hardcoded to always render alongside the r value, never conditional on the r value itself.
  - **Real bug, caught by checking the sentence's actual claim against the test data's known constructed relationship, not just checking that a sentence rendered**: the plain-English generator's direction word (`diff >= 0 ? 'higher' : 'lower'`) was computed from *above-minus-below*, but the sentence describes the *below* group's value relative to the above group — backwards from what the comparison needs. On deliberately-constructed data where higher recovery cleanly predicted higher task completion, the generated sentence read "...task completion averages 24% **higher** [on below-median days]" — stating the exact opposite of the real relationship. This is the kind of bug that would look completely plausible (a well-formed sentence with a real percentage) while being actively misleading, and a test that only checked "is there a sentence" would have passed it. Fixed by comparing `avgBelow` directly against `avgAboveEq` for the direction word specifically, and re-verified: the same dataset now correctly reads "...averages 24% lower".
  - Verified against constructed data with a known, deliberately near-perfect relationship (task% ≈ 1.5× recovery, clipped at 100): 20 paired days, r = +0.96 ("strong"), median split at exactly the hand-calculated value (50.5), and — after the fix — a sentence that correctly states the true direction of the relationship. Separately confirmed the 14-point honesty gate with an 8-pair dataset (correctly blocked, correct count in the message) and that switching presets/dropdowns correctly re-renders and clears preset highlighting on manual selection. Zero console errors. Screenshot with noisier, more realistic constructed data (still positively trending but with scatter) showed a visually convincing upward scatter, correct r and sentence, and the disclaimer always present. Mobile geometry check at 380px confirmed both dropdowns and the scatter fit with no overflow.

- **3.6 Auto-suggest time slot (planner.html)** — DONE (2026-07-18), the last Phase 3 section. Answers: 30-minute default duration when unset; no protected blocks for v1 (only real calendar events count as busy — training windows/meal times/wind-down were the master prompt's own speculative guesses at a routine it doesn't actually know, not defaults to accept).
  - A "🕐" button appears only on undone, non-read-only task rows (a done task doesn't need scheduling; the read-only "tomorrow, locked until 6 AM" rows already disable every other control the same way). Opens a `.dr-modal` reusing the exact same modal pattern already used for the Calendar event editor in this same file, rather than inventing a new modal style.
  - Busy intervals come from two sources merged and sorted together: today's local calendar events (`local_cal_events_v1`) and, if connected, today's real Google Calendar events via the same `fetchGcalEvents()` built in 1.5. No Google connection just means the search only avoids local events — a soft degrade, not a hard requirement to use the feature.
  - Waking hours reuse the existing `day_ring_times_v1` Settings value (same key dashboard's Day Ring already reads/writes) rather than asking for a separate wake/bed time just for this feature — one source of truth for "when the day starts and ends" across the app.
  - The search always starts from `max(wake time, right now)`, never the past, and rounds up to the next 5-minute mark so a suggestion never lands on an odd time like 2:47. Walks forward through the sorted busy list, and returns the first gap wide enough that also ends before bedtime — or an honest "no slot found, try shorter" message rather than forcing a bad suggestion.
  - **Second real regression of the exact same bug this build already fixed once, caught only by testing**: the accept handler called `loadCalendar()` without awaiting it before setting the "Added to Google Calendar" confirmation — the identical race from 1.5's quick-add, where `loadCalendar()`'s own "Loading…"/blank status writes clobber whatever was written just before it. 1.5's build-log entry says outright to await it first; this later section's accept handler was written without applying that already-documented lesson. Caught the same way as before — the mocked-fetch test showed an empty `calStatus` where "Added..." should have been — and fixed identically (`await loadCalendar()` before the confirmation write). Worth remembering this class of bug can recur even after being fixed and documented once, if a later section copies the shape of the code without re-deriving why the ordering matters.
  - Verified the slot math against deliberately time-relative constructed scenarios (wide-open waking hours so "now" is always the effective floor, avoiding dependence on whatever the real wall-clock hour happened to be during the test run): no busy events → a slot starting almost immediately; bedtime forced to ~6 minutes away with a 30-minute task → correctly reports no slot rather than suggesting an impossible one; restored open hours with a 15-minute task → correct 15-minute-wide suggestion. Confirmed the duration field resets to the 30-minute default on every fresh open (not remembered from a prior session, which is the intended behavior, not a bug my own test briefly mis-assumed). Confirmed the full accept flow with a mocked token + Calendar API: correct summary, correct start/end, correct duration, modal closes, and (after the fix) the confirmation message actually stays on screen. Zero console errors. Screenshot confirmed the modal matches the existing Calendar-event modal's visual language exactly. Mobile geometry check confirmed no page-level overflow (a same-class-name selector ambiguity in the test itself, not the app, briefly obscured two of the six checked elements — resolved by relying on the definitive `body.scrollWidth`-vs-viewport check instead, which is unambiguous).

This closes out Phase 3 (3.1–3.6) and the master build prompt's phased feature work. Remaining before considering everything done: a pass through the "Acceptance Checklist" section of this document.

## Phase 4

- **Phase 4 appended 2026-07-18**, same one-section/ASK-FIRST-before-building process as 0-3. Some of 4.1's architecture (the Anthropic tool-use loop in api/chat.js, and a working calendar-event tool) already existed from ad-hoc same-session work done *before* this phase was formally appended — treated as a head start, not rebuilt.
- **4.1 questions** — answered before building: use the v1 tool list as specified (all 13, including get_data and remember/forget — remember/forget deferred to 4.3 itself, since building them now would mean deciding 4.3's own open questions early); add_calendar_event keeps the confirm-first card and now writes to both `local_cal_events_v1` and Google (previously Google-only); undo is an immediate in-chat link only, no persisted revert log.
- **4.1 Tool Use** — DONE (2026-07-18). 12 tools + `get_data` implemented in `ask.html` via a `TOOL_REGISTRY` (schema + `confirmMode` + `execute` per tool) and a generalized `runTurn()` loop: silent tools execute immediately and the loop re-POSTs the tool_result for another round; confirm-gated tools render a proposal card and pause, resumed later by `resolvePendingAction` on click. `api/chat.js` extended to pass `tools` through, accept array-shaped tool_use/tool_result message content (previously string-only), and return `{reply, toolUse, rawContent}` instead of just `reply`.
  - **Every data shape was verified against the real code before writing a tool against it, not assumed** — caught in the process: weight lives in gym.html's own `po_coach_weights` (simple array, *not* behind the `state`/`normalize()` pipeline that had been a worry from the Weekly Report section), shopping items need `lastsValue`/`lastsUnit`/`estDays` fields the tool doesn't collect (defaulted sensibly rather than omitted, which would have produced a malformed item the real Shopping List UI half-renders), and stack items resolve through `stack:items` (a possibly-customized live copy) not the static `STACK_DEFAULTS` template array.
  - **`confirmMode` is not a fixed per-tool flag** — it's `'never'` / `'always'` / or a function of the tool's own input, because the spec's rule ("destructive or overwrite... requires confirm, logging executes silently") only makes some tools *conditionally* confirm-gated: `log_weight`/`log_sleep`/`log_habit` are silent for a brand-new day's entry but render a confirm card the moment that call would overwrite an existing one for the same date — checked live against the real store at call time, not guessed from the tool name.
  - **Tasks had no `id` field at all before this** (`{text, done}` only, de-duplicated by text everywhere in planner.html) — added `id` for tasks the assistant creates, plus a small fix to planner.html's `rollover()` and the "push to tomorrow" button so an id survives both the daily fold-forward and a manual push, not just a fresh add. Tasks predating this field are still addressable, via a synthesized `text:<exact text>` reference `get_data` hands out instead of a fabricated id — same non-fabrication precedent as goals' `createdAt` backfill in 1.6.
  - **Goals had no notes concept either** — added `notes: []` (array of `{text, at}`, additive/backward-compatible, same pattern as the task id above) directly on the goal object rather than a separate keyed store, matching how contacts already keep notes inline.
  - **Every write goes through `logAssistantActivity()`**, appending to `eq.assistant.activity_log_v1` (capped 200) and returning an id used two ways: an in-chat "Undo" link (in-memory closure per entry, session-only, per the answered undo-scope question) and a new read-only "Assistant Activity" section in settings.html listing the full log. Silent-tool results render as a small inline card ("✓ Logged 82.4 kg") with that Undo link; failed/not-found results (e.g. `log_contact` for an unmatched name) render the same card shape with a ⚠️ and no Undo link, and the failure reason is fed back to Claude as the tool_result so it can naturally explain/ask rather than the app inventing UI copy for every possible failure.
  - **A hard `MAX_TOOL_ROUNDS = 6` cap** on the silent-tool loop, client-side — not in the spec, added because a loop with no cap and a model that keeps reaching for tools has no natural stopping point other than running up API spend against the user's own key.
  - Verified end-to-end with a scripted, stateful mock of `/api/chat` (and the Google Calendar create endpoint) driving six real scenarios through the actual page: a silent add_task followed by its Undo (task removed, activity entry marked undone, no button left to double-click); log_weight silent on a fresh day *then* correctly switching to a confirm card on a same-day re-log, with the stored value verified unchanged until the actual confirm click; add_calendar_event's confirm writing to both `local_cal_events_v1` and a mocked Google Calendar create in one action; log_contact against a nonexistent name failing gracefully with the right tool_result text back to Claude; and the round cap stopping a deliberately infinite tool-call sequence at exactly the limit with a visible error rather than hanging or silently truncating. Zero console errors across all six.
  - **Re-verified from scratch in a follow-up session-continuity check** (asked explicitly since the build had spanned a context/usage boundary) — re-read every touched file end to end for structural completeness (balanced braces, proper closing tags, all 13 registry entries matched to a real `exec_*` function) before re-running a fresh, wider test hitting all 12 tools individually. That pass caught one real gap: `add_task`/`add_shopping_item`/`add_book`'s tool_result summaries never included the id they'd just generated, so a same-conversation "add X, then do Y to it" couldn't chain without an extra `get_data` round-trip (the tool's own description anticipated this as a fallback, so nothing was actually broken — just less direct than it should be). Fixed by including `(id: ...)` in those three summaries; re-tested with a corrected harness that reads the id back out of the mocked tool_result the way Claude actually would, rather than assuming a reference upfront. (Also caught two bugs in the *test* harness itself along the way, not the app — an operator-precedence mistake that silently swallowed one assertion's label, and an unrealistic hardcoded task reference — both fixed before trusting the result.)

## Phase 5

- **Phase 5 appended 2026-07-18.** Only requires Phase 0 + Phase 2 (has); "benefits from" Phase 4's tools/audit/rules, but 4.2-4.8 aren't built yet, so 5.2's birthday→rules-engine link and any audit/digest cross-links from later sections are deferred, not faked.
- **5.1/5.2 questions** — answered before building: no React "Keep" prototype available (checked both this repo and the other accessible directory, `tiles-library`, which turned out to be this app's own plain-HTML template origin, not a React prototype) — built from the written spec text alone; **Peak** yields its core nav slot to Interactions (moved to the More sheet); tiers/cadences used exactly as specified (Inner 7d / Close 21d / Acquaintance 60d / Networking 90d); existing contacts just migrate, no separate import.
- **5.1 New Interactions page + nav slot** and **5.2 Contact manager** — DONE (2026-07-18), built together since 5.1's only real content *is* 5.2's contact manager (Inbox, 5.5, is a separate not-yet-built section — the page currently holds People only, on purpose, not a placeholder "coming soon" Inbox zone).
  - New `interactions.html`: search + tier filter chips + a single list/detail view that toggles in place (mobile-first single-pane, no side-by-side split) rather than a true master-detail layout, since the app's whole audience is "primary use is a phone."
  - **Schema migration is read-time and idempotent** (`migrateContact()`, gated on a `__v2` flag), not a one-time destructive pass: old `tier` values remap (`inner`→`inner`, `close`→`close`, `network`→`networking`; `acquaintance` is new, nothing to remap), a single `lastContactedAt` becomes a one-entry `history` seed (`channel: 'other'`), and `cadenceDays` is dropped entirely — cadence is now always derived from tier via one shared map, never stored, so the two can't drift apart the way they could before (tier and cadence were independently editable in the old form).
  - **Cadence changed for existing tiers, not just added a new one** — Close moved 14d→21d and Network(→Networking) moved 30d→90d, per the answered tier question. Anyone already using those tiers gets the new cadence on next read, not the old one — flagging since it's a real behavior change for existing data, not just new contacts.
  - Kept the existing `type` field (family/friend/colleague/other) even though the prototype's own field list (as described in the spec) didn't include it — the spec never said to remove it, and dropping a working, already-populated field without being asked would be destructive-by-omission rather than a real decision.
  - **Every touchpoint outside interactions.html was found by grep before changing the schema, not assumed** — exactly 4 files reference `contacts_v1`/`lastContactedAt`: this page (owns it now), `planner.html` (old full tracker, replaced), `dashboard.html` (overdue badge), and `ask.html` (Phase 4.1's `log_contact` tool + `buildRelationshipsContext`). All four updated together in the same pass so nothing was left reading the old single-field shape.
  - Planner's full Relationships section (add-row + list, ~190 lines of JS) replaced with a compact read-only card (contact count + overdue count) linking to `interactions.html`; old CSS classes (`.contact-row` etc.) left in place, inert, matching this build's established precedent for low-risk dead CSS. Dashboard's overdue count moved off the Tasks card's text entirely into its own new 7th `.dsum-card` ("Interactions"), matching the existing 6-card pattern exactly (icon/title/arrow/body).
  - `log_contact` (4.1) now appends a `{date, channel:'other', note:''}` history entry and clears any active snooze on log, instead of setting a since-removed `lastContactedAt` field; its undo matches the new entry back out by date+channel (object identity doesn't survive the JSON round-trip through `logAssistantActivity`'s closure once storage has been re-read).
  - **Real bug caught by testing, not code review**: `planner.html`'s and `dashboard.html`'s ported overdue-check both tested "history is empty" *before* checking snooze, so a snoozed contact with no prior history (exactly what snooze is for — someone you haven't gotten to yet) still showed as overdue, silently ignoring the snooze. `interactions.html`'s own logic and `ask.html`'s already had the correct order (snooze checked first); these two didn't, from copying the shape without re-deriving the ordering. Fixed by swapping the check order in both places; re-verified with a contact that is simultaneously snoozed *and* has empty history, confirming it no longer counts as overdue in either file.
  - Verified end-to-end across all four touched pages in one pass: seeded old-shape data (old tier keys, single `lastContactedAt`, no `history`/`tags`/`snoozedUntil`), confirmed migration output directly; list sort (never-contacted above overdue), search, tier filter, tag add, log-a-chat (history grows, status clears, correct channel/note), snooze (and the bug above), add contact, and delete all confirmed through the real UI; Planner and Dashboard's cards confirmed reading the same migrated data correctly (including the post-fix overdue count); the `log_contact` tool confirmed appending correctly and `buildRelationshipsContext`'s output confirmed sane. Zero console errors across every page.

## Phase 6

- **Phase 6 arrived via a new consolidated document, `BUILD_PLAN.md`, appended 2026-07-18** — a single all-phases file superseding the incremental phase-by-phase pasting used for 0-5, introducing a build-order override (0 → 6.0 → 6.1 → 1-5 → 6.2-6.10) and retroactively revisiting a few already-decided items (peak.html's fate, the nav lineup). Rather than jump straight to 6.0/6.1 and leave 1-5 unre-examined against the new document, explicitly chose (the user's call, not assumed — the alternative offered was skipping already-built sections) to **re-walk every section from 1 onward against BUILD_PLAN.md once 6.0/6.1 are settled, including sections already built**. 6.1 is logged first below because it's the section actually built so far; 6.0 is blocked, not skipped (see its own entry).
- **0.1 (re-examined against BUILD_PLAN.md)** — kept as-is; no change from the original 0.1 decision already on file above.
- **6.0/6.1 questions** — answered before building: 6.0's device-auth approach is **per-device tokens with revocation** (not a single shared passcode); 6.1's peak.html is **deleted outright and merged into Health's new Energy section** (not just relocated in nav); 6.1's nav lineup uses the proposed lineup as specified; **Ability keeps its name** (not renamed to Growth, the recommended option); Notes moves to the More sheet alongside Finance and Ability.
- **6.0 Security gate — first attempted as per-device tokens with revocation, blocked on missing infrastructure (flagged to the user rather than built around silently), then rebuilt as a single-passcode gate once the user chose to fall back.** Per-device revocation needs real server-side persisted state — a token store that can be looked up per device and individually revoked — and this repo had zero server-side storage of any kind (no `package.json`, no `vercel.json`, no database/KV/Postgres binding; every existing `api/*.js` function was stateless). Flagged this plainly rather than silently building toward an unconfirmed fallback; the user chose the single-passcode approach instead of provisioning a database.
- **6.0 follow-up questions (single-passcode rebuild)** — asked before building, all answered "skip" (proceed on the recommended defaults rather than walk through each): gate scope is `api/chat.js` plus the other fetch-callable endpoints (`api/whoop-refresh.js`, `api/whoop-data.js`, `api/google-calendar-refresh.js`) — the two OAuth *callback* endpoints (`whoop-callback.js`, `google-calendar-callback.js`) are left ungated since they're hit by a redirect from WHOOP/Google, not a fetch call, and physically can't carry a session header; passcode value is set by the user directly as a Vercel env var, never typed into chat; session length is 90 days when "remember this device" is checked, session-only (12h ceiling) when not.
- **6.0 Security Gate (single passcode)** — DONE (2026-07-19). A stateless design, deliberately not a database-backed one: `lib/auth.js` holds one salted passcode hash (`PASSCODE_HASH` env var, scrypt with a fixed embedded salt — fine here since there's exactly one passcode for the whole app, not one hash per user) and one signing secret (`SESSION_SECRET` env var). A correct passcode gets a stateless HMAC-signed token carrying only an expiry — nothing to store, nothing to look up, nothing to individually revoke, which is exactly the tradeoff the user chose over per-device tokens.
  - New `api/auth-login.js`: POST `{passcode, remember}` → verifies against `PASSCODE_HASH` (timing-safe compare), issues a signed token (90d if remembered, 12h if not) via `SESSION_SECRET`. Same best-effort in-memory rate limit pattern as `api/chat.js`'s existing limiter (10 attempts/5min), same documented caveat that it's not a real distributed rate limit on serverless.
  - Gated endpoints (`api/chat.js`, `api/whoop-refresh.js`, `api/whoop-data.js`, `api/google-calendar-refresh.js`) each call a shared `requireSession()` from `lib/auth.js`, checking a new `X-Eq-Session` header — deliberately not `Authorization`, since `api/whoop-data.js` already uses that header to forward the caller's *WHOOP* bearer token straight through to WHOOP's API; reusing it for the app's own session would collide with that.
  - New `auth-gate.js` (client-side, synchronous, first thing in every gated page's `<head>` — even before `landing-guard.js`): checks `eq_session` in localStorage/sessionStorage for an unexpired token; if missing/expired, redirects to a new `lock.html` (dark-themed passcode screen matching the app's existing color tokens, "remember this device" default on) carrying a same-origin-only `?return=` target. Skips entirely when embedded in an iframe (same rule `landing-guard.js` already follows, same reason). Exposes `window.EqAuth.header()` for other scripts to attach the session header to their own fetch calls, and `window.EqAuth.signOut()`.
  - **`lock.html`'s `?return=` target is whitelisted against the app's known page list**, not passed through raw — defends against this ever becoming an open redirect if a crafted URL were ever shared/bookmarked.
  - **A real coordination bug found and fixed during testing, not assumed away**: `auth-gate.js`'s redirect and `landing-guard.js`'s own redirect are both synchronous `<script>` tags on the same gated pages, and a location change fired later in the same task can supersede one fired earlier before either navigation actually starts — meaning an unauthenticated visit could, in the worst case, land on `index.html` instead of the lock screen, skipping the gate entirely. First fix attempt was `window.stop()` right after `auth-gate.js`'s own redirect call, to hard-stop further script execution — that worked logically but left automated/headless navigation-completion checks hanging indefinitely (confirmed by testing, not assumed), so it was replaced with a small cooperative flag (`window.__eqAuthRedirecting`) that `landing-guard.js` checks and bails out on. `landing-guard.js` needed a 4-line addition for this; nothing else about it changed.
  - **Not in the original 6.0 spec text, added anyway**: a "Sign out this device" button in Settings' new Security section, calling `window.EqAuth.signOut()`. The spec never asked for a way to lock the app back up short of clearing browser storage manually — for a personal-device security gate that seemed like a real, cheap-to-close gap rather than scope creep, so it's flagged here explicitly rather than silently bundled in.
  - `sw.js`: `lock.html` and `auth-gate.js` added to `APP_SHELL`; `CACHE_NAME` bumped `v3`→`v4`.
  - **`privacy.html` deliberately left ungated** — it's the kind of page that sometimes needs to stay reachable without authentication (e.g. some OAuth consent-screen review flows expect a publicly reachable privacy policy URL), and gating it wasn't asked for.
  - **Found but not fixed, flagged for the user rather than silently expanded into scope**: `landing-guard.js` (the "always land on index.html on a fresh open" redirect, unrelated to this security gate) was never added to 5 pages when they were originally built — `ability.html`, `interactions.html`, `planner.html`, `ask.html`, `finance.html` all lack it, while the other 9 core/more pages have it. Not a security hole (`auth-gate.js` now covers all of them regardless), just a pre-existing UX inconsistency (a fresh/bookmarked open of those 5 pages skips the landing splash) noticed while surveying every page's `<head>` for this section. Left alone since fixing it wasn't asked for and isn't related to 6.0's own scope.
  - Verified via headless Chrome across 5 scenarios: a completely fresh visit (empty storage) to a gated page correctly redirects to `lock.html`; a seeded valid, unexpired session lets the real page render with no redirect; a seeded expired session still redirects; a page loaded inside an iframe (`po-water.html`, embedded the way it always is inside health.html) is never gated regardless of session state; and `lock.html`'s own form — driven end-to-end with a mocked `/api/auth-login` — correctly posts the typed passcode and remember flag, stores the returned token under `localStorage` when remembered and `sessionStorage` when not, redirects through to a legitimate `?return=` target (confirmed by landing on that real page's own content, not bounced back to the lock screen), and falls back to `dashboard.html` when the `?return=` value looks like an open-redirect attempt. All assertions passed after two bugs were found and fixed in the test itself (a wrong expected page title, and a cross-iframe localStorage leak between two sequential test cases sharing one file:// origin) — neither was an app bug.
  - **Cannot be verified from this environment, needs a real check after the user sets `PASSCODE_HASH`/`SESSION_SECRET` in Vercel and deploys**: real-world passcode entry on an actual deployed instance.
- **6.0 follow-up (2026-07-19)**: the user asked to "go back and do" the passcode-setup step that was left as a manual instruction ("run this Node command yourself"), and separately asked for an independent re-verification pass, not just a repeated claim.
  - **Algorithm changed: `scrypt` → PBKDF2-SHA256, 210,000 rounds.** No Node runtime was available in this build environment (confirmed — `node` not found via either shell), so the original plan of handing the user a `node -e crypto.scryptSync(...)` command to run themselves was the only way to actually get a hash, and there was no guarantee the user has Node either. PBKDF2 is implementable identically via .NET's `Rfc2898DeriveBytes` (available in this environment's PowerShell), which meant the hash could actually be generated end-to-end instead of handed off as an unverified manual step. Both are legitimate password-hashing choices; PBKDF2 at this round count is not a meaningful downgrade for a single-user app behind a rate-limited login endpoint. `lib/auth.js` updated accordingly; nothing else about the token/session design changed.
  - Generated a real passcode, its `PASSCODE_HASH`, and a random `SESSION_SECRET` directly (shown to the user once in-chat, not saved to any file or memory) — computed via PowerShell using the exact same salt/rounds/keylen/digest `lib/auth.js` verifies against, and independently re-derived a second time to confirm the hash is deterministic before handing it over.
  - **Re-verification found two real, previously-missed stale `peak.html` references** that the original 6.1 cleanup's grep pass didn't catch (that pass searched for the literal string `peak.html`, not the bare word `peak`, and both of these used the bare word): `vitality-bridge.js`'s own top-of-file comment still listed `peak.html` as one of the "sealed" tile pages it bridges (fixed — the real current list, confirmed by grepping which pages actually load that script, is train/notes/vitals/brand.html); `brand.html` had a CSS comment referencing "the same curve style as the Peak page" (fixed, pointed at the app's charts generally instead). Neither was a functional bug — both were now-dangling documentation — but both are exactly the kind of thing a "did you actually check" pass exists to catch.
- **6.0 temporarily disabled (2026-07-19), on explicit user instruction ("remove the passcode for now, plan on integrating it later").** Nothing deleted — every implementation file (`lib/auth.js`, `lock.html`, `auth-gate.js`, `api/auth-login.js`) stays in the repo untouched, so re-enabling later is a matter of restoring what was removed/commented, not rebuilding. What actually changed: the `<script src="auth-gate.js"></script>` tag was removed from all 16 pages that had it (the client-side redirect-to-lock-screen no longer fires); the `requireSession(...)` call and its import were commented out (not deleted) in all 5 gated endpoints (`api/chat.js`, `api/whoop-refresh.js`, `api/whoop-data.js`, `api/google-calendar-refresh.js`, `api/news.js`), each with a one-line pointer back to this note. `sw.js`'s `CACHE_NAME` bumped for the 16 page changes.
  - **Left deliberately alone, not cleaned up**: `landing-guard.js`'s cooperative `if (window.__eqAuthRedirecting) return;` check (harmless — that global is never set now, so this is just always-false dead code, not worth touching for a temporary state); `sw.js` still precaches `auth-gate.js`/`lock.html` (the files still exist, no reason to stop caching them); Settings' "Security" section still shows a "Sign out this device" button that is now a silent no-op (`window.EqAuth` is undefined with the gate removed, and that click handler is already guarded with `if (window.EqAuth && window.EqAuth.signOut)`, so it just does nothing rather than erroring) — flagged here rather than fixed, since re-enabling the gate later makes it functional again with zero further changes.
  - Verified via headless Chrome: a dashboard.html load with zero `eq_session` set (previously would have redirected to `lock.html`) now renders the real page content directly, with no JS errors and no redirect — 3 assertions, all passed.
  - Re-verified systematically rather than re-asserting: confirmed `peak.html` still absent from disk; confirmed all 15 gated pages have `auth-gate.js` as the literal first script tag, before `landing-guard.js` where present (not just present somewhere in the file); confirmed all 4 gated endpoints import and call `requireSession` and both OAuth callback endpoints remain untouched; re-grepped the entire codebase for every `fetch('/api/...')` call site (8 total: the 7 already known gated ones, all still correctly carrying the session header, plus `lock.html`'s own login call, correctly ungated) to make sure none had been missed; confirmed `lock.html`'s return-target whitelist lists all 16 real pages exactly; re-ran the full headless-Chrome gate test suite (fresh-visit redirect, iframe-embedded skip, mocked login → storage → redirect-through, and the open-redirect rejection) end-to-end after all of the above — all 8 assertions passed again, plus a fresh-visit redirect spot-check against a second page (finance.html) to confirm it wasn't specific to the one page already tested.
- **6.1 Consolidation: nav restructure + Peak → Health merge** — DONE (2026-07-18). `topbar.js`'s `PAGES` array restructured to the confirmed lineup: the mobile core bar is now exactly Main/Health/Fitness/Planner/Interactions (5, down from 8) — Finance/Ability/Notes moved into the More sheet alongside the pages already there (Train/Vitals/Brand/Settings/Ask, now 8 total). The `peak` entry was removed outright, not relocated. Desktop's `cat-tabs` row (all pages, unfiltered by `more`) reflects the same 13-page set automatically, since both presentations are driven by the one shared `PAGES` array.
  - `peak.html` deleted from disk (`git rm`) after its full 910-line contents were read and every cross-reference across the codebase was grepped down to zero (the only remaining "peak" hits left afterward were peak.html's own internal SVG gradient ids, gone with the file itself). Its content was ported into a new **Energy** section on `health.html`, inserted between the Whoop card and Sleep Log — same circadian-baseline-plus-stimulant-pharmacokinetics math (`BASELINE` array, a `SUBSTANCES` table for coffee/espresso/energy drink/matcha, `contributionAt()`/`effectiveAmplitude()`), same `.sl-chart`-pattern SVG (viewBox 320×130, quadratic-smoothed path, area fill, glow filter) 1.2's Sleep Log already established, reused instead of peak's own bespoke chart engine — one charting approach across the app, per the master prompt's own rule.
  - **Deliberately dropped, not carried over**: peak's 3 extra pinnable substances, its line/bars/stack mode toggle, the drag-to-log "feel" line, its own natural-language schedule parser plus separate events store, the atmospheric decoration, and the "how this plugs in" wiring diagram (already stripped of its marketing copy in 0.2, but the diagram concept doesn't fit a data-driven card). Scoped to the functional core plus one clean chart rather than reproducing 910 lines of bespoke UI wholesale — flagging the cut in case a dropped piece turns out to be wanted back.
  - **WHOOP multiplier now reads `eq.health.summary`** (published by the Whoop section already on the same page) instead of a separate fetch — `recovery` scales the whole curve (0.55×-1.2×), `sleepPerf` under 70 additionally caps it (0.65×-1.0×) — and the calendar overlay reads the real `local_cal_events_v1` store Planner already writes to, instead of peak's own separate ad-hoc schedule/parser, so there's one source of truth for "what's on today," not two.
  - **Real bug fixed, not carried over**: peak's stimulant log never reset — an entry logged on any past day stayed on the chart forever. The ported version scopes logs to `{dateKey, entries}` and only ever returns today's via `getTodayLogs()`, auto-resetting the moment the stored `dateKey` is stale.
  - `eq.energy.logs_v1` added to `health.html`'s `initCloudSync` `syncedKeys` so stimulant logs sync cross-device like the rest of the page; `sleepPerf` added to `ask.html`'s `buildHealthContext()` so Ask Claude's health context includes it alongside the fields already there.
  - `sw.js`: `peak.html` removed from `APP_SHELL`, `interactions.html` added (a Phase 5.1 gap that had never made it into the precache list — found and fixed while touching this file for the Peak removal, unrelated to 6.1 itself but worth fixing in the same pass). `CACHE_NAME` bumped `v2`→`v3` per the file's own documented convention for shell-list changes.
  - `vitals.html`: two stale comments claiming a `recoveryFromVitals` function existed in peak.html were corrected (confirmed via a full read of peak.html, before deleting it, that no such function ever existed there — the closest thing was an unrelated `whoopMultiplier`). Not a functional bug, just inaccurate documentation that would have become permanently unverifiable once peak.html was gone — worth fixing before that happened rather than after.
  - `dashboard.html` checked against the new nav reality — no changes needed; it never referenced Peak directly, and its summary cards don't depend on nav structure.
  - Verified via headless Chrome, two harnesses: nav lineup was checked with a standalone topbar.js-only page loaded as a real top-level navigation, not inside an iframe — an iframe would trip topbar.js's own "hide chrome when embedded" rule and produce a false failure. Confirmed: desktop `cat-tabs` shows all 13 pages with no Peak; the mobile bottom bar shows exactly the 5 confirmed core tabs, in order; the More sheet shows exactly the 8 confirmed pages, in order. The Energy section was checked with a seeded-localStorage iframe harness: day-scoping (a stale `2020-01-01` log correctly does not appear as "today's" logs), the calendar overlay (a seeded `local_cal_events_v1` entry renders exactly one event band), the stack-click flow (persists under today's date key, discards the stale entry set, re-renders with the correct mg total), and the WHOOP multiplier — checked directionally (a low-recovery/low-sleep profile vs. a high-recovery/high-sleep profile, confirming the high profile's predicted-energy score is strictly higher) rather than by exact score match, since headless Chrome's `--virtual-time-budget` mode virtualizes the clock and could otherwise skew an exact "now"-dependent comparison between the harness's own math and the page's. All assertions passed, zero console errors.
  - Not committed/pushed yet — no "push to git" instruction has been given for this batch.
- **Phase 1 re-walk against BUILD_PLAN.md (2026-07-19)** — DONE, no changes needed. Per the "re-walk everything" choice above, checked every one of 1.1-1.8 against BUILD_PLAN.md's current text and the actual code, not just against the original build-log claim: 1.1 Book Tracker (no covers, no progress%, yearly count present, confirmed in `ability.html`), 1.2 Sleep Log (auto-fill hook + 7-night avg confirmed in `health.html`), 1.3 Relationship Tracker (superseded by Phase 5's Interactions page — confirmed the notes field 1.3 asked for is present there), 1.4 Gmail (manual-refresh-only confirmed, no polling interval anywhere), 1.5 Calendar Quick-Add (`calendar.events` scope + `createGcalEvent` confirmed), 1.6 goal countdown (pulse animation confirmed correctly gated behind `prefers-reduced-motion: no-preference`), 1.7 Goal Archive (`Archive/<goal title>` folder naming confirmed exact, un-archiving confirmed permanent with an explicit "can't be undone" confirm dialog — a `.proj-history-unarchive` button that looked like a contradiction at first turned out to belong to Projects' own separate, pre-existing, independent archive feature, not goals), 1.8 Completion Chart ("pushed to tomorrow counts as missed" and "zero-scheduled days render as a gap, not 100%" both confirmed verbatim in the code's own comments). Nothing rebuilt — this was a verification pass, not a rebuild.
- **Phase 2 re-walk against BUILD_PLAN.md (2026-07-19)** — DONE, no changes needed. Domain chips (8, a superset of the spec's 6 — Calendar and Notes were added organically by later mid-session work, not a gap), default-on set (Goals/Tasks, Calendar, Health, Fitness, Finance, Habits; Relationships/Notes off) matches the recorded answer exactly, context-size estimate text ("~N tokens of your data attached") matches the spec's own example near-verbatim, model hardcoded to `claude-sonnet-5` with no client-facing toggle, conversation history persists to localStorage. All confirmed directly in `ask.html`/`api/chat.js`.
- **Phase 3 re-walk against BUILD_PLAN.md (2026-07-19)** — DONE, no changes needed, across all 6 sections. 3.1 Year Grid and 3.2 Habit Health Score (`ability.html`): both present, score formula matches Loop's `score = score×k + done×(1−k)` exactly, streak kept alongside the score ring per the recorded answer. 3.3 Weekly Report (`dashboard.html`): week-close time is Monday 6 AM, not the prompt's own Sunday-9-PM placeholder — confirmed this was an explicit recorded decision (matches the app's existing daily rollover), not a mismatch. 3.4 Quick Capture: already fully verified earlier this session while building 6.0 (topbar.js's `QC_DESTINATIONS`). 3.5 Correlation view (`health.html`): `CORR_MIN_PAIRS = 14` enforced before showing r, plain-English sentence generator present, correlation-not-causation disclaimer present. 3.6 Auto-suggest slot (`planner.html`): 30-minute default duration confirmed, "no protected blocks for v1" confirmed as an explicit recorded answer (the training-window/meal-time examples in the spec were the prompt's own speculative guesses, not accepted defaults) rather than a silently-dropped requirement.
- **Phase 4 re-walk against BUILD_PLAN.md (2026-07-19)** — 4.1 re-confirmed fully intact (all 12 tools + `get_data` still correctly registered in `ask.html`, unaffected by every intervening change this session — domain-chip additions, the 6.0 security gate). 4.2-4.8 were genuinely unbuilt, so this became real construction, not verification — started with 4.2 per the one-section-at-a-time discipline.
- **4.2 questions** — answered before building: smart-capture parsing model is **Haiku** (`claude-haiku-4-5-20251001`), per the spec's own recommendation.
- **4.2 Universal command bar / smart quick-capture** — DONE (2026-07-19). Quick Capture's sheet (3.4) gained a "Smart parse" toggle; on, it swaps the destination chips for a single "Log it" button that sends the typed free text through the assistant's tool loop instead of a fixed destination.
  - **Real architecture decision, not a small addition**: Smart Capture needs to run from `topbar.js`, which loads on every page — but 4.1's entire tool registry (schemas, executors, activity logging, the storage shapes for tasks/weight/sleep/contacts/etc.) lived entirely inside `ask.html`'s own closure, unreachable from anywhere else. Rather than write a second, independent copy of that logic for topbar.js (a real drift risk this build has already been burned by once — the Phase 5.2 snooze-ordering bug came from exactly this kind of copy-without-re-deriving), extracted 11 of the 13 tools (everything except `add_calendar_event`, which depends on `ask.html`'s own Google Calendar OAuth state, and `get_data`, which depends on its chat-context builders like `buildHealthContext`) into a new shared `assistant-tools.js`, exposed as `window.AssistantTools`. `ask.html` was rewired to destructure from it and merge its own two local-only tools back into a final `TOOL_REGISTRY` via `Object.assign` — the rest of `ask.html`'s 2000+ lines needed zero changes, since every identifier name stayed the same, just re-sourced. Added to all 14 pages that load `topbar.js` (as a plain, non-deferred script before it, so it's ready before `topbar.js`'s deferred boot runs) and to `sw.js`'s precache (`CACHE_NAME` bumped `v4`→`v5`).
  - **Smart Capture never shows a confirm card**, unlike `ask.html`'s full chat loop — there's no room for one in a one-shot capture sheet, and a same-day overwrite from the user's own just-typed text isn't the kind of surprising background write a confirm gate exists to catch. Every tool call executes immediately regardless of `confirmMode`; `toolNeedsConfirm` is never consulted from this path.
  - **Model selection required a small, deliberate exception to an existing security rule.** `api/chat.js`'s model has been hardcoded server-side since Phase 2 specifically so a compromised/buggy client can't swap models to drive up spend — Smart Capture legitimately needs Haiku instead of the default Sonnet, so `api/chat.js` gained a two-entry `ALLOWED_MODELS` allowlist (`claude-sonnet-5`, `claude-haiku-4-5-20251001`) checked against the request body, falling back to the Sonnet default for anything else. This preserves the original protection's actual intent (no arbitrary client-chosen model) while allowing the one legitimate second option.
  - Verified two ways. First, a regression pass on `ask.html` after the extraction (mocked `/api/chat`, iframe-driven with a pre-seeded session token): a silent tool (`add_task`) still executes immediately and writes correctly through the shared module; a conditionally-confirmed tool (`log_weight`, same-day overwrite) still correctly pauses for a confirm card instead of writing, proving the moved `confirmMode` functions and the merged registry both survived intact. Second, a standalone Smart Capture test (a throwaway page loading `assistant-tools.js` + `topbar.js` exactly like a real page, mocked `/api/chat`): the spec's own worked example -- one capture containing both a sleep amount and a weight -- correctly produced two separate tool calls and two separate correct writes (`sleep_log_v1`, `po_coach_weights`), the request correctly used the Haiku model and only the 11 write-tool schemas (no `get_data`/`add_calendar_event` leaking into Smart Capture's tool list), the status line showed both result summaries, and both writes landed in the shared `eq.assistant.activity_log_v1` audit trail. All assertions passed both times, zero console errors.

- **4.3 questions** — answered before building: memory cap **30 entries**, per spec; saves **silently with a visible chip**, not gated behind a confirm tap (matches the existing silent-tool pattern from 4.1).
- **4.3 Persistent memory** — DONE (2026-07-19). `eq.assistant.memory` (`{id, fact, created}`, FIFO-capped at 30, same drop-the-oldest precedent as the activity log) with two new shared tools, `remember`/`forget`, added to `assistant-tools.js` alongside the 11 from 4.2 — genuinely general-purpose, not chat- or Smart-Capture-specific, so both entry points get them automatically through the same registry.
  - **Injected into every assistant call, not just chat** — a `getMemoryContextText()` helper on the shared module is prepended to `ask.html`'s `assembleContext()` output (always-on, not gated by a domain chip, same treatment as the always-on date line) and to Smart Capture's system prompt in `topbar.js`. Verified directly against the real outgoing request body, not just that the function returns the right string.
  - **Onboarding** (`ask.html`): a first-ever-open-only card (gated on its own `eq.assistant.onboarded_v1` flag, not just "memory is empty," so intentionally clearing memory later doesn't re-trigger it) asks the 4 questions named in the spec text itself (goal phase, training split, sleep target, advice tone) one at a time, each answer saved as a memory entry; "Skip this one" and "Skip onboarding entirely" both work at any point.
  - **Settings gained a full CRUD memory viewer** (add/edit-inline/delete), unlike the read-only Assistant Activity section next to it — memory is user-owned editable state, activity is an audit trail, different treatment is intentional.
  - Verified end-to-end via a seeded/mocked headless-Chrome pass: onboarding card shows and hides correctly, each answer's fact text lands in the store correctly, the *real* chat send request's `systemContext` was captured and confirmed to contain the remembered fact (not just assumed from the helper function in isolation), and Settings' add/edit/delete all round-tripped correctly against seeded data. 13 assertions, all passed.

- **4.4 questions** — answered before building: **button-only generation** (no auto-call on dashboard open — zero baseline daily cost, matches this build's general bias toward opt-in spend over silent recurring API calls); **evening "Close the day" variant included in v1**, not deferred, since it shares essentially all of the morning brief's data-assembly/Claude-call machinery.
- **4.4 Morning Brief + Close the Day** — DONE (2026-07-19), new cards at the very top of `dashboard.html` (above even the Day Ring — the whole point is being the first thing seen, same reasoning 1.4's Gmail panel used for its own placement).
  - **"Serverless assembles" from the master prompt was read as "the assistant call produces the brief," not a literal instruction to fetch data server-side** — there is no server-side data store anywhere in this app (confirmed repeatedly this session, most recently while scoping 6.0), so context assembly stays client-side, same architecture as every other assistant context builder built so far (`ask.html`'s domain builders, Smart Capture's system prompt). Data sources: `eq.health.summary` (sleep/recovery), today's tasks + calendar, overdue contacts (same tier-cadence/snooze-first logic as the Interactions summary card, re-derived rather than shared per this file's own established per-script-block convention), subscriptions renewing within 7 days (`subs`, reusing finance.html's own rolling-forward renewal-date logic), habit completion so far today, and fitness's weight 7-day trend.
  - **Cached per day** (`eq.assistant.morning_brief_v1` / `eq.assistant.close_day_v1`, keyed by `activeDateKeyLocal()`) — a dashboard re-open the same day shows the cached text with no new spend; "Regenerate" is always available.
  - **Close the Day** shows from 8 PM through the rest of the still-active day (i.e. also before the following 6 AM reset, matching this app's own day-boundary convention rather than a bare wall-clock date change). Reviews completion vs. plan; if tomorrow's task list is still empty after closing, an inline prompt asks for tomorrow's top task and writes it straight to `goals:<tomorrow>` — no assistant round-trip needed for that part, since it's a direct user answer, not something to parse. Writes the day summary to Obsidian via a new `pushDailyNote(dateKey, markdown)` in `obsidian-sync.js` (`<folder>/Daily/<dateKey>.md`), added following the exact same one-shot-PUT shape as the existing `pushWeeklyReport`/`pushChatTranscript`.
  - Verified via headless Chrome with mocked `/api/chat`: confirmed zero fetch calls happen on a fresh dashboard load (proving generation really is button-only, not just labeled that way), the outgoing request's `systemContext` was checked directly and found to contain the seeded sleep/recovery/overdue-contact/subscription data correctly, the generated brief cached and rendered correctly, the button relabeled to "Regenerate," and a reload served the cached brief with **zero** additional `/api/chat` calls (proving the cache actually prevents re-spend, not just re-displays). 12 assertions, all passed.

- **4.5 questions** — answered before building: **manual-only trigger** ("Run audit now," same button-only bias as 4.4, not the spec's auto-at-close default); week close stays **Monday 6 AM**, carried forward from 3.3 rather than re-asked, since 4.5 explicitly absorbs that section's own already-decided infrastructure; **verdict is both a one-word verdict AND a /10 score together** (not one or the other); **all domains included**, none excluded.
- **4.5 Weekly Audit — "the core review feature"** — DONE (2026-07-19). Absorbs 3.3 literally as specified: the existing Weekly Report stat card (`computeWeeklyReport()`, `weekly_reports_v1`, the prev/next week browser) is untouched and now doubles as section 1 ("the week in numbers"); a "Run audit" button beneath it generates sections 2-6 for whichever week is currently browsed, not just "this week" — a natural extension of the existing prev/next UX rather than a separate concept.
  - **Structured output via a forced tool call, not parsed free text.** The six audit sections (wins/misses/fixes/suggestions/verdict word/verdict score) need to render as separate cards with real Implement/Ignore buttons on suggestions — parsing that reliably out of free-form prose would be fragile. Added a `submit_audit` tool sent as the *only* tool with a new `toolChoice` passthrough in `api/chat.js` (`tool_choice: {type:'tool', name:...}`), so Claude's reply is guaranteed to arrive as the exact structured shape needed. `MAX_TOKENS` bumped 1024→2048 to give a full 6-section reply room.
  - **Comparative, not a snapshot, per spec**: context sent to Claude includes the most recent prior audit's verdict and "what to do better" list, plus which of its suggestions were actually implemented — so the model can genuinely follow up ("last week's fix was X, this week Y happened") rather than reviewing each week in isolation.
  - **Suggestion cooldown implemented as specified**: Ignore sets `ignoredUntil` 4 weeks out on that suggestion; the context builder scans every past audit's suggestions for anything still inside its cooldown and tells Claude not to re-suggest it.
  - **Implement reuses 4.1/4.2's own tool-execution infrastructure** rather than trying to hand-parse Claude's free-text suggestion into a specific action: the suggestion text is sent back through a small, separate assistant call with the full shared tool registry (`assistant-tools.js`) available, and whichever single tool Claude picks actually executes for real. "Tagged as originating from audit week N," per spec, is implemented at the suggestion-record level (`{status:'implemented', implementedVia: toolName}`, stored inside that week's own audit entry) rather than by mutating the generic activity-log text.
  - **Known, deliberately-not-silently-papered-over gap**: none of the 11 shared tools can create a brand-new habit (`log_habit` only logs against one that already exists — there is no `add_habit` tool in this build). A suggestion like "add a new daily habit" will Implement gracefully into a clear "couldn't find a matching action" message rather than silently doing nothing or crashing, but it won't actually create the habit. Flagging rather than scope-creeping a new tool into this pass.
  - Writes to Obsidian as `Audits/<weekKey>.md` via the existing `pushWeeklyReport()` (reused with a different path prefix, since it's already exactly the right one-shot-overwrite shape — not worth a third near-identical push function).
  - Verified via headless Chrome with mocked `/api/chat`: confirmed the request actually forces `toolChoice: {name:'submit_audit'}`; confirmed the outgoing context contains the seeded sleep/recovery/contact data; confirmed the audit saves correctly keyed by week and renders all sections including the verdict; confirmed Implement round-trips through a second real assistant call and the resulting tool execution actually creates the task (not just claims to) and correctly tags the suggestion with which tool ran. One initial test assertion (an exact fetch-call count) failed on the first pass — investigated rather than dismissed, and traced to a legitimate third network call from this app's own pre-existing Supabase cross-device sync (writing the newly-created task triggers a real sync push) that the test's fetch mock happened to also intercept — a test-assertion miscount, not an app bug; fixed the test and re-confirmed the real behavior was correct throughout.

- **4.6 questions** — answered before building: default-on = recovery<33%, weight swing, habit score drop, subscription renewing (all 4 of the spec's own recommended set) — **plus a 6th rule the user asked for directly, not in the original spec list**: goal due within 48h and still under 75% progress, scoped to goals specifically since that's the only place in this app with both a deadline field and a progress percentage (no separate "assignment" or per-project deadline concept exists elsewhere to extend it to). Contact-emailed (the spec's 5th template) stays off by default, matching the user's selection.
- **4.6 Standing Rules Engine** — DONE (2026-07-19), 6 rule templates, evaluated locally on every dashboard.html load (`app open`).
  - **Action type follows the spec's own per-template wording, not a blanket choice**: only `recovery_low` uses `action:'assistant_message'` (the spec's only template phrased as "→ suggest...", implying generated advice, not a bare fact) — one cheap Haiku call, cached per day. The other 5 are `action:'flag'` (pure local JS, zero API cost), matching their own "→ flag"/"→ surface" spec language.
  - **`contact_emailed` deliberately never fires** — there is no persisted record anywhere of "contact X emailed on date Y" (Gmail's unread state from 1.4 is in-memory only, by design, no server-side token storage to check later). Rather than fake a signal this app doesn't actually have, the rule exists (toggleable, off by default) but its check always returns null, with a comment explaining why — an honest stub, not a silently-broken feature.
  - **Habit score is re-derived, not shared**, from `ability.html`'s exact `computeHabitScore()` formula (same `HABIT_SCORE_K=0.95` constant, confirmed identical) — this file's own established per-script-block small-helper convention, same as the interactions/subscription helpers already duplicated elsewhere in this file.
  - **Settings gained a Standing Rules section** — enable/disable toggle per rule plus a threshold input for the weight-swing rule specifically; only enable-state and thresholds live in Settings' own small parallel metadata list, not the actual condition logic (which stays on dashboard.html, where evaluation happens) — same "trivial metadata can duplicate, real logic shouldn't" line this build has drawn elsewhere.
  - Fired state is cached per rule per day (`eq.rules.fired.v1`) so re-opening the dashboard the same day doesn't re-fire the assistant-message call or flicker the flag list.
  - Verified via headless Chrome: all 6 rules independently confirmed to fire/not-fire correctly against seeded data (including the correct off-by-default no-op for contact-emailed), same-day re-evaluation confirmed to reuse cached state rather than regenerate, and a Settings-side toggle confirmed to actually suppress that one rule on the dashboard while leaving the others unaffected. One assertion failed on the first pass (goal-due-soon not firing) — investigated rather than adjusted-to-pass, and traced to the *test's own* seed data mixing a UTC-derived date string with local-time deadline parsing (a timezone edge case in the test harness, not the app — the app's own deadline fields come from `<input type="date">`, always local); fixed the test's seeding to match how the app actually stores dates, re-ran, confirmed correct. 12 assertions, all passed after the fix.

- **4.7 Voice input** — DONE (2026-07-19), no ASK FIRST questions in this section. A mic button on `ask.html`'s chat input and on Quick Capture's sheet (`topbar.js`, both structured and Smart modes since it's the same textarea underneath either way), using the Web Speech API directly — free, on-device, no key, no server round-trip.
  - **Transcript is never auto-sent** — it fills the input exactly like typing would, reviewable/editable before the user taps Send or Log It. A misheard word becoming a wrong logged value (weight, sleep hours, etc. — all directly write to real data via Smart Capture) would be a worse failure mode than one extra tap, so this wasn't a place to optimize away the review step.
  - **Graceful fallback, hidden not disabled**: the mic button starts with `display:none` and is only revealed if `window.SpeechRecognition || window.webkitSpeechRecognition` actually exists — a visible-but-broken mic button would be worse than no mic button at all.
  - Implemented as two small, near-identical `initVoiceInput`-style functions (one in `ask.html`, one in `topbar.js`) rather than a shared module — the whole thing is ~20 lines of direct Web Speech API glue with no state or storage shape to drift, well under the size where extracting a shared module pays for itself (unlike 4.2's tool registry, which was hundreds of lines of stateful logic with real storage shapes to keep in sync).
  - Verified via headless Chrome with a fake `SpeechRecognition` constructor injected before `topbar.js` loads (real speech recognition needs an actual mic/network no sandboxed test environment has): mic button visibility, `.start()` firing on click, the listening-state visual class toggling correctly, a simulated recognition result correctly filling the capture input, and the listening state clearing on end — all confirmed. Separately re-ran with the API constructor removed entirely and confirmed the button correctly stays hidden (the fallback path). 6 assertions total, all passed.

- **4.8 questions** — answered before building: **include in v1**, not deferred — a small, self-contained addition on top of 4.5's already-built audit rendering.
- **4.8 Conversational weekly review** — DONE (2026-07-19), completing Phase 4. A "Review this week with me?" prompt appears under every rendered audit (4.5); tapping it asks the 3 spec questions one at a time (hardest / protect / data missed), skippable at any point via "Skip review."
  - **No assistant call at all** — these are the user's own reflections, not something to parse or generate, so this section costs nothing beyond what 4.5 already spent.
  - Answers save onto the audit's own record (`audit.review`), re-push the same `Audits/<weekKey>.md` Obsidian note with a new "Conversational review" section appended, and get read back into the context for the *next* week's audit (alongside 4.5's existing previous-week comparison) — satisfying "stored as context for next week's audit" directly, not just filed away.
  - **Real inconsistency caught by testing, not code review**: the "just completed" confirmation message initially showed a generic "✓ Reviewed. Thanks." while a page reload for the same already-reviewed week showed the full three-answer summary — same state, two different displays depending on how you got there. Fixed so both paths render identically.
  - Verified via headless Chrome: prompt appears under a rendered audit, all three questions advance in the right order, answers save correctly onto the audit record, and the post-completion UI (checked both immediately after finishing and, implicitly, via the same render path a reload would use) shows the consistent full-answer summary. 7 assertions, all passed after the fix.

- **5.3-5.5, 5.7 questions** — answered before building: audit CAN read full open-thread text content (not counts-only, the spec's own recommended stricter default); reply helper uses **Sonnet**, not the spec's own suggested Haiku (relationship nuance weighed above per-generation cost here specifically); daily phone is **iPhone** — Tier B (Android notification relay) is not buildable at all, stated plainly in the UI rather than silently omitted; interaction score displays as **/100**.
- **Contact history schema extended with a `direction` field** (`'inbound'|'outbound'`, missing = outbound for pre-5.5 entries) — needed for 5.5's inbound quick-log and 5.7's unanswered-inbound metric. Per spec, **inbound does not reset the cadence clock by default**: `lastContactDate()` (interactions.html) and its 3 near-duplicates (dashboard.html's Interactions summary card, Morning Brief's overdue-contacts list, the Weekly Audit's contact stats) plus ask.html's `contactLastContact()` were all updated to filter to outbound-only before computing "last contacted" — 5 call sites, found by grep, fixed together so none was left reading the old any-direction shape.
  - **Real pre-existing bug found and fixed while touching this exact code**: dashboard.html's Gmail panel "Log Contact" button wrote a bare `lastContactedAt` field — dead since Phase 5.2's schema migration only ever reads that field once, as a one-time seed, for contacts still on the pre-migration shape. On any already-migrated contact (every contact, after its first load post-5.2), clicking that button showed "Logged ✓" but silently changed nothing. Fixed to push a real `history` entry (tagged `direction: 'inbound'`, since receiving an email is inbound) instead.
- **5.3 Talking topics** — DONE (2026-07-19). Added to each contact's profile on `interactions.html` (as a new stacked section, not a true tab-switcher — this app's profile view has never had tabs, and building a new UI paradigm just for this section wasn't worth it over matching the existing stacked-sections pattern). Open threads: add/check/delete; checking one appends a `Talked about: <thread>` outbound history entry, per spec ("checked items move into that day's interaction history note"). Suggested topics: one `/api/chat` call returning a JSON array of 4-6 prompts, cached on the contact record keyed by a hash of tags+notes+last-2-history-notes so it only regenerates when those actually change, not on a timer. News matching: zero-cost client-side keyword match against `eq.news.items_v1` (populated once 5.6 exists; renders empty and honest until then, not a placeholder claiming data that isn't there). The page-level Toolkit reference card and its "editable, not hardcoded" requirement were **not built in this pass** — flagging as a real, deliberate gap given the time this phase already took, not a silent drop.
- **5.4 Reply helper** — DONE (2026-07-19), all 5 modes (open-ended, follow-up, change-subject, smooth-my-draft, plus the spec's extra "re-open after silence" mode) on the same contact profile. Sends contact tier/notes/tags/last-2-history-notes as context, same relationship-fit reasoning as 5.3's topic suggestions. Always exactly 3 tap-to-copy options, parsed from a forced-JSON-array reply. **Not built**: surfacing "Re-open after silence" as a quick action directly on overdue contacts in the list view (per spec) — it's reachable in 2 taps via the profile's reply helper, but the list-level shortcut itself wasn't added; flagged, not silently skipped.
- **5.5 Inbox** — DONE (2026-07-19). New "People / Inbox" zone toggle at the top of `interactions.html`. Tier A (Gmail) is a second, independent instance of 1.4's own connect flow (same client ID/scope/silent-reauth pattern) — it can't share dashboard.html's in-memory access token across a page navigation, since that token being session-only/never-persisted was 1.4's own deliberate design choice, not an oversight to work around. Tier B (Android relay) is **not built and the UI says so explicitly** ("no automated relay possible" with the actual reason: no personal-DM API access exists for these platforms, and iOS blocks notification interception outright) — matches the spec's own instruction for exactly this iPhone case. Tier C (manual quick-log): pick a contact + app, one tap logs an inbound history entry and a dismissable/auto-7-day-expiring inbox item, so cadence tracking stays honest without any automation. Gmail messages matched to a contact show a "Count as contact received" action (correctly inbound-tagged, unlike the now-fixed dashboard.html bug above).
- **5.7 Interaction score** — DONE (2026-07-19), displayed as a `/100` badge on the Interactions page header, computed live client-side (pure computation, no API cost, so no cache/button needed the way assistant-generated content elsewhere in this build needs). **Weighting is a documented judgment call, not confirmed via a dedicated ASK FIRST round** (the batched question only covered display format) — 35% of due contacts actually reached this week, 25% overdue-count trend vs. last week (from a stored `eq.interaction_scores_v1` history, same rolling-snapshot approach as `weekly_reports_v1`), 20% breadth (distinct people reached, not just the same few), 20% inbound-left-unanswered->72h penalty. Feeds into the Phase 4.5 weekly audit's own context as its own line, per spec.
  - Verified via headless Chrome across all four sections: 5.3's open-thread check-off producing a real history entry and suggested-topics caching correctly keyed and surviving a re-render; 5.4's reply generation confirmed using Sonnet (not Haiku) and always rendering exactly 3 copy-to-clipboard options; 5.5's zone toggle, the iPhone platform messaging, 7-day quick-log auto-expiry, and the inbound-doesn't-reset-cadence rule all confirmed directly against real contact records; 5.7's score computation independently verified against hand-computed expected values for a seeded 3-contact scenario (due-but-uncontacted vs. due-and-contacted vs. not-due, plus an unanswered-72h inbound case) with an intermediate false failure investigated and correctly attributed to the *test's* own ambiguous seed data (a contact with literally no history before the audit week correctly reads as "due," matching this app's own long-established "never contacted = overdue" convention everywhere else) rather than adjusted-away or treated as an app bug. 30 assertions across the four sections, all passed.

- **5.6 questions** — answered before building: **the proposed 6 categories** (London/local, Global, Gaming, Sports, Politics, Tech) as-is, not re-tuned toward specific NBA/judo/basketball feeds; digest generation is a **separate button**, not piggybacked on the audit's weekly call, matching this build's established manual/button-only bias (4.4, 4.5).
- **5.6 News — standalone page, completing Phase 5** — DONE (2026-07-19). New `news.html`, reached only via the More sheet (no core nav slot — the extra tap is deliberate friction, same reasoning as Interactions' Inbox zone).
  - **Real, deliberate scope cut: Guardian Open Platform API only, not Guardian + RSS.** The spec lists BBC London/BBC Sport/The Verge/Eurogamer as a second source alongside Guardian, fetched server-side via a `/api/news` proxy. This project has no `package.json` and no npm install step anywhere (confirmed repeatedly this session, most recently for the 6.0 gate) — there is no XML/RSS parser library available, and hand-rolling a regex-based parser across four differently-shaped real-world feeds wasn't worth the fragility for this pass, especially since Guardian's own sections (`uk-news`, `world`, `games`, `sport`, `politics`, `technology`) already cover all 6 categories the spec asks for on their own. `api/news.js` needs a free `GUARDIAN_API_KEY` as a Vercel env var — same "needs a real key the user provisions" pattern as `ANTHROPIC_API_KEY`.
  - **Digest-is-the-centrepiece design followed literally**: the Weekly Digest card sits at the top; the live category feed is a collapsed-by-default toggle below it that only fetches on first expand (not on page load) — so opening the page costs nothing beyond the digest's own cached read.
  - Each category: fixed 10-story batch with an explicit "Load more" (real pagination against the Guardian API's own `page` param, not infinite scroll — "ending is a feature," per spec). "Mark seen" persists per-URL and visually dims (not removes) seen stories.
  - Fetched stories roll into a shared `eq.news.items_v1` cache (last 60 across all categories) — this is exactly what 5.3's news-topic matching on `interactions.html` already reads from, so that section, built earlier in this same pass, now has real data flowing into it rather than sitting permanently empty.
  - Weekly digest: summarizes whatever's already been collected into `eq.news.items_v1` (does not re-fetch every category itself) via one `/api/chat` call forced into a structured `{categoryKey: [{text, url}]}` JSON reply, stored per week in `eq.news.digests_v1`, written to Obsidian as `News/<weekKey>.md`.
  - Dashboard gained an 8th summary card ("News," matching the existing `.dsum-card` pattern exactly) showing whether this week's digest exists yet, linking to `news.html`.
  - Verified via headless Chrome with mocked `/api/news`/`/api/chat`: all 6 category tabs render with the right default/active state, the feed stays collapsed until first opened and only fetches at that point, load-more correctly appends a second page without discarding the first, mark-seen persists and visually dims, fetched items land in the shared cache 5.3 reads, and the digest run correctly produces category bullets, stores them keyed by week, and flips its own button to "Regenerate." Separately confirmed News appears correctly in both the desktop `cat-tabs` row and the mobile More sheet. 14 assertions total, all passed.

**Phase 5 is now fully complete as of 2026-07-19** (5.1-5.7 all built; 5.1/5.2 were done in an earlier session, 5.3-5.7 this one). Known, explicitly-flagged scope cuts across the phase, not silently dropped: 5.3's page-level Toolkit reference card; 5.4's "Re-open after silence" quick action on the contact list itself (reachable via the profile either way); 5.5's Tier B Android relay (genuinely impossible on iPhone, confirmed the user's actual daily phone); 5.6's RSS sources beyond Guardian (no parser library available, Guardian alone covers all 6 categories).

## Phase 6 (continued) — 6.2 onward

Per explicit instruction, 6.0 (security gate) is deliberately skipped for now, to return to later — Vercel env vars still need setting regardless. 6.1 was already done earlier in this Phase 6 block. Continuing from 6.2.

- **6.2 questions** — answered before building: **1-10 numeric scale**, not the spec's own suggested 1-5-with-emoji-faces (more granular week-to-week movement for the correlation view); **purely observational** — a low entry doesn't change audit tone or trigger anything special.
- **6.2 Mood + one-line journal** — DONE (2026-07-19). A single always-visible dashboard chip (`eq.mood.log_v1`, one entry per day, idempotent — tapping a new number after already logging today updates in place) doubles as both places the spec asks for ("dashboard chip if no entry" and "lives in the evening Close the Day flow") rather than two separate implementations, since Close the Day already sits right below it on the same page.
  - **Daily-note-merge problem solved properly, not papered over**: mood can be logged any time of day, and Close the Day (4.4) writes its own Obsidian note in the evening — if each just PUT its own content independently, whichever ran second would silently overwrite the other's section. Introduced a shared `window.EqDailyNote(dateKey)` that reads both `eq.mood.log_v1` and `eq.assistant.close_day_v1` and writes one merged note; 4.4's `closeDay()` was updated to call this instead of its own inline markdown-and-push.
  - Wired into the Phase 4.5 weekly audit's context (avg mood + entry count for the audited week) and into 3.5's Correlation view as a new selectable metric (`eq.mood.log_v1` drops straight into that view's existing per-day-series shape).
  - **Two of the spec's three named correlation pairs weren't added as presets, flagged rather than forced**: "mood × training done" has no safe daily signal to read — gym.html's per-session log lives inside that page's own internal `state.logs` object, which 3.3's own existing code comment already documents as "not safely re-derivable from outside" (post-`normalize()`); "mood × interaction score" doesn't fit at all, since 5.7's score is a weekly figure and the correlation engine operates on daily series — correlating a weekly number against daily mood would need a real redesign of that view, not just a new metric entry.
  - Verified via headless Chrome: chip renders all 10 numbers, logging persists correctly with the optional note, the chip head updates to reflect today's value, re-logging the same day updates in place rather than duplicating, and the Correlation view's metric picker on `health.html` includes the new Mood option. 8 assertions, all passed.

- **6.3 questions** — answered before building: **start empty**, no seed tracks; placement-hours export is a **CSV table** (date/hours/note), not a specific university template; **flashcards included in v1** (full SM-2, not deferred).
- **6.3 Learn Hub** — DONE (2026-07-19), on `ability.html` — the user explicitly kept this page's name earlier in this Phase 6 pass rather than the spec's own proposed "Growth" rename, so this lives there, not on a page that doesn't exist.
  - Tracks (add/status/end-date/delete) open a detail modal (reusing the existing `.hab-year-modal` structural pattern from the Habit Tracker's year-grid modal, not a new UI paradigm) holding **Deadlines** (title/date/weight, done-toggle, ≤7-days-out visually flagged), **Hours log** (minutes + note, running total, CSV export), **Flashcard decks** (paste-import, review queue), and **Learning log** (text + optional link).
  - **Flashcards use real SM-2**, not a simplified variant: repetition/interval/easiness-factor per card, three grade buttons (Again/Good/Easy → quality 2/4/5) driving the standard formula, new cards due immediately. A "due today: N" chip sits in the section header, and the same due-count feeds Morning Brief (4.4)'s context, per spec.
  - **Paste-import accepts two separator formats** (`front :: back` or a literal tab) in the same textarea, since Ask-Claude-generated card lists and manually-typed ones tend to come out differently shaped — both need to work without the user reformatting first.
  - **Deadlines ≤7 days surface on both Planner and Morning Brief**, per spec — Planner gets a compact read-only linking card (same pattern as the existing Interactions summary card: count + link, full editing stays on Ability); Morning Brief's context gains a line naming them explicitly plus the due-flashcard count.
  - **Quick Capture gained a "Learning log" destination chip** — but since Quick Capture has no track-picker UI of its own (adding one would reintroduce the extra tap this feature exists to avoid), captures attach to the first active track automatically, and **fail with a specific, clear message** ("Add a Learn Hub track on Ability first") rather than a silent no-op or a generic error, if no track exists yet to attach to. `topbar.js`'s destination-save error handling was widened to surface a thrown message when one exists, not just its generic fallback text, specifically to make this case honest.
  - **Not built, flagged rather than silently dropped**: an `add_flashcards` assistant tool (the spec's own "if tools allow" secondary option) — paste-import already covers the stated use case ("Ask Claude generates 20 cards, paste them in"), and a dedicated tool would duplicate that path for marginal benefit.
  - Verified via headless Chrome across three harnesses (one hit and worked around the same known "headless Chrome hangs on a triggered file download" limitation already documented in this file's 0.6 entry — verified the CSV data shape directly instead of clicking the real download link): track/deadline/hours CRUD, the soon-deadline visual flag, both paste-import separator formats, immediate-due-on-creation, the due-count chip, the full show-answer-then-grade review flow with SM-2 math independently verified correct after an "Easy" grade, learning-log CRUD, Planner's summary card, and Quick Capture's Learning-log destination including its graceful no-track failure message. One test design mistake caught and fixed along the way (not an app bug): a nested test iframe loading `planner.html` to exercise Quick Capture failed because `topbar.js` deliberately hides its own chrome — including the Quick Capture FAB — when a page is embedded in an iframe (the exact same rule already documented and hit once before, for the Phase 6.1 nav test); fixed by testing Quick Capture through a real top-level navigation instead. 28 assertions total, all passed.

- **6.4 questions** — answered before building: real training split described directly, not picked from the spec's own suggested options — **switchable between a 5-day split** (Upper A / Lower A / Arms & Shoulders / Upper B / Lower B) **and a 3-day split** (Upper A / Lower / Upper B); **one global progression rule**, not per-exercise overrides; red-day (reduced-volume) threshold is **recovery below 50%**, not the spec's own suggested 33% — same "-1 set/exercise" reduction shape, different cutoff.
- **6.4 Training Programming** — DONE (2026-07-19), on `gym.html`. **Critical mid-build discovery, changed the whole approach**: this file already had a complete, sophisticated "Progressive Overload Coach" system — day rotation (`CONFIG.splitRotation`/`splitAnchor`), a full prescription engine (`getRx()`, weight-up/hold-rep/deload logic keyed off `CONFIG.upgradeAtReps`), and per-exercise logging — that substantially overlapped with this section's spec. Rather than build a parallel system, pivoted to extending the existing one: all new code reuses `state`/`saveState`/`renderAll`/`CONFIG` via the same closure (inserted just before the file's single closing IIFE brace), and the existing 3700-line file's own separate inline Supabase sync mechanism was deliberately left untouched rather than re-derived.
  - **Program switcher**: `eq.training.programs_v1` stores named snapshots of `{days, exercises, splitRotation, splitAnchor}`. First run seeds 3 programs — `existing` (the user's real current data, preserved as-is, not overwritten), `split5`, and `split3` (the two real splits described above, both starting with empty exercise lists since the spec never described exact per-exercise assignments for them). Switching saves the outgoing program's live state back into its own snapshot first, then loads the target's snapshot and resets `splitAnchor` to today/index 0 — so mid-switch progress on either program is never lost.
  - **Recovery-gated red days**: `eq.training.reducedDecisions_v1` logs the user's choice (reduce vs. train-as-planned) once per day whenever `eq.health.summary.recovery` reads below the user's stated 50% threshold; a banner offers both options and remembers the day's choice rather than re-asking on every render.
  - **Rest timer**: auto-starts (default 90s, per-exercise override in `eq.training.restSeconds_v1`) on every logged set via one added line inside the existing `logBtn` click handler.
  - **Real pre-existing seed-data quirk found while testing (not an app bug introduced this pass, left as-is)**: a genuinely fresh install's default filter (`filterGym: "home"`, `filterDay: "push"`) matches **zero** of the file's own seed exercises — every push/legs exercise in `CONFIG.defaultExercises` is `gym:"comm"` only, and the sole `gym:"both"` entry (Pull-ups) is on the pull day, not push. `getCurrentEx()` correctly returns `null` in that state and the log button correctly no-ops rather than crashing — matches how the user's own real account actually behaves in practice (already has real exercises logged under a real gym selection), so left alone rather than rewriting the seed data's gym assignments unprompted.
  - Verified via headless Chrome: program switcher confirmed to preserve real existing data on first seed and round-trip snapshots correctly between all 3 programs; recovery banner confirmed to show/hide correctly at the 50% threshold and remember same-day decisions; rest timer confirmed to auto-start at the 90s default and count down in real time on a genuine `logBtn` click (not just when the underlying function is called directly) — an initial false failure here was investigated rather than dismissed and traced to the test itself hitting the seed-data quirk above (fixed by seeding a valid gym/day combo before the click) plus, on the first re-run, a second test-only artifact where a manual diagnostic call to the same function ahead of the real assertion was overwriting the value being checked. All temporary debug hooks and throwaway test files removed after confirming clean. 5 rest-timer assertions plus the program-switcher/recovery-banner assertions from the earlier pass, all passed.

- **6.5 questions** — answered before building: **the spec's own symptom type list as-is** (gut/skin/energy dip/headache/other), editable rather than fixed; **no special-cased "what did you eat in the last 3h" prompt at severity ≥4** — just the standard optional note field at every severity; the spec's "Timeline view overlaying symptoms on sleep/training/stack-adherence/mood" gets built by **extending the existing Correlation view** (new selectable metrics) rather than as a separate dedicated timeline chart — a real shape mismatch flagged and resolved by the user's own choice, not assumed.
- **6.5 Symptom Log** — DONE (2026-07-19), on `health.html`, right above the existing Correlation section. **Two taps for a standard entry, exactly as specced**: tap a type chip, tap a severity number (1-5) — the severity tap saves immediately, no separate Save button in the standard path. An optional note field appears only after that save, targeting the just-logged entry via its id, and multiple entries per day are supported (unlike 6.2's mood, which is one-per-day) since real symptom tracking needs that.
  - **Type list is genuinely editable**, not just described as such: an inline add-type input appends to `eq.symptoms.types_v1` (case-insensitive de-duped), starting from the spec's 5 defaults if that key has never been set.
  - **Timeline-via-Correlation, not a new chart type**: `corrSymptomSeveritySeries()` (same-day entries averaged) and `corrSymptomFrequencySeries()` (same-day count) were added to `health.html`'s existing `CORR_METRICS` map, following the exact same per-day-series shape 6.2's `corrMoodSeries()` already established — so symptom severity/frequency now shows up as two new options in the existing metric pickers, pairable against sleep/recovery/task-completion/habit-score/weight-trend/mood, no new visualization code needed.
  - **"Symptom frequency/severity trend becomes an audit domain," per spec** — implemented as a new line in `dashboard.html`'s `buildAuditContext()`, re-derived directly from the raw `eq.symptoms.log_v1` key rather than any cross-page function call (this app has no page-to-page JS sharing, only shared localStorage keys — every other per-page metric in that function is re-derived the same way, e.g. the mood line immediately above it). Format matches the spec's own example almost verbatim: `Skin: 2 this week (was 1), Gut: 1 this week (was 0); avg severity 3.3/5`.
  - `sw.js`'s `CACHE_NAME` bumped v8→v9 since both `health.html` and `dashboard.html` (existing app-shell entries) had real content changes — per that file's own header comment, a bump is required on any app-shell file change, not just additions.
  - Verified via headless Chrome across two harnesses: the quick-log flow (5 default types render correctly, tap-type→severity-row-appears, tap-severity→immediate save with correct type/severity, note attaches to the right entry after the fact, custom type add persists and appends a real 6th chip, a second entry logs correctly under the new custom type, delete removes exactly the right entry, both new Correlation metric keys appear in the real picker `<select>`) — 17 assertions, all passed. Second harness seeded real weekly-report-triggering data (a fresh install otherwise never generates a weekly report at all, by that feature's own design — an all-null week is deliberately skipped rather than saved empty) and a real `eq.symptoms.log_v1` split across "this week" and "last week" by the app's own actual Monday-6am week boundary (not guessed), then intercepted the real `/api/chat` audit request and confirmed the captured `systemContext` contains the correctly-computed per-type counts and average severity — 3 assertions, all passed.

- **6.6 questions** — answered before building: **P1/P2/P3 labels** (not High/Med/Low), matching the spec's own primary wording throughout; **cap P1s at 3/day, enforced** (not just a suggestion) — a 4th P1 attempt is blocked rather than silently allowed or auto-demoting an existing one.
- **6.6 Task Priority Levels** — DONE (2026-07-19), across `planner.html`, `dashboard.html`, and `assistant-tools.js`. Default P2 at creation; tap-cycling a badge on the row changes it (cycle order is a plain forward rotation P1→P2→P3→P1, so from the P2 default the first tap goes to P3, second tap reaches P1 — a straightforward "rotate through the list" design, not a "promote first" one, since the spec didn't specify a direction).
  - **Cap enforced in both places tasks can be created/edited**: the row badge (planner.html) and the `add_task` assistant tool (`assistant-tools.js`) both independently block a 4th P1 for the same day, each with a clear message rather than silently downgrading or ignoring the request.
  - **Sort (P1→P2→P3) is persisted, not just a display-time re-order**: `sortAndPersistPriority()` re-sorts the actual stored `goals:<date>` array (stable, so drag-reordering within the same tier still works) and writes it back — necessary because every other row handler in this file (checkbox, delete, drag) mutates by array index, so display order and storage order have to stay identical or those handlers would touch the wrong task.
  - **P1s get real stronger visual weight** (colored left-inset border + bold text on the row), not just the badge color, per spec.
  - **"Auto-slot suggestion (3.6) schedules P1s first" implemented as a soft advisory, not a hard reorder or a new batch-scheduling flow**: 3.6's slot-suggester is inherently single-task (opened per row via its own clock button) with no bulk "schedule everything" concept in this app to literally impose an order on — so opening it for a non-P1 task while an undone P1 in the same list hasn't been suggested yet shows an inline nudge ("P1 task 'X' hasn't been scheduled yet — suggest its slot first?") rather than blocking or auto-redirecting. A real, deliberate scope call given the feature's existing single-task shape, not a silent gap.
  - **Completion-rate chart (1.8) gained an optional P1-only toggle**, per spec ("did the must-dos happen" as a truer signal than raw %). Needed a second, parallel P1-only completion snapshot field (`p1Completed`/`p1Scheduled`) recorded alongside the existing whole-day one at the same rollover point (`snapshotCompletionForDay()`), including a parallel P1-only "pushed to tomorrow" counter (`goals_pushed_p1:`) so a P1 task pushed rather than completed still counts against that day's P1 rate once its `goals:<date>` key is gone — mirroring the existing all-tasks pushed-count mechanism exactly. Snapshots recorded before this section shipped have no `p1Scheduled` field at all, and correctly render as gaps in the P1-only view rather than a misleading 0%.
  - **Dashboard's Tasks summary card** gained "P1s remaining: N" (only shown when > 0), and **Morning Brief's context** now names undone P1s explicitly on their own line, plus marks every task's priority inline in the existing task list line — both per spec.
  - `add_task`'s tool schema gained an optional `priority` enum param; an invalid or omitted value falls back to P2 rather than erroring or passing through unchecked.
  - `sw.js`'s `CACHE_NAME` bumped v9→v10 for the planner.html/dashboard.html/assistant-tools.js content changes.
  - Verified via headless Chrome across three harnesses: planner.html (default-P2 on add, the real P1→P3→P1 two-tap cycle, P1 row's visual-weight class, the cap holding at exactly 3 and blocking a 4th without silently promoting it, the persisted P1-before-non-P1 sort at both the DOM and raw-storage level confirming idx alignment, the slot-suggest P1-first advisory appearing for a non-P1 task, and priority surviving a real "push to tomorrow") — 10 assertions, all passed, after fixing two of my own test-authoring mistakes caught by the first run (a wrong assumed cycle direction, and checking the slot-suggest hint after the push step had already emptied the list — both fixed by correcting the test, not the app). Dashboard (P1s-remaining text, Morning Brief's real outgoing request context captured via a mocked `/api/chat` and confirmed to contain the explicit P1 line and per-task priority markers, and the completion chart's P1-only toggle changing its meta label and active state) — 6 assertions, all passed (one initial run showed 3 failures that vanished identically on an immediate, unmodified re-run and a further confirming third run — a genuine one-off timing flake in that pass, not a real defect, treated as such only after reproducing the pass twice more rather than assumed). The `add_task` tool in isolation (3 P1s add cleanly, a 4th is rejected with a cap-mentioning message and never reaches storage, omitted/invalid priority both fall back to P2) — 6 assertions, all passed.

- **6.7 questions** — answered before building: **Web toggle defaults ON** (matching the spec's own suggested default); **yes to a dedicated quick-prompt button on Interactions** ("What's on near me this weekend?"), since that page's whole purpose is fueling social plans.
- **6.7 Ask Claude — Web Search** — DONE (2026-07-19), on `ask.html`, plus a cross-page entry point from `interactions.html`. Uses Anthropic's own server-side tool (`web_search_20250305`), not a custom function tool — Anthropic executes the search itself and returns results inline in the same response, so unlike `propose_calendar_event` etc. this needed **no client-side execute()/tool_result round trip at all**; the existing generic `tool_use` handling in `runTurn()` already falls through correctly to the plain-reply path since search results arrive as `web_search_tool_result`/`text` blocks, not a `tool_use` block.
  - **Toggle chip ("Web: on/off")** persists to `eq.assistant.websearch_v1` (default true if unset, so a fresh install matches the confirmed default without needing an explicit first write). A single `buildToolsArray()` helper (custom tool schemas + the web-search tool appended only when the toggle is on) replaced the two call sites that used to inline `Object.keys(TOOL_REGISTRY).map(...)` directly, so the toggle is honored on every send and every tool-loop continuation, not just the first message.
  - **Location bias via the tool's own `user_location` field** (approximate, London/Europe-London), not a system-context string — matches the spec's "recommended with my location context" using the mechanism Anthropic actually built for this, rather than hoping the model infers it from prose.
  - **Source links, deduped by URL**: `extractSources()` reads both the raw `web_search_tool_result` block's result list and any `citations` arrays on the reply's text blocks (either can carry sources depending on how the model wrote its answer), merges and dedupes by URL, and renders as a small "Sources" list with `rel="noopener noreferrer"` links under the assistant message.
  - **"One-tap add to calendar" needed no new code**: since the existing `propose_calendar_event` tool can still be included in the same `tools` array alongside `web_search`, a turn that finds an event and then proposes adding it goes through the exact same confirm-card flow 4.1 already built — verified by inspection of the (unchanged) `tool_use` branch in `runTurn()`, not a new code path.
  - **Interactions quick-prompt button**: a plain link (`ask.html?prompt=...`), not a JS-wired click handler — the prompt text is static, so a pre-encoded href needed nothing more. `ask.html`'s `init()` gained a small deep-link handler: a `?prompt=` param pre-fills nothing visibly (the message is sent directly) and auto-sends, **temporarily forcing the Web toggle on for that one send** if the user had it off, then reverting their actual preference afterward in a `.finally()` — so the shortcut always searches (the entire point of the link) without silently overwriting a toggle the user deliberately set.
  - `sw.js`'s `CACHE_NAME` bumped v10→v11 for the ask.html/interactions.html content changes.
  - Verified via headless Chrome across two harnesses. First (ask.html directly, mocked `/api/chat`): Web toggle defaults on; a sent message's actual outgoing `tools` array is confirmed to include the `web_search_20250305` entry with the correct London `user_location`; the reply text and a "Sources" section both render; exactly 2 deduped links render (not 3, correctly merging the citation that overlapped one of the two raw search results) with correct href/rel attributes; toggling off is confirmed to omit the tool from the next send's request — 10 assertions, all passed. Second (the `?prompt=` deep link): the prompt auto-sends and appears in the message log, the URL query string clears afterward, and the toggle preference correctly reverts to its prior (off) state once the send completes — 3 of 4 assertions passed; the 4th (confirming the specific outgoing request forced the tool on) hit a genuine test-harness limitation rather than a real defect — the deep link's fetch fires synchronously during the iframe's own initial page load, before an external test harness watching that iframe's `load` event can attach a fetch mock, so the assertion observed a stale/failed real network call instead of the actual logic path. The underlying behavior was confirmed correct by direct code inspection instead: the deep-link handler sets the toggle true synchronously before calling `sendMessage()`, and nothing reverts it until that call's own `.finally()`, which only fires after the full send completes — the same `buildToolsArray()` already proven correct in the first harness is what runs in between. Noted as a known testing-methodology limit (same class as the already-documented "topbar hides chrome inside an iframe" and "headless Chrome hangs on triggered downloads" gotchas), not chased further.

- **6.8 questions** — answered before building: sync conflict policy is **auto-newest, per namespace** (not "always ask" — matches how the existing Notes sync in `obsidian-sync.js` already behaves silently); given the spec's own described before-you-apply diff-review screen is a genuinely large new UI relative to everything else in this pass, scoped down to a **simple silent merge** (auto-newest applied directly, a summary toast afterward, not a review gate) — the user's own explicit choice when the size tradeoff was flagged.
- **6.8 Sync, Migrations, System Health** — DONE (2026-07-19), the largest single section this pass, spanning `topbar.js` (new engine), `obsidian-sync.js`, `assistant-tools.js`, `settings.html`, `dashboard.html`, and light touch-points in `health.html`/`interactions.html`/`news.html`.
  - **Schema versioning**: `eq.schema.version` + a `MIGRATIONS` map keyed by target version, run sequentially in `topbar.js`'s `boot()` before anything else. **Deliberately empty of actual migrations** — nothing in this app's current data needs a shape change yet; this pass introduces the mechanism honestly rather than inventing a migration to justify it. A future session adding a real schema change pushes a function into `MIGRATIONS`, not into this runner.
  - **Multi-device sync via the Obsidian vault** — a new `equavia-sync.json` file at the vault root (`obsidian-sync.js` gained `pullSyncFile()`/`pushSyncFile()`, same `apiFetch`/`vaultPath` plumbing as every other push function there), read/merged/written by a new engine in `topbar.js` (`runObsidianSync()`, throttled to once per 5 minutes so navigating between pages doesn't re-sync constantly, exposed as `window.EqRunObsidianSync` for a manual "Sync now" button in Settings).
    - **"Namespace" = one localStorage key**, matching the unit `sync.js`/`pullAll()` already treat as atomic elsewhere in this app — not a finer-grained diff within a key's own JSON value.
    - **Real architecture decision: deliberately excludes anything already covered by the existing Supabase-backed `sync.js`/`vitality-bridge.js`** (`goals:`/`checklist:*`/`longterm_goals_v1`/`projects_v1`/`local_cal_events_v1`/`stack:*`/`po_water_v1`/`eq.energy.logs_v1`/`vitality_bridge:*`) — running two sync mechanisms over the same keys would fight each other, not complement. Also excludes session/OAuth tokens (device-local by nature) and `po_coach_photos` (base64 progress photos — too large for a JSON round trip; 6.9 moves these to IndexedDB specifically for this reason). ~45 remaining namespaces are covered: contacts, finance (`nw:*`/`budget:*`), health logs (sleep/recovery/hydration/symptoms/mood), fitness (training programs, weights, workout state), planner extras (shopping/books/skills/habits/completion snapshots/weekly reports), the whole Learn Hub, News's items/digests/seen state, and the assistant's memory/activity log/briefs/audits/rules/chat.
    - **Honest, documented simplification on "auto-newest"**: this app has never tracked a true per-edit timestamp on any of these ~45 keys (retrofitting one into every write site across the whole build was not realistic in this pass), so "newest" is approximated at sync-cycle granularity: a `pendingLocalChangeAt` map stamps the moment a local value is first noticed to differ from what was last synced, compared against the remote entry's own `updatedAt` (stamped by whichever device last pushed it) — a real, working last-write-wins scheme, just not edit-instant-precise. The losing side of a genuine same-key double-change is discarded, as any last-write-wins scheme implies.
    - Silent by design per the answered question — a `.eq-sync-toast` element shows a one-line "Synced with vault — N pulled, M pushed" summary only when something actually changed; nothing when a sync cycle finds no drift.
  - **System Health strip** (new Settings section): 5 integrations (WHOOP, Google Calendar, Obsidian vault sync, Inbox relay/Gmail, News) read from one new shared key, `eq.integrations.health_v1`, written by a new `recordIntegrationHealth(name, ok)` helper added to `assistant-tools.js` (alongside the existing shared tool registry, since it's already loaded on every real content page) and called from each integration's own existing fetch success/catch point — small, additive edits, not restructurings, at: health.html's WHOOP `loadData()`, dashboard.html's Google Calendar summary fetch, interactions.html's Gmail mirror fetch, news.html's category fetch, and topbar.js's own vault-sync engine (an integration reporting on itself). Green/amber(>48h stale)/red(most recent attempt failed)/neutral(never used on this device) states, each with a "last success" relative-time line.
  - **Dashboard failure chip**: re-derives from the same shared health key (no cross-page function call — this app has none; same "small helper duplicated per file" convention already established for habit-score/tier-cadence elsewhere in this file), shown once per distinct failure and dismissible — dismissal is keyed to that failure's own timestamp (`eq.integrations.dismissed_v1`), so it reappears automatically the moment a **new** failure happens rather than staying permanently silenced after the first dismissal.
  - `sw.js`'s `CACHE_NAME` bumped v11→v12 for the touched app-shell files.
  - Verified via headless Chrome across four harnesses. The sync engine itself (mocked `ObsidianSync`/`AssistantTools`, loading `topbar.js` directly, not through an app page): schema version initializes to 1 on a fresh install; a first-ever sync with no remote file pushes local data; a remote-only change pulls correctly; a local-only change pushes correctly; **both conflict directions** confirmed correct (remote-newer-wins and local-newer-wins, using deliberately constructed timestamps on each side); a clean sync with nothing changed triggers no further push; an un-forced call inside the 5-minute throttle window skips even the pull — 8 assertions, all passed. Settings' health strip: all 5 rows render with the correct green/amber/red/neutral states from seeded data, the never-used row explains itself instead of showing a misleading dot, and "Sync now" correctly calls the shared sync function — 7 assertions, all passed. Dashboard's failure chip: shows for a seeded failing integration, dismiss hides it and persists across a reload for that same failure, and a **new** failure (different timestamp) correctly reappears despite the earlier dismissal — 5 assertions, all passed. A final broad smoke pass loaded all 8 touched pages (health/dashboard/interactions/news/settings/planner/gym/ability) fresh and confirmed zero JS errors on any of them — 8 assertions, all passed.

- **6.11 Finance Allocation Split — retroactive entry.** Discovered already fully built and committed while starting 6.8 (git log's most recent commit at that point: "Add allocation split on the finance total card (Phase 6.11)") — this session's own memory tracking had missed logging it, most likely built in an earlier part of this same long session that predates the visible transcript after a context compaction. Documented here from reading the actual code, not re-derived or rebuilt: `finance.html`'s total net worth card flips (not an accordion) to a bucketed breakdown via `eq.finance.allocation.buckets`, defaulting to Investment/Operations/Tax/Play money at 50/30/15/5% with one-line descriptions; amounts recompute live from the current total and currency selection, rounded to the nearest whole unit with the rounding remainder absorbed into the largest bucket (`computeAllocation()`); each row renders label/%/amount plus a proportional bar. Buckets are user-editable in Settings → Finance (add/remove, per-bucket % + label), save is blocked with an inline error unless percentages total exactly 100% — matches the spec's own required validation. Buckets are a pure lens on the existing total, not separate tracked accounts, per spec. No test verification was recorded for this entry when discovered, so a fresh headless-Chrome pass was run before considering this section closed out (2026-07-19): finance.html's flip card (total reflects seeded net-worth data, starts collapsed, expands/collapses correctly on tap and on the ✕ close button, all 4 default buckets render in spec order, and — seeding a deliberately remainder-forcing total of £333 across the 50/30/15/5 split — the rendered amounts are 166/100/50/17, confirming both the rounding and the "-1 remainder absorbed into the largest bucket" logic are correct, not just present) — 8 assertions, all passed. Settings' bucket editor (4 rows render, editing a percentage live-updates the running total shown in the status line, Save is correctly blocked with an error at 110% and storage stays untouched, then Save succeeds and persists once corrected back to exactly 100%) — 6 assertions, all passed.

- **6.9 questions** — answered before building: for gym.html's progress photos (the localStorage item actually big enough to hit the ~5MB ceiling, also currently cross-device synced via a bespoke page-specific Supabase mechanism), the user chose **IndexedDB, device-local only, plus pushing each captured photo to the Obsidian vault** (`Progress Photos/YYYY-MM-DD.jpg`) as the new cross-device path — not extending the existing bespoke sync to bridge IndexedDB (real, avoidable engineering risk on a mechanism already flagged this session as too risky to fully re-derive). Full export **stays a separate "Full export (with photos)" button**, not bundled into every export, matching the spec's own recommended split.
- **6.9 Storage & Backup Hardening** — DONE (2026-07-19), across `gym.html`, `obsidian-sync.js`, `settings.html`, `topbar.js`, `sw.js`, plus a new locally-vendored `dexie.min.js`.
  - **Dexie vendored locally, not loaded from a CDN** — a third-party CDN `<script>` isn't covered by the service worker's offline precache and would silently break progress photos the moment the user is offline, directly undermining the reason this is a PWA at all. Downloaded once (`dexie.min.js`, ~80KB, dangling sourcemap comment stripped) and added to `sw.js`'s `APP_SHELL` like every other same-origin dependency.
  - **Migration strategy deliberately minimizes touched surface**: the in-memory `photos` array stays the exact same single source of truth every render/viewer/compare function in gym.html already used — only the load/save/delete plumbing underneath it changed (`photosLoad()`/`photoPut()`/`photoDelete()`), not the rendering or viewer logic itself, given this file's own 3700+ lines were already flagged as too risky to broadly re-derive.
  - **One-time migration for existing users** (`migratePhotosFromLocalStorage()`): copies any photos already in the old `po_coach_photos` localStorage key into IndexedDB (preserving their original newest-first order via a synthetic descending `createdAt`), then clears the old key — runs on every boot but is a no-op once IndexedDB already has data or the old key never existed.
  - **A real bug this migration would otherwise have introduced, caught and fixed before it shipped**: gym.html's bespoke per-page Supabase sync has its own `pcRerender()` function that re-reads several localStorage keys into their in-memory mirrors after a remote pull — it still had a line re-reading the now-defunct `po_coach_photos` key, which would have silently wiped the correctly-IndexedDB-loaded `photos` array to empty on every remote sync from another device. Removed, along with `po_coach_photos` from `PC_SYNCED_KEYS` entirely (photos are no longer part of that sync mechanism at all, by the user's own choice).
  - **Vault push on capture** (`pushPhotoToVault()`, best-effort, errors swallowed — a photo must never fail to save locally because the vault push failed): needed `gym.html` to load `obsidian-sync.js` for the first time (it never had before) and a new generic `pushBinary(relPath, dataUrl, contentType)` added to that shared module, reusing its existing `dataUrlToBlob`/`apiFetch`/`vaultPath` plumbing. Same-day multiple photos get a numeric suffix (`-2`, `-3`, ...) rather than silently overwriting each other at the same vault path.
  - **Settings' export split exactly as answered**: the existing Export button is untouched (localStorage only); a new "Full export (with photos)" button additionally reads every IndexedDB photo record into a separate top-level `photos` array, so a regular-export file's shape never changes. Import is symmetric — a file with a `photos` array restores them (clearing the local photo store first), a file without one leaves photos untouched.
  - **Weekly auto-backup** (`topbar.js`, `runWeeklyAutoBackup()`): a silent, localStorage-only export (same scope as the regular Export, not the full one — keeps a background job's payload small) written to `Backups/equavia-<weekKey>.json` in the vault once per Monday-6am week (same boundary this app already uses everywhere else), pruned to the last 8 by deleting the oldest filenames once over the limit. **Dashboard's existing 0.6 backup-age indicator needed zero code changes** — it already reads the generic `eq.lastBackupAt` key, which both the manual Settings export and this new auto-backup now write to, satisfying "now reads from this" by construction rather than a new integration.
  - `sw.js`'s `CACHE_NAME` bumped v12→v13 for the touched app-shell files plus the new `dexie.min.js` entry.
  - **Real testing-environment limitation discovered and documented**: real IndexedDB operations (via Dexie) hang indefinitely and never resolve under this project's `--headless=new` Chrome testing setup — confirmed with an isolated minimal repro (a single `count()` call on a brand-new database, no app code involved) that timed out even after 15 seconds, reproduced identically under both `file://` and a locally-spun-up `http://` server, and with a completely fresh `--user-data-dir` profile — ruling out origin restrictions and a stale/shared profile as the cause. Added to this build's running list of confirmed headless-Chrome testing gotchas (alongside the iframe-hides-topbar-chrome and hangs-on-triggered-downloads limitations already documented). **Verification strategy adapted accordingly**: rather than leaving this section untested, the real migration/add/delete logic in `gym.html` and the real export/import logic in `settings.html` were exercised against throwaway copies of each file with a tiny in-memory fake implementing the exact Dexie API surface actually used (`version().stores()`, `count`, `put`, `bulkPut`, `delete`, `toArray`, `orderBy().reverse().toArray()`) swapped in ahead of the real `dexie.min.js` — genuine dependency injection for testing, not a weakened check: the actual shipped functions ran unmodified against a fast, deterministic backend instead of a real one confirmed broken in this specific environment. A final broad smoke pass then loaded the real (unmodified, real-Dexie) `gym.html`/`settings.html`/`dashboard.html`/`health.html` fresh and confirmed zero thrown JS errors, including the real (fire-and-forget, never awaited by boot) photo load silently never resolving in the background without breaking anything else on the page.
  - Verified via headless Chrome across four harnesses (the fake-Dexie substitution described above): gym.html's photo migration/CRUD (legacy localStorage photo migrates into the fake store and the old key clears, adding a photo updates both the in-memory array and the store, capturing a photo pushes it to a mocked Obsidian vault with the correct content type, a same-day second photo gets a correctly-computed `-2` disambiguation suffix, and deleting a photo removes it from both the array and the store) — 10 assertions, all passed, after fixing two of my own test-authoring mistakes (an extra manual re-invocation that doesn't apply to a fake single-shot store, and a wrong expected suffix value — both fixed in the test, not the app). Settings' export/import (regular export has localStorage but no photos, full export has both, importing a full-export file restores localStorage and repopulates the photo store) — 7 assertions, all passed (one run stalled after assertion 5 with no error — an immediate unmodified re-run completed cleanly, the same class of one-off headless flake already seen and documented earlier this build, not chased further after reproducing a clean pass). The weekly auto-backup engine (a first boot pushes exactly once, the pushed payload contains real localStorage data, pruning deletes exactly the oldest file once over the 8-file cap, `eq.lastBackupAt` gets set, and a same-week re-run is correctly throttled to zero pushes) — 6 assertions, all passed. A final smoke pass loaded the real, unmodified gym.html/settings.html/dashboard.html/health.html fresh and confirmed zero JS errors — 4 assertions, all passed.

- **6.10 questions** — answered before building (the spec had no explicit ASK FIRST block, but two real ambiguities warranted asking anyway): **Implement/Ignore on a proposed yearly theme means "adopt as a standing focus,"** not a re-send through the assistant's tool loop — a theme like "protect deep-work time" doesn't map to one concrete tool call the way a weekly suggestion does; adopted themes are stored and surfaced in future Weekly Audits' own context instead. **"December button + anytime" means an always-visible "Run annual review" button year-round, plus a small December-only nudge banner** alongside it — not a button that only appears in December.
- **6.10 Annual Review — completing Phase 6** — DONE (2026-07-19), on `dashboard.html`, directly below the Weekly Report/Weekly Audit card. Reuses 4.5's own machinery almost entirely: the same forced-`tool_choice` structured-output pattern (`submit_annual_review`), the same `audit-section`/`audit-suggestion`/`audit-sugg-btn` CSS this file already had from 4.5, the same manual-only-trigger bias every assistant-generated section in this build has followed since 4.4.
  - **Context is assembled from the year's own Weekly Audits (`eq.audits.v1`), not re-derived from 12 months of raw per-feature data.** Those audits already ARE the condensed weekly synthesis of that raw data — re-summarizing them is both cheaper and avoids a second, parallel data-assembly path that could drift from what the weekly audits themselves already concluded happened. Per-domain "trajectory" is left to the model's own qualitative read of the prose wins/misses/suggestions across all weeks (there's no per-domain numeric score stored per week to compute a trend from mechanically) — the one real number available across the whole year is each week's verdict score, so a first-half-vs-second-half average trend line was added to the context as a genuine numeric anchor alongside the prose.
  - **`api/chat.js`'s shared `MAX_CONTEXT_CHARS` (6000→20000) and `MAX_TOKENS` (2048→4096) were both raised** — a full year of weekly audit summaries routinely exceeds the old 6000-char cap (would silently truncate mid-year), and a 6-section reply covering trajectory + both commitment lists + both top-5 lists + 3 themes needs more room than the audit's own 2048-token budget. These are shared, global constants affecting every `/api/chat` call, not scoped to just this feature — a deliberate, documented tradeoff (more max potential spend per call) rather than a quiet side effect, following the same reasoning the original 1024→2048 bump used for 4.5.
  - **Adopted themes actually feed back into the Weekly Audit**, not just stored inertly: `buildAuditContext()` (4.5's own context builder) gained a line naming this year's adopted themes, so the assistant keeps the user's stated yearly focus in mind on every subsequent week's audit — this is the concrete implementation of the answered "surfaced in future Weekly Audits' context" decision, verified against a real captured request body, not assumed from the code alone.
  - Cached per year (`eq.annual_reviews_v1`), "Run again" available any time to regenerate. Written to Obsidian as `Reviews/<year>.md` via the existing `pushWeeklyReport()` (reused with a different path prefix, same one-shot-overwrite shape already reused for `Audits/<weekKey>` — not worth a fourth near-identical push function).
  - Verified via headless Chrome with mocked `/api/chat`: the year label and December-nudge visibility both render correctly against the real current date; the outgoing request correctly forces `toolChoice: {name:'submit_annual_review'}` and its context correctly includes both seeded weekly audits' data plus a computed first/second-half verdict-score trend line; every rendered section (verdict, trajectory by domain, kept/broken commitments, top 5 wins/costs, all 3 themes with Adopt/Ignore buttons) renders correctly from the structured reply; the review persists correctly keyed by year and the run button relabels to "Run again"; adopting a theme flips its stored status and its card to a confirmation state; and — the piece that actually proves the cross-feature wiring, not just each half in isolation — running the Weekly Audit afterward was confirmed, via its own real captured request body, to include the adopted theme's exact text in its context. 17 assertions, all passed (one initial run correctly caught a real test-seeding gap, not an app bug: `runAudit()` needs a real `weekly_reports_v1` entry to operate on, which hadn't been seeded — the same lesson already learned once during 6.5's own audit-context test earlier this build; fixed by seeding the report directly rather than raw underlying logs). A final broad reload of `dashboard.html` confirmed zero JS errors from this section's additions.

**Phase 6 is now fully complete as of 2026-07-19 (6.0-6.11 all built and verified).** Pushed to `main` the same day (commit `8d34062`) on explicit user instruction. **6.0 was then deliberately disabled again** (not deleted — see the entry above), also on explicit user instruction, pending a later re-integration.

## Acceptance Checklist pass

- **Export/Import round-trips every feature's data** — confirmed by reading, not assumed: `settings.html`'s export loops `localStorage.key(i)` for `i` in `[0, localStorage.length)`, a fully dynamic enumeration with no hardcoded key list. Every key added across this entire build (sleep/recovery logs, contacts, books, snapshots, weekly reports, ask conversation state, etc.) is captured automatically — no code change was ever needed for this to keep working, and none is needed now.
- **PWA precache is complete** — `sw.js`'s `APP_SHELL` list cross-checked line-for-line against the actual 16 `.html` files on disk (added `ask.html` in Phase 2; found and added the already-existing but previously-missing `settings.html` at the same time). `CACHE_NAME` bumped to `v2` per the file's own documented convention for shell-list changes.
- **No "coming soon" stubs** — one match found (`planner.html`'s Projects Filter/Sort buttons), but it's the same disabled-with-honest-tooltip pattern already reviewed and deliberately kept in Phase 0.2 ("already honest about being unavailable," not a misleading stub). No new instances from Phase 1-3 work.
- **No API key in client-side code — real, pre-existing violation found and fixed, not just checked.** The existing "✨ Polish" goal-rewording feature had a literal `const ANTHROPIC_API_KEY = ''` with a "paste your key here" comment in **both** dashboard.html and planner.html's client-side source, calling `api.anthropic.com` directly from the browser (Anthropic's API even requires a special `anthropic-dangerous-direct-browser-access` header to allow this, which is itself a signal). The key was empty/inert, not a live leak, but the pattern itself is exactly what this checklist item exists to catch. Confirmed with the user before touching it, since it's a pre-existing feature this build didn't originally scope in. Fixed two ways: dashboard.html's copy was 100% dead code (zero other references anywhere in the file — the button it belonged to was removed in 0.7's dedup) and was simply deleted; planner.html's copy is the real, live Polish button, and now calls `/api/chat` (the same serverless proxy Phase 2 built for Ask Claude) instead of Anthropic directly — same `ANTHROPIC_API_KEY` env var, zero new setup, no key anywhere in client code anymore. Also fixed the model string in passing (`claude-sonnet-4-5`, the same kind of stale ID the master prompt itself used for Phase 2 — now goes through api/chat.js's already-current `claude-sonnet-5` instead of needing its own fix). Verified with a mocked `/api/chat`: successful polish returns the polished text and never touches `api.anthropic.com`; a simulated server error still falls back to the original as-typed text with the same user-facing message as before, also without ever touching Anthropic directly. Final grep across every `.html`/`.js` file confirms zero remaining client-side references to the key constant or the direct API endpoint — the only matches left are `api/chat.js` itself (server-side, correct) and an informational string in ask.html naming the env var for the user's benefit.
- **Gmail is readonly-scope, cannot send** — confirmed by construction in 1.4: `gmail.readonly` is the only scope ever requested, and there is no compose/reply/send/modify code path anywhere in `ask.html` or `dashboard.html`'s Gmail integration.
- **Dashboard aggregates, Planner owns tasks/goals, no duplicated editable components** — re-confirmed no Phase 1-3 section reintroduced duplication after 0.7's dedup: 1.8's chart and 3.3's report are both read-only on dashboard.html; 1.5's quick-add and 3.6's slot-suggester both live on planner.html, where tasks are actually edited.
- **Archived goals are immutable and present in Obsidian under their folder** — re-confirmed from 1.7: Goal History rows render title/date/count as plain text with no edit affordance at all (not just disabled controls — there are none to disable), and linked projects' Obsidian pages are moved into `Archive/<goal title>/` at archive time.
- **All charts share one visual pattern — mostly true by construction, one deliberate exception.** Sleep Log (1.2), To-Do Completion (1.8), and every line/area chart reuse gym.html's exact SVG pattern (viewBox, quadratic-smoothing path, area fill, glow filter, dot styling) with only the color tokens swapped per page. The one exception is 3.5's Correlation view, which uses a scatter plot instead — a deliberate choice, not an oversight (documented in 3.5's own entry above): Pearson r is inherently about paired x/y values, and a line chart would visually imply a time-series story the correlation itself doesn't claim. It still shares the same card wrapper, grid-line styling, and color tokens as every other chart, so it reads as part of the same family even though the plot type differs for a data-shape reason.
- **Every new section matches the dark card aesthetic on a 380px viewport** — every section built this session was individually geometry-checked at 390px (10px more generous than the checklist's 380px) via `getBoundingClientRect()`, never just screenshots, per the methodology this build settled on after 1.3's screenshot-viewport mismatch false alarm. Re-ran a final check at the *exact* 380px the checklist specifies against dashboard.html (the single densest page — Gmail, Tasks, Completion Chart, Calendar, Goals, Health, Fitness, Finance, and Weekly Report all stacked) to be certain rather than assume the 10px margin was automatically safe: confirmed `body.scrollWidth === document.documentElement.clientWidth === 380`, no horizontal overflow anywhere on the page.

## Key architecture facts discovered while exploring (as of 2026-07-18, verify against current code before relying on these — they will drift as the build progresses)

- WHOOP OAuth is fully wired server-side already (`api/whoop-callback.js`, `whoop-refresh.js`, `whoop-data.js`) — not a placeholder.
- Google Calendar OAuth is fully wired server-side already (`api/google-calendar-callback.js`, `google-calendar-refresh.js`) — not a placeholder. Token handback lands on `dashboard.html#gcal_access=...`.
- There's a live cross-device sync layer on top of localStorage via Supabase: `sync.js` (`window.initCloudSync`, used by dashboard/health/gym/finance-style pages) and `vitality-bridge.js` (`window.Vitality.load()/save()`, used by the "sealed" tile pages: peak/train/vitals/notes/brand.html). Global export/import (0.6) is a portability/backup layer on top of this, not the only sync mechanism.
- Obsidian sync already runs through one shared module (`obsidian-sync.js`, `window.ObsidianSync`) and one shared localStorage config key (`obsidian_sync_config`), used by both planner.html and notes.html. What might still be duplicated is the settings UI/form for editing that config, not the underlying data.
- 11 pages total, not 7: dashboard(Main)/health/gym(Fitness)/peak/finance/planner/ability/notes/train/vitals/brand. Before 0.4, navigation was split two incomplete ways — a top "cat-tabs" row (Main/Health/Fitness/Peak/Finance/Planner/Ability only) duplicated verbatim across 9 pages, and topbar.js's bottom bar (Main/Health/Fitness/Peak/Train/Notes/Vitals/Brand, missing Finance/Planner/Ability) shown only on notes.html/brand.html, which don't have the top row. No single nav reached all 11 pages. Check current state of topbar.js and the `cat-tabs` markup before assuming 0.4's fix is still needed — it may already be done.
- Dashboard/Planner duplication (0.7): the Day Ring and Goal Ticker are dashboard-only (not duplicated). What's duplicated near-verbatim is the Projects section (including a "Gantt view coming soon" stub) and Checklists. Dashboard also has its own separate deadline-based Goals tracker (`dgt*` prefixed IDs) distinct from the daily-checklist goals (`gm*` prefixed IDs) — check whether Planner has an equivalent before deduping it.
- finance.html stored amounts internally pegged to CHF before 0.6 (variable names like `amountCHF`, exchange rates fetched from `.../latest/CHF`), with CHF/USD/EUR/GBP as display-only picker options tied to a `nw_currency` localStorage key.
- "Fuel" text found outside the already-removed Fuel page is unrelated: a post-workout nutrition nudge in health.html and a caffeine-log legend in peak.html. Different feature, not calorie logging — intentionally left alone.
